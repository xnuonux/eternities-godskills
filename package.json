{
  "name": "@eternities/godskills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --test",
    "sync:stars": "node scripts/sync-star-delta.mjs",
    "build:catalog": "node scripts/build-catalog.mjs",
    "profile": "node scripts/profile.mjs"
  }
}
