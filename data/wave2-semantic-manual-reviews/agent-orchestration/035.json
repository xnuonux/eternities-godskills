{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, bind the review to exact packet evidence, and retain only source-supported mechanisms behind independent authority, safety, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-9e3a52f5b18b3bf3",
      "neutralCapabilitySummary": "Improves large collections of tool or feature pages through codebase reconnaissance, unique content registries, metadata and heading integrity, internal-link topology, accessibility, trust signals, measured search evidence, staged batches, and live verification.",
      "neutralIntentExamples": ["reduce near-duplicate content across hundreds of tool pages", "prioritize pages with search impressions for unique content and links"],
      "legacyAliases": ["tools-page-seo-optimizer"],
      "inputs": ["site repository, page registry, templates and routes, sitemap, search-console evidence, crawl and index state, brand facts, accessibility baseline, deployment process, and ranking hypotheses"],
      "operations": ["map page generation, metadata, canonical, content, and link sources", "measure duplication, thin content, orphan pages, and heading defects", "create unique evidence-backed per-page records", "repair accessible hierarchy, slugs, canonicals, and related-page links", "batch highest-evidence pages first with completeness gates", "verify builds, live responses, rendered metadata, indexing signals, and before-after search outcomes"],
      "outputs": ["technical and content audit", "page-content registry", "priority and batch plan", "build, live, and search-performance evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["do not publish partial registry batches", "avoid fabricated author or trust claims", "preserve redirects when changing slugs", "treat ranking changes as uncertain and delayed"],
      "exclusions": ["does not keyword-stuff", "does not promise rankings", "does not generate fake experience signals", "does not deploy without authority", "does not treat accessibility solely as a ranking tactic"],
      "usefulInvariants": ["each indexable page has unique useful intent", "metadata matches visible content", "canonical and status behavior are correct", "every page participates in a meaningful link graph", "measurement precedes and follows changes"],
      "materialRisks": ["traffic loss", "duplicate content", "broken redirects", "thin generated prose", "accessibility regression", "misread search causality"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "evidence-driven-scaled-page-seo-engineering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-9e8c95e20a68cc32",
      "neutralCapabilitySummary": "Operates preregistered remote targets through a reviewed wrapper that withholds connection secrets from the agent, exposes named bounded actions, returns structured evidence, and separates read-only diagnostics from service, database, and deployment mutations.",
      "neutralIntentExamples": ["check disk and OOM history without exposing SSH credentials", "restart one declared service through an approved named target"],
      "legacyAliases": ["sshepherd"],
      "inputs": ["reviewed local executable, predeclared opaque target alias, approved action group, exact service or database object, mutation authority, timeout, rollback command, and output redaction policy"],
      "operations": ["verify executable provenance and discover its current command surface", "resolve only preregistered aliases and recipes", "prefer structured read-only health or database actions", "show the exact named mutating action and expected effect", "execute under explicit scope with timeout", "record redacted result and verify postcondition"],
      "outputs": ["structured health or log evidence", "bounded operation receipt", "postcondition and rollback status"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["reject inline hosts, usernames, ports, keys, and passwords", "remain read-only without action-specific authority", "stop on unknown aliases or unsupported commands", "do not retry ambiguous mutations"],
      "exclusions": ["does not ship or install the wrapper", "does not expose SSH configuration", "does not allow arbitrary remote shell", "does not broaden a recipe", "does not infer credential safety from branding alone"],
      "usefulInvariants": ["agent sees opaque targets only", "actions come from a reviewed allowlist", "mutations are explicit and receipted", "outputs are structured and redacted", "postconditions are checked"],
      "materialRisks": ["remote outage", "wrapper supply chain", "overbroad recipes", "ambiguous retries", "sensitive logs", "false zero-knowledge claims"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "credential-opaque-allowlisted-remote-operations",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-9ebe50028fbc328c",
      "neutralCapabilitySummary": "Builds an Expo native module or view through scaffold selection, shared TypeScript contracts, Swift and Kotlin parity, native lifecycle and event handling, autolinking, config-plugin changes, and cross-platform example validation.",
      "neutralIntentExamples": ["add a local native sensor module to an Expo app", "publish a reusable native view with typed events"],
      "legacyAliases": ["expo-module"],
      "inputs": ["current Expo and Modules API versions, local or standalone distribution target, supported platforms, TypeScript surface, native capability and permissions, view and event requirements, config changes, and example app"],
      "operations": ["choose local versus reusable scaffolding before files", "declare one cross-platform module contract", "implement matching Swift and Kotlin definitions", "bridge views, events, shared objects, lifecycle, and permissions explicitly", "add config plugins only for native project mutations", "verify autolinking, builds, type declarations, runtime behavior, cleanup, and unsupported-platform fallback"],
      "outputs": ["native module package", "typed JavaScript API", "platform implementations and config plugin", "example and cross-platform verification"],
      "effects": ["read", "write"],
      "failureBehavior": ["fail clearly on unsupported platforms", "avoid partially implemented cross-platform APIs", "release native resources on teardown", "stop when required permissions or build environments are absent"],
      "exclusions": ["does not edit generated native projects when a config plugin owns the change", "does not expose platform APIs without permission checks", "does not assume feature examples are scaffolded", "does not publish automatically"],
      "usefulInvariants": ["TypeScript and native contracts agree", "platform behavior is parity-tested", "autolinking metadata names real modules", "lifecycle owns resources", "native project mutations are reproducible"],
      "materialRisks": ["native crashes", "contract divergence", "permission misuse", "resource leaks", "build incompatibility", "autolinking failure"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "cross-platform-contract-first-expo-module-engineering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-9ebffb0dc399f4d2",
      "neutralCapabilitySummary": "Operates paid-media work through source-grounded platform modules, deterministic account and portfolio scoring, explicit objectives and conversion definitions, read-only audits, risk-ranked experiments, and fully specified mutation gates.",
      "neutralIntentExamples": ["audit cross-platform paid-media performance", "prepare a budget experiment without changing live campaigns"],
      "legacyAliases": ["ads"],
      "inputs": ["business objective, offer, geography, budget, conversion definition, account exports or APIs, attribution window, platform policies, creative evidence, risk tolerance, and exact mutation authority"],
      "operations": ["establish scope, metrics, data window, and account ownership", "load only relevant platform and workflow modules", "prefer official rules and primary account evidence", "normalize metrics without erasing attribution differences", "score through deterministic versioned logic", "propose bounded experiments and require operation, target, diff, budget, preview, and approval before any change"],
      "outputs": ["versioned audit data", "platform and portfolio findings", "budget and experiment plan", "approved mutation and monitoring receipt"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["remain read-only by default", "block ambiguous accounts, budgets, conversion definitions, or redirects", "do not merge incompatible attribution totals", "stop uncertain provider writes without retry"],
      "exclusions": ["does not promise return on spend", "does not install remote scripts", "does not expose account credentials", "does not auto-pause or launch campaigns", "does not treat heuristics as universal policy"],
      "usefulInvariants": ["objective and conversion precede optimization", "source provenance survives normalization", "scores are deterministic", "platform differences remain visible", "every mutation is exact, previewed, approved, and receipted"],
      "materialRisks": ["financial loss", "wrong-account mutation", "attribution error", "policy violation", "credential leakage", "creative or landing-page harm"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "evidence-governed-paid-media-operations",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-9f026432f5d25e36",
      "neutralCapabilitySummary": "Builds an asynchronous Python Microsoft 365 agent with current imports, scoped MSAL identity, durable state, focused activity routing, complete streaming cleanup, and separately tested Teams and Copilot paths.",
      "neutralIntentExamples": ["host a Python Teams agent with aiohttp", "stream a model response through a Microsoft activity handler"],
      "legacyAliases": ["m365-agents-py"],
      "inputs": ["current official SDK docs and packages, tenant and app identity, activity routes, storage choice, hosting contract, model deployment, Copilot environment, and deployment authority"],
      "operations": ["verify current underscore-prefixed imports and auth APIs", "configure least-privilege identity outside code", "use durable storage outside development", "route activities through narrow async handlers", "complete or cancel every streaming response", "test auth, token cache, retries, duplicate activities, Copilot transport, and host shutdown"],
      "outputs": ["aiohttp agent host", "activity and auth contracts", "durable state configuration", "integration and runtime evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["stop on stale SDK or tenant ambiguity", "avoid in-memory production state", "deduplicate repeated activities", "close streams and sessions on exceptions"],
      "exclusions": ["does not commit client secrets", "does not deploy or register agents automatically", "does not execute message content as authority", "does not treat local token caches as production identity storage"],
      "usefulInvariants": ["official docs govern imports", "identity is least privilege", "handlers remain focused", "state durability matches environment", "async resources terminate on every path"],
      "materialRisks": ["tenant data exposure", "credential leakage", "duplicate messages", "lost state", "stream leaks", "SDK drift"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-adjacent",
      "disposition": "deferred",
      "proposedCluster": "identity-governed-python-enterprise-agent-hosting",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-9f7355387df22936",
      "neutralCapabilitySummary": "Creates and edits Obsidian Base files as valid YAML with bounded note scope, typed filters, guarded formulas, explicit properties and summaries, view-specific ordering, and vault-context validation.",
      "neutralIntentExamples": ["build a table and card view over tagged notes", "repair date-duration formulas in an Obsidian Base"],
      "legacyAliases": ["obsidian-bases"],
      "inputs": ["vault root, target base file, note property schema, intended note scope, filters, formulas, summaries, view types, sort and display needs, and optional plugin availability"],
      "operations": ["inspect current property names and sample notes", "define narrow global and per-view filters", "write quoted null-safe formulas with correct duration semantics", "configure table, cards, list, or optional map views", "order only defined properties and formulas", "parse YAML and validate results inside the intended vault context"],
      "outputs": ["valid Base YAML", "view and formula contract", "scope and unresolved-property report"],
      "effects": ["read", "write"],
      "failureBehavior": ["guard absent properties", "reject undefined formula references", "omit map views when dependencies or coordinates are absent", "preserve unknown supported settings"],
      "exclusions": ["does not mutate matched notes", "does not execute formulas outside Obsidian", "does not infer property types from names alone", "does not require Bases for ordinary note lists"],
      "usefulInvariants": ["file remains valid YAML", "global filters bound every view", "formulas are null-safe", "duration values access explicit units", "view order references declared properties"],
      "materialRisks": ["silent empty views", "formula errors", "overbroad note inclusion", "YAML corruption", "plugin-dependent failures"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "typed-vault-aware-obsidian-base-authoring",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a034fbe6544c3709",
      "neutralCapabilitySummary": "Provides a provider-bound multi-chain wallet surface for balance and portfolio reads plus high-risk swaps, transfers, and blockchain transactions that require independently verified addresses, simulations, fees, slippage, and explicit final approval.",
      "neutralIntentExamples": ["read balances for a supplied public address", "prepare but do not execute a token-swap quote"],
      "legacyAliases": ["emblemai-crypto-wallet"],
      "inputs": ["exact chain and network, public address, token contracts, provider account and credential, amount, recipient, slippage and fee limits, transaction simulation, wallet ownership, and final execution authority"],
      "operations": ["verify network, address format, token identity, and provider status", "separate read-only portfolio calls from transaction preparation", "fetch balances, allowances, quote, route, fees, and slippage", "simulate and independently decode the proposed transaction", "present immutable transaction facts for explicit approval", "submit once and track chain confirmation without ambiguous replay"],
      "outputs": ["balance or portfolio evidence", "unsigned or provider-prepared transaction preview", "submission hash and confirmation status"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["default to reads only", "stop on chain, token, recipient, fee, or simulation ambiguity", "never repeat a transaction after uncertain submission", "report irreversible failure plainly"],
      "exclusions": ["does not install remote wallet packages", "does not request seed phrases or private keys", "does not promise profits", "does not swap or transfer on generic confirmation", "does not bridge chains implicitly"],
      "usefulInvariants": ["chain and contract identities are exact", "private keys remain outside the agent", "preview and approval bind identical transaction data", "submission is single-shot", "receipts are independently observable"],
      "materialRisks": ["irreversible asset loss", "wrong-chain transfer", "malicious token contract", "slippage", "provider compromise", "fee spikes", "transaction replay"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "rejected",
      "proposedCluster": "high-risk-provider-bound-blockchain-transaction-execution",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a08271c9118f2c4f",
      "neutralCapabilitySummary": "Improves prompts through explicit task contracts, representative examples, authority-aware structure, measurable evaluations, controlled revisions, and concise failure handling rather than magical phrases or hidden-reasoning requests.",
      "neutralIntentExamples": ["make a tool-routing prompt more reliable", "compare concise prompt variants on a fixed task set"],
      "legacyAliases": ["prompt-engineering"],
      "inputs": ["model and host contract, target task distribution, current prompt, authority layers, representative examples, output schema, failure cases, token budget, and evaluation metrics"],
      "operations": ["state objective, inputs, constraints, effects, and success criteria", "use minimal representative positive and negative examples", "separate instructions from untrusted data", "build reusable variable-bound templates", "evaluate baseline and candidate on held-out tasks", "change attributable components and retain the shortest version meeting gates"],
      "outputs": ["versioned prompt template", "example and constraint set", "comparative quality, safety, latency, and token evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["reject prompts that depend on exposing hidden reasoning", "do not add complexity without measured benefit", "mark model-specific behavior", "roll back regressions across important slices"],
      "exclusions": ["does not promise deterministic model output", "does not use confidence scores as calibration by default", "does not let user data override system authority", "does not optimize on evaluation examples"],
      "usefulInvariants": ["prompt intent and authority are explicit", "examples match real tasks", "evaluation is held out", "changes are attributable", "token cost is measured with quality"],
      "materialRisks": ["benchmark overfitting", "prompt injection", "context bloat", "model drift", "false reliability", "example contamination"],
      "providerCoupling": "conditional",
      "projectCoupling": "portable",
      "disposition": "independent-implementation",
      "proposedCluster": "evaluation-driven-authority-aware-prompt-engineering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a158e14236797d4f",
      "neutralCapabilitySummary": "Routes n8n workflow work to the narrowest specialist guidance, resolves live node and tool schemas before calls, defaults to declarative nodes and expressions, and keeps credentials, validation, testing, deployment, and execution under distinct gates.",
      "neutralIntentExamples": ["select the right n8n specialist before editing a workflow", "validate node parameters against the connected instance"],
      "legacyAliases": ["using-n8n-mcp-skills"],
      "inputs": ["n8n task intent, connected instance and tool server, live tool documentation, workflow identifier, node schemas, credential references, existing execution evidence, and mutation authority"],
      "operations": ["classify design, node, expression, validation, credential, deployment, execution, or debugging intent", "load only the matching specialist contract", "inspect live tool and node schemas", "prefer native nodes and expressions before custom code", "validate locally and through provider tools", "separate edit, activation, execution, and credential effects"],
      "outputs": ["specialist route", "schema-grounded workflow plan or change", "validation and execution evidence"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["stop when instance schemas or workflow access are absent", "remain read-only without exact edit authority", "do not guess remembered node parameters", "surface partial provider failures"],
      "exclusions": ["does not force slash commands", "does not load every specialist", "does not expose credential values", "does not activate or execute after validation automatically", "does not use custom code as first resort"],
      "usefulInvariants": ["one specialist owns each task phase", "live schemas override memory", "credentials remain references", "effects are independently gated", "validation precedes activation and execution"],
      "materialRisks": ["wrong workflow mutation", "credential exposure", "stale schemas", "unexpected execution", "router bloat", "provider lock-in"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "live-schema-routed-n8n-workflow-operations",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a18a1fd0715a5102",
      "neutralCapabilitySummary": "Delegates one bounded coding, review, or verification mission to an isolated Codex CLI process with explicit repository, sandbox, model, file ownership, output capture, diff inspection, and parent-run verification.",
      "neutralIntentExamples": ["run an isolated review against one subsystem", "delegate a disjoint implementation packet to a worktree"],
      "legacyAliases": ["codex-subagent"],
      "inputs": ["verified CLI and login, exact repository or worktree, one self-contained mission, model and sandbox policy, owned files, success commands, output location, timeout, and integration authority"],
      "operations": ["preflight host, login, repository, and clean isolation", "write a focused prompt with constraints and expected evidence", "launch noninteractively with stdin closed and bounded permissions", "capture the final message and process status", "inspect actual diffs and commands rather than trusting summary", "run parent verification before integration"],
      "outputs": ["isolated task result", "captured final report", "owned diff", "parent verification and integration decision"],
      "effects": ["read", "write"],
      "failureBehavior": ["terminate hung processes at the timeout", "do not share mutable files across parallel runs", "reject unverified summaries", "leave merge and publication to the parent"],
      "exclusions": ["does not install or authenticate the CLI automatically", "does not delegate vague whole-project goals", "does not bypass sandbox policy", "does not let a child merge, push, or claim completion"],
      "usefulInvariants": ["one launch owns one bounded mission", "worktrees isolate edits", "stdin cannot hold the process open", "the parent inspects diffs", "full verification remains centralized"],
      "materialRisks": ["unbounded process cost", "overlapping edits", "wrong repository", "sandbox overreach", "unverified child claims", "orphaned processes"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "isolated-bounded-coding-agent-delegation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a1a257d05974f7ff",
      "neutralCapabilitySummary": "Builds and verifies formula-backed workbook logic through a code-first JSON runtime or MCP surface, using exact sheet and cell references, typed formulas, dependency recalculation, snapshots, and exports without UI automation.",
      "neutralIntentExamples": ["create a formula-driven workpaper in an agent service", "verify changed inputs and dependent outputs without controlling Excel"],
      "legacyAliases": ["bilig-workpaper"],
      "inputs": ["reviewed package version, workbook JSON or construction spec, exact sheets and ranges, formulas and expected values, output format, filesystem boundary, and MCP or library configuration"],
      "operations": ["verify the reviewed package and package-owned challenge", "construct or load a bounded workbook", "address sheets and cells explicitly without shell concatenation", "set inputs and formulas through typed APIs", "recalculate and inspect dependent values", "export and compare snapshots with exact changed-cell evidence"],
      "outputs": ["WorkPaper JSON", "calculated values", "exported workbook artifact", "before-after cell verification"],
      "effects": ["read", "write"],
      "failureBehavior": ["reject unsupported formulas and ambiguous sheet names", "preserve inputs on calculation failure", "do not claim Excel parity beyond tested functions", "avoid executing user text through a shell"],
      "exclusions": ["does not drive desktop Excel", "does not support VBA, pivots, or every chart", "does not infer cell intent", "does not install latest unpinned packages automatically"],
      "usefulInvariants": ["cell identities are exact", "formula dependencies recalculate deterministically", "inputs and formulas remain distinguishable", "exports trace to a workbook snapshot", "changed outputs are verified"],
      "materialRisks": ["formula incompatibility", "wrong-cell mutation", "calculation drift", "path injection", "package supply chain", "false Excel-equivalence claims"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "formula-backed-code-first-workpaper-automation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a2b1095fdc34dfb0",
      "neutralCapabilitySummary": "Builds leakage-safe right-censored or competing-risk survival analyses with explicit estimands and event coding, pipeline-contained preprocessing, suitable models, censoring-aware metrics, time-point checks, calibration, and reproducible uncertainty.",
      "neutralIntentExamples": ["fit and evaluate a Cox model without leakage", "estimate cause-specific cumulative incidence under competing risks"],
      "legacyAliases": ["scikit-survival"],
      "inputs": ["time origin, event and censoring definitions, competing-event codes, predictors, grouping and temporal split constraints, prediction horizons, model assumptions, metric plan, software version, and interpretation boundary"],
      "operations": ["fix the estimand and encode events before modeling", "split at the correct subject or temporal unit", "fit imputation and preprocessing only within training folds", "select interpretable or flexible models from design evidence", "tune through nested censoring-aware validation", "evaluate discrimination, calibration, prediction error, and uncertainty at valid horizons"],
      "outputs": ["validated survival pipeline", "risk or survival predictions", "censoring-aware evaluation and diagnostics", "reproducibility report"],
      "effects": ["read", "write"],
      "failureBehavior": ["stop on invalid event coding or impossible horizons", "avoid ordinary classification metrics", "report proportional-hazards violations", "keep competing-risk estimands separate"],
      "exclusions": ["does not interpret censoring as no event", "does not fit preprocessing before splitting", "does not claim causality", "does not compare metrics outside observed support", "does not treat one concordance score as complete performance"],
      "usefulInvariants": ["time origin and event are explicit", "preprocessing is fold-local", "metrics account for censoring", "prediction horizons have support", "competing events remain correctly coded"],
      "materialRisks": ["data leakage", "informative censoring", "event miscoding", "unsupported extrapolation", "metric bias", "proportional-hazards failure"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "independent-implementation",
      "proposedCluster": "leakage-safe-censoring-aware-survival-modeling",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
