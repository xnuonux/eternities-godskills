{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, bind the review to exact packet evidence, and retain only source-supported mechanisms behind independent authority, safety, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-c00f60a67fdd022e",
      "neutralCapabilitySummary": "Improves already-correct, tested code through one behavior-preserving performance, abstraction, dead-code, or readability pass at a time, with measured justification, characterization of invariants, green tests, and immediate reversion on regression.",
      "neutralIntentExamples": ["remove a measured N+1 query without changing results", "extract proven three-way duplication behind characterization tests"],
      "legacyAliases": ["Max optimizer agent"],
      "inputs": ["explicit refactor request, exact repository revision, behavioral contract and tests, target concern, profile or duplication evidence, baseline metrics, compatibility constraints, and review scope", "hidden callers, ordering and error semantics, test gaps, generated code, database plans, concurrency, benchmark variance, user changes, and revert policy"],
      "operations": ["establish current behavior and baseline with tests and measurements", "apply one scoped behavior-preserving change whose benefit is concrete", "rerun tests and representative measurements, revert on regression, and route behavioral changes back to feature development"],
      "outputs": ["scoped refactor", "before and after complexity, duplication, or readability evidence", "test, benchmark, revert, and deferred-work report"],
      "effects": ["read", "write"],
      "failureBehavior": ["role mythology assumes unavailable reviewers and handoffs", "tests may not encode all behavior", "declarative rewrites can reduce clarity", "line and function-length thresholds are not universal"],
      "exclusions": ["does not change behavior", "does not refactor automatically", "does not mix unrelated concerns", "does not patch tests to hide regression", "does not optimize without a measured or asymptotic reason"],
      "usefulInvariants": ["explicit request leads", "baseline is green", "one concern changes", "observable behavior stays fixed", "failed changes revert", "benefit is measured", "delta receives review"],
      "materialRisks": ["semantic regression", "benchmark distortion", "test-gap exploitation", "overabstraction", "database performance regression", "user-change conflict"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "evidence-gated-behavior-preserving-refactoring",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c01f68b8be381719",
      "neutralCapabilitySummary": "Builds .NET Microsoft 365 and Teams agents with current Microsoft Agents SDK hosting, authenticated inbound routing, scoped state, MSAL or managed identity, Copilot Studio integration, cancellation, and channel-aware testing.",
      "neutralIntentExamples": ["host an authenticated Teams agent in ASP.NET Core", "connect an approved .NET client to a Copilot Studio agent"],
      "legacyAliases": ["Microsoft 365 Agents SDK for .NET"],
      "inputs": ["tenant, channels and audiences, exact SDK packages, app registration, identity flow and scopes, route and activity requirements, state store, Copilot environment, data policy, hosting target, and deployment authority", "client secrets, tenant confusion, token validation, broad service URL mapping, development auth bypass, conversation PII, error disclosure, state deletion, package drift, multichannel behavior, and external messaging authority"],
      "operations": ["verify current official APIs and pin reviewed packages", "design least-privilege registration, token validation, authorized routes, durable state, and channel-specific behavior", "test authentication, tenant and audience rejection, routing, errors, state, cancellation, and messages before controlled deployment"],
      "outputs": [".NET agent host or client", "identity, routing, state, and channel contracts", "build, auth, integration, and deployment evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["embedded APIs and package names drift", "sample configuration encourages client secrets", "development-only unauthenticated routes can leak into deployment", "exception text can disclose internals to users"],
      "exclusions": ["does not embed secrets in appsettings", "does not disable production token validation", "does not use in-memory state for durable production conversations", "does not send messages without authority", "does not trust wildcard routing without tenant tests"],
      "usefulInvariants": ["tenant, audience, and scopes are exact", "secrets use managed storage", "production routes require auth", "state retention is explicit", "errors are redacted", "messages retain channel provenance"],
      "materialRisks": ["tenant data leakage", "credential compromise", "unauthorized messaging", "state loss", "token-validation bypass", "PII disclosure", "SDK incompatibility"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "tenant-safe-microsoft-365-agent-hosting",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c0f27887da63c0c2",
      "neutralCapabilitySummary": "Authors Expo EAS workflow YAML from the current provider schema and syntax documentation, validating triggers, jobs, dependencies, expressions, runner constraints, secrets, concurrency, and deployment effects before adoption.",
      "neutralIntentExamples": ["create an EAS build workflow from the current schema", "validate needs and expression references in an existing Expo workflow"],
      "legacyAliases": ["Expo EAS CI/CD workflows"],
      "inputs": ["Expo project and exact EAS revisions, workflow goal, triggers, jobs and dependencies, build profiles, runner requirements, secrets, environments, concurrency, budget, artifacts, deployment target, and publication authority", "remote schema integrity, cache freshness, pull-request secret exposure, untrusted build scripts, supply chain, signing credentials, paid builds, concurrency races, production deployment, and rollback"],
      "operations": ["fetch and pin the current official schema and documentation", "derive the smallest workflow with valid references, scoped secrets, explicit environments, bounded concurrency, and effect gates", "validate locally, review generated jobs and scripts, dry-run where possible, and enable deployment only under authority"],
      "outputs": ["EAS workflow YAML", "schema and documentation provenance", "validation, security, cost, and rollout evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["provider schemas and job types change", "fetch-time network content is a supply-chain input", "workflow triggers can spend build credits or deploy artifacts", "forked pull requests can mishandle secrets"],
      "exclusions": ["does not rely on memorized schema values", "does not expose signing or provider secrets", "does not enable paid or production jobs without authority", "does not execute unreviewed remote scripts", "does not call schema validity runtime proof"],
      "usefulInvariants": ["schema revision is recorded", "job references resolve", "secrets are scoped", "environments are explicit", "concurrency is bounded", "deployments have separate gates", "workflow validates"],
      "materialRisks": ["unauthorized deployment", "secret disclosure", "build cost", "supply-chain execution", "workflow race", "schema drift", "signing-key compromise"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "schema-verified-mobile-cicd-workflows",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c191463ab01200a2",
      "neutralCapabilitySummary": "Updates a SpecKit installation by comparing a pinned release to a local manifest, classifying customized files, previewing changes, creating a restorable backup, applying only approved selective updates, resolving conflicts, validating results, and retaining bounded backup history.",
      "neutralIntentExamples": ["check a SpecKit project for updates without changing files", "apply an approved pinned template update while preserving local customizations"],
      "legacyAliases": ["SpecKit safe updater"],
      "inputs": ["exact project and SpecKit state, current manifest, pinned target release, clean worktree, customization policy, backup location and retention, conflict choices, scripts and network provenance, and explicit proceed or rollback authority", "moving releases, plugin scripts, GitHub API, normalized-hash collisions, symlinks, user changes, automatic subprocesses, backup deletion, rollback completeness, and editor automation"],
      "operations": ["inspect exact updater code and target release before execution", "run a check-only comparison and present add, update, remove, preserve, and conflict actions with backup plan", "after approval, create and verify a backup, apply selective changes, resolve conflicts explicitly, validate the installation, and test rollback"],
      "outputs": ["update plan or updated SpecKit tree", "manifest and customization state", "backup, conflict, validation, and rollback receipts"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["a wrapper script has broad filesystem and network authority", "normalized hashes can hide meaningful whitespace changes", "automatic companion command invocation broadens scope", "backup retention deletes recovery points"],
      "exclusions": ["does not install a moving branch", "does not proceed without explicit approval", "does not force-overwrite customized files by default", "does not invoke companion workflows automatically", "does not prune backups without validated targets and authority"],
      "usefulInvariants": ["target release is immutable", "worktree state is known", "customizations remain visible", "preview precedes mutation", "backup restores exact pre-state", "conflicts are explicit", "validation follows update"],
      "materialRisks": ["customization loss", "supply-chain execution", "partial rollback", "wrong-target writes", "backup deletion", "manifest drift", "unapproved subprocess effects"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "manifest-bound-customization-preserving-updates",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c1beffca515b42f8",
      "neutralCapabilitySummary": "Automates authorized browser and Windows desktop interactions for reproducible analysis evidence, using fresh semantic snapshots, bounded UI actions, screenshots, logs, and optional network observation while keeping reverse-engineering and security activity scoped to approved targets.",
      "neutralIntentExamples": ["reproduce a web UI sequence and capture a network trace on an owned target", "collect screenshots from an authorized desktop-analysis tool"],
      "legacyAliases": ["reverse browser and desktop automation"],
      "inputs": ["exact authorized application or URL, allowed accounts and actions, browser or desktop mode, interaction script, test data, network-capture scope, evidence policy, tool revisions, cleanup, and stop conditions", "credentials and sessions, third-party targets, form submissions, security payloads, CAPTCHA policy, TLS interception, personal data, downloads, desktop control, tool installation, and destructive or offensive effects"],
      "operations": ["verify target authority, tool provenance, session identity, and evidence boundary", "capture a fresh accessibility or UI state before each bounded action and wait for stable transitions", "collect minimal screenshots, logs, and approved traffic, redact evidence, close processes, and verify no persistent changes"],
      "outputs": ["reproducible UI action trace", "screenshots and approved network evidence", "session, tool, target, cleanup, and limitation record"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["selectors and visual targets drift", "automatic installation broadens supply-chain exposure", "network interception captures secrets", "security-oriented examples can facilitate unauthorized probing"],
      "exclusions": ["does not bypass CAPTCHA or access controls", "does not submit forms or payloads without authority", "does not intercept unrelated traffic", "does not install heavy tooling automatically", "does not reuse stale element references", "does not leave controlled processes running"],
      "usefulInvariants": ["target and account are exact", "actions are allowlisted", "UI state is refreshed", "network capture is scoped", "evidence is redacted", "processes close", "persistent effects are verified"],
      "materialRisks": ["unauthorized access", "credential capture", "desktop damage", "privacy breach", "supply-chain compromise", "wrong-target action", "offensive misuse"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "authorization-bound-ui-analysis-automation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c24a411d97100c2d",
      "neutralCapabilitySummary": "Ports suitable scientific Python hot paths to the smallest maintained NVIDIA GPU layer only after profiling, then preserves a coherent device-resident path, validates numerical semantics, benchmarks synchronized end-to-end performance, and keeps or rejects the port from measured value.",
      "neutralIntentExamples": ["test whether a large NumPy hot path benefits from CuPy", "remove host-device round trips from an existing GPU-native pipeline"],
      "legacyAliases": ["NVIDIA scientific Python GPU optimization"],
      "inputs": ["correct CPU implementation, representative data and expected outputs, numerical tolerances, profiler evidence, hardware and CUDA stack, package lock, shapes and dtypes, memory budget, production metric, portability need, and installation authority", "numerical drift, asynchronous timing, CPU fallback, host-device transfers, device OOM, approximate algorithms, package and driver compatibility, GPU contention, thermal and power limits, multi-GPU complexity, and network installs"],
      "operations": ["capture the semantic contract and synchronized CPU baseline", "prove GPU suitability and adopt the least disruptive maintained backend while minimizing transfers", "compare correctness and edge cases, benchmark warmed synchronized kernel and end-to-end paths, and retain a CPU fallback or reject the port"],
      "outputs": ["validated GPU implementation or rejection rationale", "CPU and GPU correctness comparison", "hardware, environment, memory, transfer, kernel, and end-to-end benchmark evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["GPU enqueue timing overstates speed", "approximate and exact methods are not equivalent", "silent CPU fallback invalidates claims", "large data and temporaries exceed memory", "embedded library status changes"],
      "exclusions": ["does not port without profiling", "does not compare unequal semantics", "does not claim speed without synchronization", "does not install packages automatically", "does not remove a working CPU path when portability is required", "does not use reduced precision without contract approval"],
      "usefulInvariants": ["CPU contract is preserved", "data is representative", "environment is exact", "transfers are counted", "correctness precedes speed", "timing is synchronized", "keep or reject is evidence-driven"],
      "materialRisks": ["wrong scientific results", "false speedup", "GPU OOM", "driver incompatibility", "thermal instability", "dependency conflict", "hardware lock-in"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "semantics-first-gpu-acceleration",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c2c80e7a5eec2b0c",
      "neutralCapabilitySummary": "Builds reproducible NeuroKit2 workflows for physiological time-series inspection, cleaning, peak and event correction, interval and event analysis, multimodal alignment, variability, respiration, and complexity with explicit sampling, units, method, quality, and interpretation boundaries.",
      "neutralIntentExamples": ["audit an ECG-to-HRV pipeline with corrected peaks and duration-aware metrics", "align multimodal physiology events on a declared clock without making a diagnosis"],
      "legacyAliases": ["NeuroKit2 physiological signal analysis"],
      "inputs": ["authorized deidentified signals, modality and device, sampling rate and clock, units, channel and event schema, recording context and duration, preprocessing history, artifacts and gaps, analysis methods, package lock, scientific question, and validation criteria", "participant privacy, device validity, diagnosis boundary, resampling and filter order, peak correction, epoch leakage, multimodal clock drift, method sensitivity, short recordings, missingness, and network or package provenance"],
      "operations": ["inspect timebase, units, channels, gaps, artifacts, and deidentification before transformation", "apply a pinned modality-specific preprocessing order and preserve corrections and runtime schemas", "validate quality and sensitivity, align events and modalities explicitly, and qualify physiological interpretation against study design"],
      "outputs": ["processed signals and features", "quality, event, interval, variability, and complexity diagnostics", "method, environment, alignment, provenance, and limitation record"],
      "effects": ["read", "write"],
      "failureBehavior": ["schema and defaults drift across versions", "filter and peak choices materially change features", "short or nonstationary signals invalidate metrics", "RSA and related measures are easily overinterpreted", "medical-looking outputs invite diagnosis"],
      "exclusions": ["does not diagnose or validate a device", "does not process identified data without authority", "does not infer sampling or units", "does not hide corrected peaks or exclusions", "does not call one preprocessing method ground truth", "does not interpret metrics outside context"],
      "usefulInvariants": ["data are deidentified", "sampling and units are explicit", "raw data remain immutable", "preprocessing order is recorded", "events use one clock", "quality precedes summary", "methods and sensitivity are reported"],
      "materialRisks": ["clinical overclaim", "privacy breach", "signal distortion", "clock misalignment", "invalid HRV or complexity", "version drift", "irreproducible research"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "method-aware-physiological-signal-analysis",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c345a877557362f9",
      "neutralCapabilitySummary": "Lowers time or space complexity in existing code by characterizing behavior, stating current and target complexity, locating the dominant term, applying one semantics-preserving transformation, verifying, measuring, and reverting or stopping when evidence fails.",
      "neutralIntentExamples": ["replace repeated linear lookup with a precomputed index while preserving order", "stream a file-processing path to reduce peak memory without changing errors"],
      "legacyAliases": ["complexity cuts"],
      "inputs": ["exact hot path, behavioral and error contract, existing or characterization tests, dominant input dimensions and realistic bounds, profiler or complexity evidence, baseline time and memory, compatibility requirements, and benchmark environment", "ordering and stability, concurrency limits, database query semantics, cache invalidation, memory tradeoffs, benchmark noise, caller assumptions, input adversaries, and revert history"],
      "operations": ["prove the bottleneck and state current complexity and semantics", "apply one selected asymptotic or explicitly labeled constant-factor transformation", "rerun unchanged tests, revert on any regression, benchmark representatively, state new complexity and risk, and stop when lower bounds or repeated failures are reached"],
      "outputs": ["one scoped optimization", "current and new complexity with bottleneck evidence", "tests, benchmark, semantic-risk, revert, and stop receipts"],
      "effects": ["read", "write"],
      "failureBehavior": ["Big-O can ignore real I/O and constants", "a fixed three-revert rule is heuristic", "benchmarks can be unrepresentative", "parallelizing independent awaits can violate provider rate or resource limits", "tests may omit semantics"],
      "exclusions": ["does not optimize without green tests", "does not weaken tests", "does not stack transformations", "does not change ordering or errors silently", "does not claim unmeasured speedup", "does not optimize small cold paths"],
      "usefulInvariants": ["bottleneck is exact", "input dimension is defined", "one transformation lands at a time", "tests are unchanged and green", "semantic risk is explicit", "measurement is reproducible", "failed work reverts"],
      "materialRisks": ["semantic regression", "rate-limit overload", "memory tradeoff", "benchmark fraud", "cache inconsistency", "test-gap exploitation", "premature complexity"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "verified-asymptotic-complexity-reduction",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c4421cb47f43aba1",
      "neutralCapabilitySummary": "Coordinates substantial work across non-overlapping agents only when independent lanes materially justify overhead, preserving scoped authority, source-linked context, typed outputs, user approvals, conflict resolution, and coordinator-owned integration verification.",
      "neutralIntentExamples": ["delegate disjoint repository modules while retaining integrated verification", "run independent research lanes and reconcile disagreements against sources"],
      "legacyAliases": ["Codex subagent orchestration"],
      "inputs": ["objective, independent task graph, agent ceiling and capabilities, exact context and evidence per lane, disjoint write ownership, output schemas, resource budgets, authority boundaries, integration plan, and verification commands", "shared-state conflict, context leakage, prompt injection, correlated models, duplicated work, agent failure, cost and latency, user messages during execution, external approvals, and final responsibility"],
      "operations": ["keep critical coupled work local and select only independent high-value lanes", "dispatch bounded non-overlapping assignments with minimal complete context, scoped authority, and no recursive delegation", "review outputs, resolve conflicts, integrate changes, run combined verification, and keep consequential approvals with the user"],
      "outputs": ["task-scoped agent results", "ownership, cost, failure, and evidence ledger", "coordinator-verified integrated result"],
      "effects": ["read", "write"],
      "failureBehavior": ["delegation can cost more than inline work", "agents may modify overlapping state despite prompts", "results can be correlated or stale", "a coordinator can accept claims without rerunning evidence"],
      "exclusions": ["does not delegate trivial or immediate blocking work", "does not exceed agent ceilings", "does not overlap write ownership", "does not broaden user authority", "does not accept subagent completion as verification", "does not let leaf agents redelegate"],
      "usefulInvariants": ["lanes are genuinely independent", "ownership is disjoint", "context is source-linked", "authority is scoped", "failures remain visible", "coordinator verifies integration", "user approvals remain user-owned"],
      "materialRisks": ["parallel conflicts", "context leakage", "token cost", "correlated error", "authority diffusion", "integration regression", "stale evidence"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "bounded-nonoverlapping-agent-coordination",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c45687d6ae09994e",
      "neutralCapabilitySummary": "Diagnoses failures in one named Kubernetes namespace by reconstructing pod, event, prior-log, resource, dependency, queue, Redis, health, telemetry, volume, and deployed-configuration timelines before proposing any change.",
      "neutralIntentExamples": ["determine whether simultaneous pod restarts share a Redis failure", "distinguish historical restart counts from an active CrashLoopBackOff"],
      "legacyAliases": ["Buttercup Kubernetes failure diagnosis"],
      "inputs": ["authorized cluster and exact context, crs namespace, symptom and time window, affected pods and services, deployment revision and intended Helm values, read-only Kubernetes access, log and telemetry policy, and expected healthy state", "wrong-cluster context, secrets in logs and environment, kubectl exec, Redis data, production load, broad log collection, sensitive vulnerability queues, stale events, and mutation authority"],
      "operations": ["verify cluster identity, namespace, read-only permissions, symptom, and time boundary", "correlate pod states, events, previous and current logs, resources, shared dependencies, queue lag, health, telemetry, volumes, and deployed values", "rank evidence-backed root-cause hypotheses and propose the smallest separately authorized remediation and verification"],
      "outputs": ["incident timeline and active-state determination", "dependency, resource, queue, and configuration evidence", "ranked root cause and unexecuted remediation plan"],
      "effects": ["read"],
      "failureBehavior": ["hardcoded service and queue names drift", "commands expose logs, environment, and Redis state", "high restart counts may be historical", "grep filters can hide context", "exec creates a higher-risk cluster surface"],
      "exclusions": ["does not debug another namespace implicitly", "does not mutate pods, Redis, queues, Helm, or volumes", "does not dump all logs by default", "does not expose secrets", "does not infer current failure from counters alone", "does not execute a remediation during diagnosis"],
      "usefulInvariants": ["cluster and namespace are exact", "timeline distinguishes historical from active", "previous logs are retained", "shared dependencies are checked first", "actual config is compared to intended", "evidence precedes remediation"],
      "materialRisks": ["wrong-cluster access", "secret disclosure", "production load", "misdiagnosed cascade", "Redis data exposure", "vulnerability-data leakage", "unauthorized remediation"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "timeline-first-kubernetes-cascade-diagnosis",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-c5d9c5fbfb19631f",
      "neutralCapabilitySummary": "Outlines an AI-agent delivery lifecycle from purpose and success criteria through tool, memory, single or multi-agent architecture, state orchestration, implementation, safety, evaluation, and deployment, while delegating most detail to assumed companion skills and named frameworks.",
      "neutralIntentExamples": ["derive the minimum agent architecture from a real workflow", "evaluate whether an agent needs tools, memory, or multiple workers"],
      "legacyAliases": ["AI agent development workflow bundle"],
      "inputs": ["user workflow and success criteria, environment, deterministic alternatives, model and tool capabilities, effect and authority boundaries, state and memory needs, privacy, latency and cost budget, failure modes, evaluation corpus, deployment, and rollback", "prompt injection, tool misuse, memory privacy, model drift, multi-agent overhead, framework lock-in, retries and duplicate effects, observability, human escalation, and external action authority"],
      "operations": ["model the user workflow and prove where an agent adds value over deterministic software", "select the smallest architecture and define typed tool, state, memory, authority, and failure contracts", "implement incrementally and evaluate representative outcomes, safety, cost, latency, degradation, and rollback before deployment"],
      "outputs": ["agent architecture and implementation plan", "tool, memory, state, authority, and failure contracts", "evaluation, safety, cost, and deployment evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source is a broad checklist coupled to assumed skills", "framework-first selection overbuilds simple workflows", "memory and multiple agents amplify privacy and cost", "generic quality gates do not prove behavior"],
      "exclusions": ["does not choose a framework before requirements", "does not add memory or agents without measured need", "does not grant broad tool authority", "does not deploy from checklist completion", "does not treat model output as proof", "does not omit deterministic fallback"],
      "usefulInvariants": ["workflow and success criteria lead", "architecture is minimal", "tools are typed and scoped", "memory is governed", "effects are idempotent where possible", "evaluation is representative", "rollback exists"],
      "materialRisks": ["unsafe tool action", "private-memory leakage", "cost explosion", "framework lock-in", "prompt injection", "false autonomy", "deployment regression"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "minimal-governed-agent-system-delivery",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
