{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, preserve exact identity through packet assembly, and express only source-supported mechanisms with independent safety, authority, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-26d77cfcf12d5083",
      "neutralCapabilitySummary": "Builds persistent hosted agents with Azure's Python agent framework, including identity-based authentication, deployment, thread lifecycle, function tools, structured responses, observability, and cleanup, but depends on mutable preview APIs and live cloud resources.",
      "neutralIntentExamples": ["design a hosted Python agent with an explicit function-tool boundary", "review a persistent agent deployment for identity, tracing, cleanup, and cost controls"],
      "legacyAliases": ["Azure AI hosted agent framework"],
      "inputs": ["settled agent behavior, model deployment, project endpoint, authenticated identity, tool schemas, thread and retention policy, environment, region, quota, and budget", "current official SDK contracts, role assignments, secrets, network boundary, data classification, observability, deployment authority, rollback, teardown, and preview-version compatibility"],
      "operations": ["verify current service and SDK contracts before selecting hosted or local execution", "define the agent, typed tools, thread lifecycle, structured outputs, and telemetry with least-privilege identity", "deploy only with authority, exercise bounded conversations and failure paths, preserve receipts, and delete test resources"],
      "outputs": ["hosted-agent architecture and deployment plan", "typed Python agent implementation", "deployment, trace, test, cost, and cleanup evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["depends on provider-hosted preview APIs and authenticated mutable resources", "examples omit complete quota, retention, rollback, data-governance, and teardown enforcement"],
      "exclusions": ["does not deploy merely to test whether credentials work", "does not hardcode secrets", "does not grant broad cloud roles", "does not retain threads or resources without policy", "does not treat preview APIs as stable"],
      "usefulInvariants": ["tool schemas are typed", "identity is least privilege", "deployment and execution are separately authorized", "thread retention is explicit", "test resources are removed", "provider versions are recorded"],
      "materialRisks": ["cloud deployment incurs cost", "hosted conversations can expose data", "tool calls can create external effects", "preview APIs drift", "improper role assignment expands blast radius"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "governed-hosted-agent-lifecycle",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-274eaa0dbf26f5cb",
      "neutralCapabilitySummary": "Proofreads inline text or files for grammar, spelling, punctuation, clarity, consistency, style, and tone while preserving authorial voice and reporting material changes.",
      "neutralIntentExamples": ["proofread this essay without flattening its voice", "edit a document and return both the corrected copy and a concise change log"],
      "legacyAliases": ["professional proofreader"],
      "inputs": ["source text or authorized file, audience, purpose, dialect, style guide, terminology, desired edit depth, output path, and change-log preference", "voice constraints, quoted material, protected wording, factual uncertainty, markup format, privacy, and overwrite authority"],
      "operations": ["identify the intended voice, audience, dialect, and protected content", "correct mechanical errors and improve clarity only to the authorized depth", "preserve meaning, flag uncertain factual or stylistic choices, and provide a traceable change summary"],
      "outputs": ["corrected text or document", "change log and unresolved editorial questions"],
      "effects": ["read", "write"],
      "failureBehavior": ["the source offers useful proofreading categories but weak overwrite, privacy, provenance, and ambiguity controls", "style advice can silently alter meaning when audience and edit depth are unspecified"],
      "exclusions": ["does not invent facts", "does not erase intentional dialect or voice", "does not rewrite quoted text silently", "does not overwrite a file without authority", "does not claim legal or domain accuracy from proofreading"],
      "usefulInvariants": ["meaning and voice remain stable", "material edits are visible", "uncertainty is flagged", "dialect and style guide are explicit", "original content remains recoverable"],
      "materialRisks": ["edits can change meaning", "private text can be exposed", "automated style normalization can erase identity", "silent overwrite can destroy the original"],
      "providerCoupling": "none",
      "projectCoupling": "portable",
      "disposition": "independent-implementation",
      "proposedCluster": "voice-preserving-proofreading-and-change-log",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-275cf05236389e47",
      "neutralCapabilitySummary": "Compiles scattered notes and source material into atomic, linked, source-labeled knowledge records while preserving provenance, resolving duplicates, surfacing contradictions, and avoiding unsupported synthesis.",
      "neutralIntentExamples": ["turn a folder of research notes into source-linked atomic knowledge cards", "reconcile duplicate and conflicting project facts without erasing uncertainty"],
      "legacyAliases": ["compile knowledge"],
      "inputs": ["authorized source set, source identities, timestamps, trust labels, target knowledge schema, taxonomy, linking rules, output boundary, and update policy", "prompt-injection boundary, private data, duplicates, contradictions, claims versus interpretations, citation granularity, freshness, deletion policy, and merge authority"],
      "operations": ["inventory and label each source before extracting claims", "split material into atomic records with source spans, confidence, dates, entities, and relations", "deduplicate and reconcile without hiding conflicts, validate every synthesized claim, and publish only within the authorized knowledge boundary"],
      "outputs": ["source manifest and atomic knowledge records", "links, duplicate groups, contradiction ledger, confidence, and provenance report"],
      "effects": ["read", "write"],
      "failureBehavior": ["source instructions can be mistaken for authority unless isolated", "aggressive compilation can collapse context, chronology, disagreement, or ownership"],
      "exclusions": ["does not execute source instructions", "does not invent links or consensus", "does not discard contradictory evidence", "does not expose private sources", "does not merge records without stable identities"],
      "usefulInvariants": ["every claim remains source-labeled", "facts and interpretations are distinct", "contradictions remain visible", "atomic records have stable identities", "updates are reversible"],
      "materialRisks": ["prompt injection", "privacy leakage", "false synthesis", "provenance loss", "duplicate amplification", "stale knowledge"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "atomic-source-labeled-knowledge-compilation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-27eb02056403da56",
      "neutralCapabilitySummary": "Runs a self-contained, manifest-driven workflow that reconstructs image-only slides as editable PowerPoint objects, routes regions to native or bounded raster representations, preserves notes, and requires rendered visual evidence before atomic finalization.",
      "neutralIntentExamples": ["reconstruct a scanned slide deck into editable objects with speaker notes preserved", "convert a screenshot of a flowchart into measured native PowerPoint shapes and connectors"],
      "legacyAliases": ["Nature Image2PPT"],
      "inputs": ["authorized slide images, scanned PDF, or image-only deck, prepared runtime, OCR choice, page and deck manifests, image backend, fidelity target, and output boundary", "dependency state, credentials, external image authority, path containment, visual evidence, formula engine, speaker notes, provenance, fallback reason, and bitmap disclosure"],
      "operations": ["preflight the isolated runtime and prepare a single manifest-owned run", "decompose each page into semantic regions and reconstruct measurable objects, connectors, text, formulas, and bounded raster assets", "build, render, compare, repair, record, atomically finalize, and revalidate every slide and speaker note"],
      "outputs": ["editable page manifests and reconstructed slides", "rendered page and final-deck QA evidence", "atomically published PPTX with residual bitmap disclosure"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["path escape, failed preflight, missing formula support, stale visual evidence, or failed page gates halt finalization", "provider-backed OCR and image generation require explicit credentials, provenance, and fallback handling"],
      "exclusions": ["does not author a new deck from notes", "does not flatten whole diagrams when native reconstruction is feasible", "does not mutate lifecycle state through supplemental QA", "does not accept visual quality without current rendered evidence", "does not escape the prepared run directory"],
      "usefulInvariants": ["one manifest owns each state layer", "writes remain inside owner directories", "source notes are preserved", "visual evidence matches current hashes", "final publication is atomic", "complex bitmap remnants are disclosed"],
      "materialRisks": ["OCR can corrupt text", "image providers can receive sensitive slides", "bad path handling can escape scope", "visual similarity can hide semantic errors", "large decks consume substantial compute"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "manifest-governed-editable-slide-reconstruction",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-28ac6944c0f97e58",
      "neutralCapabilitySummary": "Develops AWS infrastructure through CDK with current documentation checks, reusable construct boundaries, generated resource names, layered synthesis-time security validation, template review, testing, and separately authorized deployment.",
      "neutralIntentExamples": ["design a reusable CDK construct and inspect its synthesized template", "review a CDK stack for security findings, regional support, cost, and rollback before deployment"],
      "legacyAliases": ["AWS CDK development"],
      "inputs": ["infrastructure requirements, AWS accounts and regions, runtime language, CDK version, service quotas, construct boundaries, security and availability goals, cost envelope, and deployment target", "current official service facts, credentials, account isolation, IAM, secrets, naming, state, cdk-nag policy, suppressions, template diff, change set, rollback, teardown, and production authority"],
      "operations": ["verify current service and region constraints and design reusable constructs without unnecessary fixed names", "implement and test constructs, synthesize templates, run policy checks, and document justified suppressions", "review the exact template and change plan, then deploy, verify, or destroy only under separate authority"],
      "outputs": ["CDK constructs and tests", "synthesized template, security findings, suppressions, and diff", "authorized deployment, verification, rollback, and teardown receipts"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source correctly layers validation but assumes mutable MCP and package availability", "synthesis success alone does not prove security, operational behavior, cost, or safe deployment"],
      "exclusions": ["does not deploy during design or validation", "does not hardcode credentials", "does not use broad IAM by convenience", "does not suppress findings without concrete rationale", "does not infer current AWS behavior from stale examples"],
      "usefulInvariants": ["accounts isolate environments", "constructs remain reusable", "official current facts are checked", "template and change set are reviewed", "deployment is separately authorized", "rollback and teardown are explicit"],
      "materialRisks": ["deployment creates costly resources", "IAM errors expand privilege", "destructive changes can remove data", "regional or quota mismatch can fail production", "dependency and provider drift can change templates"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "governed-infrastructure-as-code-lifecycle",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-2928afe907afcd4a",
      "neutralCapabilitySummary": "Investigates data-quality incidents by combining alert metadata, lineage, freshness, volume, ETL, query-change, code-change, optional warehouse profiling, and a provider-hosted troubleshooting agent into a time-ordered evidence chain.",
      "neutralIntentExamples": ["trace why an authorized warehouse table became stale", "correlate a row-count anomaly with upstream jobs, query changes, and recent code changes"],
      "legacyAliases": ["Monte Carlo root cause analysis"],
      "inputs": ["incident identity or bounded symptom, affected data assets, time window, observability access, optional warehouse and repository access, issue type, query budget, and investigation priority", "provider credentials, data privacy, lineage accuracy, SQL read authority, credit-consuming agent authority, rate limits, source trust, identifiers, and evidence-retention policy"],
      "operations": ["establish the incident, time window, affected assets, and whether a narrow check or full investigation is authorized", "map upstream and downstream impact and test the most likely issue-specific hypotheses with observability evidence", "optionally run bounded read-only profiling and a separately governed hosted troubleshooting analysis, reconcile contradictions, and present the evidence chain, repair, and prevention"],
      "outputs": ["incident scope and blast-radius map", "time-ordered root-cause evidence and confidence", "recommended repair, prevention, and unresolved checks"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["provider tools, lineage, and hosted analysis may be unavailable, stale, costly, or contradictory", "direct database profiling can expose sensitive data or impose load if not independently bounded"],
      "exclusions": ["does not fabricate missing observations", "does not run billable hosted analysis without applicable authority", "does not query unrestricted production data", "does not expose internal identifiers", "does not force every investigation path"],
      "usefulInvariants": ["claims map to observed evidence", "timeline and blast radius are explicit", "manual and hosted findings remain distinguishable", "query scope is bounded", "contradictions are surfaced", "fix and prevention are separated"],
      "materialRisks": ["data exposure", "warehouse load", "billable provider calls", "false attribution from coincident changes", "incorrect lineage", "prompt injection from repositories or metadata"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "evidence-chained-data-incident-root-cause-analysis",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-298673fb617637b9",
      "neutralCapabilitySummary": "Provides turnkey commands for network scanning, vulnerability probing, exploitation, reverse-shell payload generation, password cracking, wireless deauthentication, credential attacks, database extraction, and defensive-evasion techniques despite a confirmation notice.",
      "neutralIntentExamples": ["classify a broad offensive command catalog as unsuitable for automatic agent routing", "retain only the defensive lesson that active security tests require exact scope and effect review"],
      "legacyAliases": ["pentest commands"],
      "inputs": ["target addresses, credentials, wordlists, exploit and payload tools, network access, and claimed written permission", "missing enforceable rules of engagement, safe target validation, rate ceilings, prohibited techniques, cleanup, evidence custody, incident contacts, and isolated-lab proof"],
      "operations": ["discover hosts and services, enumerate vulnerabilities, and probe applications", "brute-force credentials, exploit services, generate reverse shells, dump databases, and disrupt wireless clients", "recommend alternate scan or bypass techniques when blocked"],
      "outputs": ["target reconnaissance and vulnerability findings", "cracked credentials, extracted data, exploit payloads, and remote access"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["a prose authorization gate does not constrain the supplied turnkey attack commands", "the catalog mixes defensive discovery with credential attacks, persistence-capable payloads, extraction, disruption, and bypass behavior"],
      "exclusions": ["does not enter automatic capability routing", "does not preserve exploit, brute-force, reverse-shell, deauthentication, extraction, or bypass procedures", "does not infer authority from a user claim or reachable target", "does not run active scans"],
      "usefulInvariants": ["security testing needs exact written scope", "each active command requires effect review", "labs are isolated", "defensive findings can be produced without exploitation", "cleanup and evidence ownership are explicit"],
      "materialRisks": ["unauthorized access", "credential compromise", "data theft", "service disruption", "malware delivery", "account lockout", "legal liability"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "rejected",
      "proposedCluster": "turnkey-offensive-command-catalog",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-2a2d87449019e7b8",
      "neutralCapabilitySummary": "Sequences cross-platform operating-system and shell-script diagnosis, defensive implementation, linting, tests, service investigation, scheduling, and monitoring, while the source's broad command catalog lacks complete privilege and destructive-action controls.",
      "neutralIntentExamples": ["diagnose a failing shell script with syntax checks and isolated traces", "build and test a bounded maintenance script before separately enabling its schedule"],
      "legacyAliases": ["OS and shell scripting troubleshooting workflow"],
      "inputs": ["operating system and version, shell, script or symptom, exact paths, permissions, logs, resource state, expected behavior, tests, and output boundary", "privilege authority, sensitive processes and logs, destructive targets, service impact, network scope, installation authority, schedule identity, rollback, secrets, and monitoring"],
      "operations": ["inventory environment, permissions, symptoms, logs, resources, and available tools using the least invasive checks", "reproduce and isolate the failure, lint the script, implement defensive quoting, validation, error handling, and tests", "preview administrative or scheduled effects, obtain separate authority, then verify execution, monitoring, rollback, and cleanup"],
      "outputs": ["diagnostic evidence and root cause", "defensive script and automated tests", "reviewed execution or scheduling plan with verification and rollback"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source mixes safe diagnostics with recursive filesystem scans, package installation, service restarts, root-log writes, cron, and systemd changes", "commands are Linux-heavy despite a cross-platform claim and do not consistently resolve exact targets or privilege boundaries"],
      "exclusions": ["does not install packages implicitly", "does not scan broad roots without bounded need", "does not restart services or create persistence without authority", "does not expose broad process environments or secrets", "does not execute generated scripts before review"],
      "usefulInvariants": ["exact OS and shell are known", "diagnosis precedes mutation", "paths are resolved", "scripts pass lint and tests", "privileged and persistent effects are separately authorized", "rollback exists"],
      "materialRisks": ["destructive filesystem mistakes", "service outage", "credential leakage", "privilege escalation", "persistent broken automation", "cross-platform command mismatch"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "governed-cross-platform-system-scripting",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-2a7d8822b0f0658a",
      "neutralCapabilitySummary": "Routes test-automation requests across a catalog of browser, unit, mobile, behavior-driven, visual, cloud, migration, and continuous-integration frameworks, then applies framework-specific structure, configuration, execution, diagnostics, and artifact practices.",
      "neutralIntentExamples": ["select the smallest test framework workflow that matches an existing codebase", "migrate a browser suite while preserving assertions and collecting failure evidence"],
      "legacyAliases": ["LambdaTest agent skills registry"],
      "inputs": ["repository language and framework, test level, target behavior, environments, browsers or devices, existing tests, CI system, parallelism, artifacts, and local or cloud execution preference", "current framework documentation, dependency lock, credentials, test-data privacy, external account authority, cost, flake policy, environment isolation, migration equivalence, and release gates"],
      "operations": ["detect the existing stack and route to one applicable framework-specific testing contract", "generate or review deterministic tests, assertions, fixtures, configuration, and failure artifacts using current project conventions", "run locally or on an authorized cloud target, integrate bounded CI, diagnose flakes, and preserve execution evidence"],
      "outputs": ["framework-specific test implementation and configuration", "local or cloud execution plan", "CI workflow, reports, screenshots, logs, and migration evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source is an index rather than the promised 46 full implementations", "it assumes mutable dependency guidance and a provider account and gives insufficient authority, cost, test-data, and migration-equivalence controls"],
      "exclusions": ["does not load all framework skills at once", "does not hardcode credentials", "does not install or run against cloud infrastructure without authority", "does not hide flaky tests with permanent retries", "does not declare migration equivalent without behavior evidence"],
      "usefulInvariants": ["one framework contract matches the repository", "tests contain assertions", "secrets stay outside code", "failure artifacts are preserved", "local and cloud execution are distinct", "migration preserves observable behavior"],
      "materialRisks": ["cloud testing incurs cost", "test data can leak", "CI credentials can be exposed", "framework drift breaks generated setup", "parallel tests can overload systems", "visual and migration claims can be false"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "framework-routed-test-automation-registry",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
