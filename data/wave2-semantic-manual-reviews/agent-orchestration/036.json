{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, bind the review to exact packet evidence, and retain only source-supported mechanisms behind independent authority, safety, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-a3e537f83a806b64",
      "neutralCapabilitySummary": "Designs orchestration- or choreography-based sagas for long-running distributed business operations using durable state, correlated idempotent steps, timeouts, asynchronous commands, reverse-order compensations, and explicit completed or failed terminal states.",
      "neutralIntentExamples": ["coordinate inventory, payment, and shipment with independently testable compensations", "recover a timed-out workflow without duplicating an already completed external effect"],
      "legacyAliases": ["distributed transaction saga orchestration"],
      "inputs": ["business invariant, participating services, ordered effects, message contracts, idempotency identities, compensation semantics, timeouts, retry policy, terminal states, and consistency requirements", "durable store, message delivery guarantees, concurrency, duplicate and late events, outbox or inbox policy, compensation failure, observability, privacy, operator intervention, and recovery objectives"],
      "operations": ["model forward and compensating effects with durable state transitions and exact correlation identities", "execute idempotent steps through reliable messaging while handling retries, duplicates, timeouts, and concurrent events", "compensate only confirmed effects, persist every transition, surface irrecoverable states, and verify business invariants at termination"],
      "outputs": ["saga state machine and message contracts", "durable orchestration or choreography implementation", "forward, compensation, timeout, retry, and invariant evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the sample mutates saga progress without version or concurrency guards", "event publication and state persistence lack an atomic outbox boundary", "current-step arithmetic and broad completion predicates can mishandle duplicate, late, or out-of-order events", "compensations themselves can fail or be nonreversible"],
      "exclusions": ["does not promise ACID rollback across services", "does not retry non-idempotent effects blindly", "does not compensate an unconfirmed step", "does not publish state and messages without a consistency mechanism", "does not hide manual-recovery states", "does not use a saga when one transactional owner suffices"],
      "usefulInvariants": ["each saga and step has stable identity", "state transitions are concurrency-safe", "messages are deduplicated", "forward and compensation effects are idempotent", "timeouts and retries are explicit", "terminal states preserve unresolved obligations"],
      "materialRisks": ["duplicate charges", "inventory corruption", "lost messages", "partial compensation", "state races", "infinite retries", "false terminal completion"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "durable-compensating-business-workflows",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a43aeca28b9340fa",
      "neutralCapabilitySummary": "Builds and operates DNAnexus genomics data, applet, workflow, and automation workloads through version-bound CLI and Python interfaces with immutable object resolution, regulated-data controls, reproducible dependencies, cost limits, bounded monitoring, and explicit mutation authority.",
      "neutralIntentExamples": ["launch a pinned genomics applet against exact project and input IDs with a cost ceiling", "transfer regulated data only after verifying region, access level, archival state, and destination controls"],
      "legacyAliases": ["DNAnexus integration"],
      "inputs": ["authorized user and project IDs, region, folders and object IDs, data classifications, executable revision, exact inputs, destination, workflow language, dependencies, instance policy, cost limit, timeout, and desired outputs", "DX toolkit and compiler revisions, organization policies, access levels, download restrictions, PHI or TRE controls, egress, credentials, reuse, retries, publication, deletion, retention, and provenance"],
      "operations": ["perform a read-only preflight that resolves names to immutable identities and verifies access, state, region, versions, and policy", "build or validate reproducible applets and workflows, or transfer data through the least risky supported path", "launch only authorized billable work with exact controls, monitor remote state, classify failures from fresh descriptions, and preserve cost and artifact receipts"],
      "outputs": ["resolved platform and object inventory", "validated applet, workflow, transfer, or automation artifacts", "execution, data movement, cost, policy, provenance, and terminal-state receipts"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["names and glob searches can resolve ambiguous objects", "uploads may compress or rename inputs unless controlled", "timeouts in local waiting do not prove remote failure", "runtime dependencies, instance types, platform APIs, and organization policies drift", "operations can expose regulated data or incur material cost"],
      "exclusions": ["does not print security context or tokens", "does not infer destructive targets from names", "does not bypass regional or download controls", "does not launch billable work without exact authorization", "does not use unpinned runtime dependencies for reproducibility", "does not classify a run terminal from a local timeout alone"],
      "usefulInvariants": ["project and object identities are immutable", "read-only preflight precedes mutation", "credentials stay with official endpoints", "regulated-data controls remain intact", "dependencies and executables are pinned", "cost and waits are bounded", "remote terminal state is re-described"],
      "materialRisks": ["regulated-data disclosure", "credential exposure", "irreversible deletion", "permission drift", "compute and egress cost", "workflow nonreproducibility", "cross-region policy violation"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "governed-reproducible-cloud-genomics-operations",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a46d981dad7fc758",
      "neutralCapabilitySummary": "Uses MiniMax's mmx CLI for text, search, vision, image, video, speech, and music generation with JSON and noninteractive modes, dry runs, asynchronous task handling, region selection, file outputs, and provider-specific exit codes.",
      "neutralIntentExamples": ["preview a bounded MiniMax media request before incurring generation cost", "submit an authorized asynchronous video task and validate the returned artifact before downstream use"],
      "legacyAliases": ["MiniMax mmx CLI"],
      "inputs": ["requested modality, source text or media, model and region, prompt, output format and directory, number of variants, async policy, account and budget", "credential storage, prompt and media privacy, provider terms, content and copyright policy, latency and quota, provenance, accessibility, artifact validation, retention, and publication authority"],
      "operations": ["verify installed CLI, active auth source without exposing credentials, current command schema, model, region, budget, and data boundary", "dry-run the exact request where supported and execute only under applicable authority", "poll bounded asynchronous tasks, validate content type and paths or URLs, preserve provenance and costs, and route artifacts through safety and quality review"],
      "outputs": ["provider request and task receipt", "generated or analyzed text and media artifacts", "model, region, cost, provenance, validation, and failure metadata"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["examples persist API keys in user configuration or expose them in command arguments", "quiet output assumptions can break shell parsing across formats", "generated URLs and files remain untrusted", "async tasks can consume quota after the local process exits", "region routing changes data residency"],
      "exclusions": ["does not embed keys in command history", "does not install or authenticate silently", "does not send private source media without review", "does not use --yes as generic authority", "does not trust returned URLs or files", "does not publish generated media automatically"],
      "usefulInvariants": ["model and region are explicit", "secrets remain outside arguments and output", "requests are machine-readable and bounded", "async tasks retain identity and terminal state", "artifacts are validated", "provider provenance follows every output"],
      "materialRisks": ["credential leakage", "private-data disclosure", "copyright conflict", "unsafe generated content", "quota cost", "data-residency violation", "malicious artifact or URL"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "portable",
      "disposition": "pattern-reference",
      "proposedCluster": "governed-multimodal-provider-cli-execution",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a47d4196c1ccbe9d",
      "neutralCapabilitySummary": "Diagnoses a failing test suite by establishing a fresh baseline, grouping failures by likely shared cause and dependency order, applying the smallest production or test correction, rerunning focused evidence after each causal fix, and finishing with the complete authoritative suite.",
      "neutralIntentExamples": ["trace many import failures to one package-layout regression", "repair a changed behavior contract without weakening a valid assertion"],
      "legacyAliases": ["systematic test fixing"],
      "inputs": ["user-authorized fix scope, canonical test commands, fresh failure output, code and test revisions, recent diff, environment and dependency state, expected behavior, and acceptance criteria", "flakiness, test isolation, generated files, environment drift, test validity, production versus test defect, coverage, performance, and unrelated user changes"],
      "operations": ["run the narrowest authoritative baseline and preserve complete failure evidence", "cluster failures by causal evidence rather than error label alone and prioritize foundational causes", "form one hypothesis, add or preserve behavior evidence, make the smallest fix, rerun focused tests, then execute the full relevant suite"],
      "outputs": ["failure inventory and causal clusters", "minimal code or test correction", "focused and full-suite verification with residual failures and confidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["assuming make test ignores repository-specific commands", "grouping only by exception class can combine unrelated failures", "requiring one whole group to pass before investigation can serialize independent work", "making tests green can mask a production regression if assertions are weakened"],
      "exclusions": ["does not change tests merely to match broken behavior", "does not assume every failure shares one cause", "does not edit unrelated user changes", "does not suppress flaky tests without diagnosis", "does not claim completion from focused tests alone", "does not require a fixed toolchain command"],
      "usefulInvariants": ["fresh failures are authoritative", "expected behavior precedes edits", "hypotheses are falsifiable", "changes are causally minimal", "focused tests accelerate feedback", "the complete relevant suite proves integration"],
      "materialRisks": ["test weakening", "misdiagnosed shared cause", "hidden environment drift", "flaky false pass", "unrelated regression", "incomplete suite evidence"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "causal-test-failure-repair",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a4a1519bfe44133d",
      "neutralCapabilitySummary": "Detects consequential underspecification after cheap local discovery, asks only the smallest questions that eliminate materially different branches, offers explicit defaults when appropriate, and withholds direction-dependent mutation until the user answers or authorizes stated assumptions.",
      "neutralIntentExamples": ["inspect repository defaults before asking which compatibility target matters", "pause a destructive migration until target, backup, and terminal criteria are exact"],
      "legacyAliases": ["ask questions if underspecified"],
      "inputs": ["user request, available local evidence, plausible interpretations, objective, scope, done criteria, constraints, environment, authority, reversibility, and risk", "discovery cost, consequence of a wrong assumption, defaults already encoded by the project, urgency, communication burden, and safe reversible next actions"],
      "operations": ["perform bounded read-only discovery and identify only unresolved facts that materially change the result", "rank missing decisions by branch-elimination value and ask the minimum clear questions", "record answered requirements or explicitly authorized assumptions, confirm the operative contract, and then release dependent work"],
      "outputs": ["minimal clarification set or evidence-backed assumption ledger", "confirmed task contract", "clear continuation or bounded stop condition"],
      "effects": ["read"],
      "failureBehavior": ["rigidly pausing can block useful reversible work", "multiple-choice options can falsely narrow open design space", "asking for confirmation after the user explicitly requested progress can create loops", "defaults can smuggle in consequential choices"],
      "exclusions": ["does not ask what local evidence can answer", "does not block low-risk reversible discovery", "does not force multiple choice", "does not repeatedly reconfirm settled intent", "does not assume destructive scope", "does not turn clarification into ceremony"],
      "usefulInvariants": ["questions correspond to material branches", "local evidence is checked first", "risk scales the pause threshold", "defaults are visible", "settled answers persist", "dependent mutations wait for necessary authority"],
      "materialRisks": ["user friction", "analysis paralysis", "false option framing", "unauthorized assumptions", "scope drift", "repeated questioning"],
      "providerCoupling": "none",
      "projectCoupling": "portable",
      "disposition": "independent-implementation",
      "proposedCluster": "risk-scaled-requirement-clarification",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a5ce977556d05018",
      "neutralCapabilitySummary": "Provides React, Next.js, TypeScript, and Tailwind scaffolding, component and hook generation, bundle heuristics, server and client component guidance, image and data-fetching patterns, accessibility checks, and interaction tests through bundled scripts and examples.",
      "neutralIntentExamples": ["add a repository-consistent accessible component with focused interaction tests", "measure a Next.js bundle before replacing a dependency on size and behavior evidence"],
      "legacyAliases": ["senior React and Next.js frontend workflow"],
      "inputs": ["existing frontend repository, framework and runtime versions, package manager, design system, component contract, data and state boundaries, browser support, accessibility criteria, performance budget, and test stack", "script provenance, current framework APIs, server and client trust boundaries, dependency policy, bundle measurement method, visual baselines, content security, and user authorization for scaffolding or installs"],
      "operations": ["inspect the existing stack and conventions before choosing generation, refactor, accessibility, or performance paths", "preview and adapt the smallest repository-consistent component or configuration change", "verify types, lint, behavior, accessibility, rendering, responsiveness, bundle deltas, and framework-specific runtime boundaries"],
      "outputs": ["frontend source and tests", "bundle, accessibility, visual, and interaction evidence", "dependency and framework-compatibility record"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["generic scaffolders can overwrite or diverge from an existing architecture", "hardcoded package-size estimates and replacement advice are stale and ignore actual tree shaking", "examples contain incomplete types, fetching, error, and security behavior", "install commands execute supply-chain code"],
      "exclusions": ["does not scaffold over an existing project blindly", "does not install dependencies without review", "does not replace packages from static size tables alone", "does not default every component to client execution", "does not call an interface accessible from a small checklist", "does not bypass the design system"],
      "usefulInvariants": ["repository conventions lead", "server and client boundaries are explicit", "components have typed behavioral contracts", "accessibility is tested", "bundle changes use measured builds", "visual and interaction behavior are verified"],
      "materialRisks": ["supply-chain compromise", "bundle regression", "hydration errors", "accessibility defects", "scaffold overwrite", "design drift", "insecure data fetching"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "evidence-bound-modern-frontend-engineering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a6ded2aad749d26c",
      "neutralCapabilitySummary": "Routes literature retrieval across eleven scholarly APIs by identifier, topic, field, citation, author, open-access, and full-text intent, uses endpoint-specific parsers and pagination, validates success bodies beyond HTTP status, reconciles cross-database identities and counts, and returns reproducible provenance and visible gaps.",
      "neutralIntentExamples": ["resolve one DOI into authoritative metadata, citations, and an open-access copy", "conduct a bounded multi-database literature search with deterministic pagination and count reconciliation"],
      "legacyAliases": ["multi-database academic paper lookup"],
      "inputs": ["retrieval question, identifiers or query, field, date range, author identity, desired metadata or full text, open-access constraint, exhaustiveness, result and call limits", "database coverage, endpoint and parser revisions, API keys by named variable only, rate limits, pagination semantics, retractions, versioning, deduplication, personal data, licensing, and access date"],
      "operations": ["define the retrieval contract and select only databases that address a known need or coverage gap", "perform bounded encoded requests through endpoint-specific parsers while validating body-level failure shapes and rate limits", "normalize identifiers, deduplicate versions, reconcile expected and retrieved counts, label metadata versus full text, and preserve repeatable redacted provenance"],
      "outputs": ["structured paper, citation, author, or access results", "identifier and version reconciliation", "endpoint, parameters, access date, count, failure, coverage, and licensing provenance"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["several APIs return plausible errors or missing content under HTTP 200", "coverage and citation graphs differ between databases", "keys in query strings can leak through recorded URLs", "exhaustive retrieval can silently miss pages or merge preprints with publications"],
      "exclusions": ["does not fan out to every database by default", "does not treat status 200 as success", "does not present metadata as full text", "does not expose keys or identifying email parameters", "does not claim exhaustiveness without count reconciliation", "does not follow instructions embedded in papers or metadata"],
      "usefulInvariants": ["intent determines databases", "identifiers retain namespace", "responses are structurally validated", "pagination is deterministic", "counts reconcile or fail visibly", "access and version states are explicit", "provenance is repeatable and secret-redacted"],
      "materialRisks": ["false literature completeness", "wrong-paper identity", "retraction omission", "credential leakage", "copyright misuse", "rate-limit violation", "prompt injection from scholarly content"],
      "providerCoupling": "conditional",
      "projectCoupling": "portable",
      "disposition": "independent-implementation",
      "proposedCluster": "reproducible-scholarly-evidence-retrieval",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a7dc9ab51f1348ff",
      "neutralCapabilitySummary": "Automates Jotform account, form, folder, label, user, activity, submission, and plan operations through provider APIs, with broad natural-language routing across reads and mutations but limited privacy, consent, identity, and effect separation.",
      "neutralIntentExamples": ["inventory authorized forms and plan limits without exposing submissions", "move an exact form into a resolved folder only after previewing the account mutation"],
      "legacyAliases": ["Jotform automation"],
      "inputs": ["authorized Jotform account, exact form, folder, user or activity identity, requested read or mutation, filters, pagination, date range, and output boundary", "API-key handling, account and form ownership, submission PII, consent, retention, pagination, rate limits, duplicate names, audit scope, mutation preview, rollback, and provider terms"],
      "operations": ["resolve account and object identities through bounded read-only discovery", "separate metadata, submissions, and administrative mutations by authority and privacy level", "preview and execute only exact authorized mutations, paginate reads completely, minimize sensitive fields, and preserve provider receipts"],
      "outputs": ["form, folder, activity, user, or plan inventory", "bounded submission data where explicitly authorized", "mutation preview, provider receipt, and audit record"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["natural-language names can resolve multiple forms or folders", "submission and activity reads can expose personal data", "folder, label, user, or form mutations may not be easily reversible", "rate and pagination behavior can yield incomplete inventories"],
      "exclusions": ["does not retrieve submission contents for generic form-management requests", "does not mutate by nonunique name", "does not expose API keys", "does not change users or permissions without exact authority", "does not claim complete results without pagination", "does not create or publish forms automatically"],
      "usefulInvariants": ["account and object IDs are exact", "metadata and respondent data are separated", "sensitive fields are minimized", "mutation previews precede effects", "pagination and rate limits are explicit", "provider receipts preserve before and after state"],
      "materialRisks": ["personal-data disclosure", "unauthorized account mutation", "credential exposure", "incomplete pagination", "ambiguous targets", "retention violation", "provider lock-in"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "governed-form-platform-administration",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a82553e93ca8ca53",
      "neutralCapabilitySummary": "Surveys autonomous-agent architecture through goal decomposition, ReAct loops, planning, reflection, tool use, memory, checkpointing, structured output, cost and step limits, human approval, least privilege, error recovery, observability, and context compaction.",
      "neutralIntentExamples": ["decide whether a deterministic workflow is sufficient before introducing an agent loop", "build a bounded agent that drafts external actions but requires human authority to execute them"],
      "legacyAliases": ["autonomous agent architecture"],
      "inputs": ["objective, terminal predicates, task uncertainty, environment, tools, permissions, model and provider, memory, context, budget, timeout, checkpoint, validation, and human escalation policy", "prompt injection, untrusted observations, output schemas, side-effect idempotency, privacy, cost, recursion, recovery, observability, evaluation corpus, and simpler workflow baseline"],
      "operations": ["justify agentic control against a simpler deterministic baseline and define a bounded state machine", "scope tools and memory, validate observations and plans, enforce steps, time, cost, and authority gates, and checkpoint durable state", "evaluate task success, safety, recovery, quality, latency, and cost on representative cases before staged use"],
      "outputs": ["agent state, tool, authority, and memory architecture", "bounded implementation with checkpoints and observability", "evaluation, failure, recovery, and effect receipts"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["generic ReAct prompts can loop, hallucinate tools, or treat observations as instructions", "broad permissions and self-generated plans diffuse authority", "raw thought logging can expose sensitive reasoning and data", "summarization can erase constraints", "reflection can repeat errors without independent evidence"],
      "exclusions": ["does not use an agent when a workflow suffices", "does not expose unrestricted tools", "does not log private chain-of-thought", "does not let model plans authorize effects", "does not run without hard bounds", "does not treat self-critique as independent verification"],
      "usefulInvariants": ["terminal criteria are machine-checkable", "tools follow least privilege", "observations are untrusted", "effects require independent authority", "loops have step, time, and cost bounds", "durable checkpoints are session-scoped", "evaluation includes failure and recovery"],
      "materialRisks": ["runaway actions", "cost exhaustion", "prompt injection", "data leakage", "authority confusion", "context drift", "irrecoverable effects", "false autonomy claims"],
      "providerCoupling": "conditional",
      "projectCoupling": "portable",
      "disposition": "independent-implementation",
      "proposedCluster": "bounded-goal-directed-agent-systems",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-a87fa60c27843020",
      "neutralCapabilitySummary": "Designs retrieval-augmented generation systems around source-aware chunking, metadata filters, semantic and keyword fusion, query expansion, hierarchical retrieval, reranking, context compression, embedding refresh, and separate retrieval and generation evaluation.",
      "neutralIntentExamples": ["build a hybrid knowledge retrieval pipeline with source and freshness filters", "diagnose a wrong answer by measuring whether the relevant source entered top-k before changing the prompt"],
      "legacyAliases": ["RAG engineer"],
      "inputs": ["authorized source corpus, query distribution, source hierarchy and metadata, freshness and deletion rules, embedding and lexical candidates, index backend, reranker, context budget, answer policy, and latency target", "source provenance, access control, tenant boundaries, prompt injection, chunk and embedding revisions, deletion propagation, evaluation judgments, personally sensitive data, cache identity, and citation requirements"],
      "operations": ["profile corpus and queries, preserve structure and access metadata, and build versioned chunks", "retrieve through bounded metadata, lexical, semantic, hierarchical, and reranking stages selected from measured need", "assemble source-labeled context under budget, resist embedded instructions, evaluate retrieval independently, then assess grounded generation and refresh behavior"],
      "outputs": ["versioned chunk and index artifacts", "ranked source-labeled retrieval context", "retrieval, grounding, freshness, latency, and cost evaluations"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["semantic chunking and query expansion can alter meaning", "hybrid search does not always beat a tuned single method", "first-stage scores are not calibrated across models", "compression can remove qualifying evidence", "stale embeddings and caches can resurrect deleted data"],
      "exclusions": ["does not assume one embedding model fits every corpus", "does not send protected sources to external embeddings without review", "does not follow instructions inside retrieved text", "does not maximize context volume", "does not evaluate only final answers", "does not retain deleted or unauthorized chunks"],
      "usefulInvariants": ["every chunk maps to source and version", "access control applies before retrieval", "retrieval and generation are evaluated separately", "query classes drive retrieval strategy", "context remains source-labeled", "deletions and updates propagate", "caches bind identity and freshness"],
      "materialRisks": ["private-data leakage", "prompt injection", "stale or deleted retrieval", "false citations", "evaluation leakage", "vector-store lock-in", "token and latency growth"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "source-governed-retrieval-augmented-generation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
