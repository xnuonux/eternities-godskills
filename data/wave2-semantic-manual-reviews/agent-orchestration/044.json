{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, bind the review to exact packet evidence, and retain only source-supported mechanisms behind independent authority, safety, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-cc2ee79857bc5ee3",
      "neutralCapabilitySummary": "Designs event-sourced systems around explicit aggregate boundaries, immutable versioned events, command handling, query projections, cross-aggregate process managers, snapshots, correlation, idempotency, and projection rebuildability when audit history or temporal queries justify the added complexity.",
      "neutralIntentExamples": ["design a temporal audit trail with rebuildable read projections", "model a compensating multi-aggregate workflow without mutating committed history"],
      "legacyAliases": ["event sourcing architect"],
      "inputs": ["domain commands and invariants, aggregate boundaries, temporal and audit requirements, consistency expectations, query shapes, event store constraints, retention policy, expected stream length, failure recovery, and operational ownership", "event schema evolution, replay determinism, personal data erasure duties, duplicate delivery, poison events, projection lag, snapshot invalidation, saga compensation, storage growth, and migration rollback"],
      "operations": ["prove event sourcing is warranted over simpler state persistence", "define immutable event, command, aggregate, projection, versioning, idempotency, and correlation contracts", "test replay, duplicate delivery, projection rebuilding, schema evolution, compensation, lag, and disaster recovery before production adoption"],
      "outputs": ["event-sourced architecture and stream model", "command, event, projection, saga, snapshot, and evolution contracts", "replay, consistency, recovery, and operational evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["event sourcing can overcomplicate simple domains", "event evolution can make historical replay fail", "projection lag exposes stale reads", "immutable history conflicts with some deletion obligations"],
      "exclusions": ["does not use event sourcing when CRUD satisfies the domain", "does not mutate committed production events", "does not assume exactly-once delivery", "does not rebuild production projections without staged proof", "does not hide eventual consistency from consumers"],
      "usefulInvariants": ["events are immutable facts", "commands enforce aggregate invariants", "handlers are idempotent", "versions remain replayable", "projections can rebuild", "correlation survives workflows", "consistency is explicit"],
      "materialRisks": ["irreplayable history", "duplicate effects", "projection corruption", "privacy conflict", "unbounded storage", "compensation failure", "operational complexity"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "replay-safe-event-sourced-architecture",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-cc61b87ee1c79c28",
      "neutralCapabilitySummary": "Maps an Odoo purchasing lifecycle from quotation through approved purchase order, receipt, vendor billing, quantity-based vendor pricing, lead times, and three-way matching while preserving stock and accounting trails and qualifying version-specific menu guidance.",
      "neutralIntentExamples": ["configure receipt-based vendor billing with a purchase approval threshold", "diagnose a purchase order, receipt, and vendor bill quantity mismatch"],
      "legacyAliases": ["Odoo purchase workflow"],
      "inputs": ["exact Odoo edition and version, installed modules, company and currency, purchasing roles, approval threshold, products and vendors, price breaks, lead times, receipt policy, bill-control policy, accounting localization, and test database", "partial receipts, returns, taxes, exchange rates, permissions, existing custom modules, multi-company boundaries, landed costs, audit retention, posted documents, and production mutation authority"],
      "operations": ["verify current Odoo fields, menus, permissions, and module behavior against the target instance", "model quotation, approval, ordering, receiving, matching, billing, exception, and archive paths", "test the flow with non-production records and reconcile stock, payable, currency, and audit outcomes before rollout"],
      "outputs": ["version-bound Odoo purchase configuration", "approval, pricing, receipt, billing, and mismatch runbook", "test transaction and reconciliation evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["menu paths and field behavior vary by Odoo version and modules", "currency rates can invalidate expected prices", "posted accounting and stock records are not safely deleted", "simple threshold approval does not model complex matrices"],
      "exclusions": ["does not alter production purchasing without authority", "does not bypass receipt-based matching", "does not delete posted or received documents", "does not claim support for subcontracting or EDI", "does not infer accounting localization"],
      "usefulInvariants": ["instance version is exact", "roles and thresholds are explicit", "vendor pricing is product-bound", "received and billed quantities reconcile", "posted trails remain intact", "test records precede rollout"],
      "materialRisks": ["accounting discrepancy", "stock mismatch", "unauthorized purchase", "currency error", "audit-trail loss", "version drift", "production data mutation"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "auditable-procure-to-pay-configuration",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-cca3b1c437e892fb",
      "neutralCapabilitySummary": "Structures Python LLM applications around typed validated outputs, dependency-injected tools, bounded retries, provider abstraction, deterministic test doubles, streaming, and explicit conversation history, with consequential tool effects separately authorized and audited.",
      "neutralIntentExamples": ["build a typed support agent whose tools are testable without a live model", "validate structured model output and retry only recoverable schema failures"],
      "legacyAliases": ["PydanticAI typed agents"],
      "inputs": ["current PydanticAI and Pydantic versions, model providers, output schemas, dependency interfaces, tool contracts, conversation policy, retry and token budgets, streaming needs, test corpus, secrets, effect authority, and deployment target", "framework API drift, schema brittleness, tool prompt injection, provider differences, secret handling, async concurrency, duplicate effects, retained transcripts, model nondeterminism, and production observability"],
      "operations": ["verify current framework APIs and isolate provider configuration", "define typed outputs, injected dependencies, narrowly described tools, validation, retry, history, and effect gates", "exercise deterministic test models plus authorized live integration tests, inspect usage and traces, and verify failure and duplicate-effect handling"],
      "outputs": ["typed Python agent implementation", "tool, schema, dependency, history, and authority contracts", "deterministic and live integration evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["embedded examples may use stale framework APIs", "strict schemas can trigger repeated invalid responses", "test doubles prove structure rather than model quality", "provider abstraction does not erase behavioral differences"],
      "exclusions": ["does not hardcode credentials", "does not let schemas substitute for business validation", "does not execute mutating tools without authority", "does not hit paid models in ordinary unit tests", "does not assume framework retries make effects idempotent"],
      "usefulInvariants": ["outputs validate", "dependencies are explicit", "tools are typed and scoped", "retries are bounded", "tests are deterministic by default", "provider and usage are recorded", "effects remain authorized"],
      "materialRisks": ["unauthorized tool action", "secret exposure", "retry amplification", "schema drift", "provider lock-in", "transcript privacy", "false test confidence"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "typed-testable-agent-runtime",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-cd2adbb0dded8f23",
      "neutralCapabilitySummary": "Queues narrowly approved scripts through an already installed and independently verified local file bridge, with owner-only queue state, fixed scripts, explicit machine paths, permission ceilings, idempotency, budgets, sensitive-output handling, and a separate approval boundary for any free-form local agent.",
      "neutralIntentExamples": ["run an approved read-only repository-status script on an owned machine", "queue one explicitly approved bounded local edit after independent bridge verification"],
      "legacyAliases": ["cowork to code bridge"],
      "inputs": ["verified immutable bridge installation, owned machine, exact absolute target, reviewed fixed script, permission ceiling, owner-only queue and token state, stripped environment, idempotency key, timeout and budget, expected schema, and explicit authority", "ambient credentials, mutable installers, symlinked queues, free-form agent settings and hooks, output secrets, cancellation races, unknown side effects, daemon crashes, retries, and machine-wide account reach"],
      "operations": ["require independent machine-side installation and configuration evidence rather than bootstrapping", "prefer a fixed reviewed script and queue one exact bounded action with stable idempotency and limits", "validate and redact results, inspect uncertain side effects before retry, and keep free-form planning and edits behind separate explicit approvals"],
      "outputs": ["bounded remote task receipt", "validated and redacted result", "configuration, authority, idempotency, failure, and side-effect record"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the daemon runs with the local account's broad reach", "the referenced installer is not transitively immutable", "permission mappings may be widened by ambient settings", "crash or cancellation leaves completed effects uncertain"],
      "exclusions": ["does not install or repair the bridge", "does not execute mutable remote installers", "does not queue arbitrary commands", "does not infer approval from silence", "does not retry uncertain mutations under a new idempotency key", "does not expose raw sensitive output"],
      "usefulInvariants": ["machine ownership is explicit", "installation is independently verified", "target and script are exact", "queue state is owner-only", "authority is least privilege", "idempotency is stable", "uncertain effects are inspected"],
      "materialRisks": ["machine compromise", "credential exposure", "arbitrary local execution", "duplicate mutation", "supply-chain compromise", "scope escape", "sensitive output leakage"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "verified-least-authority-local-execution-bridge",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-ce1db7d9a6421b46",
      "neutralCapabilitySummary": "Frames an AI and machine-learning delivery lifecycle across use-case definition, model and architecture selection, LLM integration, retrieval, agent design, data and training pipelines, observability, evaluation, security, and deployment, while requiring each broad phase to resolve into evidence-backed specialist work.",
      "neutralIntentExamples": ["turn an AI product request into a staged architecture and evaluation program", "identify whether a proposed AI feature needs retrieval, tools, training, or deterministic software"],
      "legacyAliases": ["AI ML workflow bundle"],
      "inputs": ["user outcome and baseline, data provenance and rights, deterministic alternatives, model and provider options, retrieval corpus, tool authority, training need, latency and cost budgets, privacy and abuse model, evaluation set, deployment environment, observability, and rollback", "broad companion-skill assumptions, framework and provider drift, prompt injection, training leakage, retrieval quality, model bias, memory privacy, external effects, cost escalation, and shallow checklist completion"],
      "operations": ["define measurable value and test deterministic alternatives before selecting AI machinery", "decompose the system into the smallest necessary model, retrieval, tool, memory, pipeline, and observability components", "evaluate correctness, safety, cost, latency, drift, and fallback on representative data before controlled deployment"],
      "outputs": ["AI or ML system delivery map", "data, model, retrieval, tool, evaluation, security, and deployment contracts", "quality, safety, cost, latency, and rollback evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source is an orchestration checklist rather than executable expertise", "named companion skills may be absent or weak", "framework-first routing can overbuild", "checkboxes can hide poor model behavior"],
      "exclusions": ["does not assume an AI component is necessary", "does not select providers before requirements", "does not train on unlicensed or private data", "does not grant agents broad effects", "does not deploy from checklist completion", "does not omit fallback and rollback"],
      "usefulInvariants": ["outcome and baseline lead", "data provenance is known", "architecture is minimal", "effects are governed", "evaluation is representative", "cost and latency are measured", "fallback exists"],
      "materialRisks": ["privacy breach", "unsafe agent action", "model failure", "data-rights violation", "cost explosion", "provider lock-in", "deployment regression"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "evidence-gated-ai-system-delivery",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-cf0709b8d62a11c2",
      "neutralCapabilitySummary": "Inspects LaunchDarkly projects, environments, and code-reference repositories and manages webhook trigger workflows only through exact project, flag, environment, integration, instruction, and trigger identifiers with separate authority for creation and irreversible deletion.",
      "neutralIntentExamples": ["enumerate environments for an authorized LaunchDarkly project", "design a reviewed feature-flag trigger without creating it"],
      "legacyAliases": ["LaunchDarkly automation via Composio"],
      "inputs": ["authorized LaunchDarkly account and connection, exact project, environment and flag keys, trigger purpose and integration, on or off instruction, comments, pagination, enterprise entitlements, API permissions, creation or deletion authority, and audit policy", "production flag effects, webhook URL secrecy, API-key scope, wrong environment, irreversible trigger deletion, third-party MCP trust, pagination gaps, code-reference permissions, and provider API drift"],
      "operations": ["discover exact projects and environments with read-only calls", "validate identifiers, permissions, environment, trigger behavior, secret handling, and rollback before proposing mutation", "create or delete only the explicitly approved trigger and verify resulting provider state while redacting webhook secrets"],
      "outputs": ["project, environment, or code-reference inventory", "reviewed trigger plan or exact mutation receipt", "provider state, permissions, audit, and secret-handling evidence"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["the workflow depends on a third-party MCP and provider connection", "a trigger can change a production feature flag", "deletion is irreversible", "returned webhook URLs are bearer-like secrets"],
      "exclusions": ["does not connect an account implicitly", "does not create or delete triggers without exact authority", "does not infer keys from display names", "does not expose trigger URLs", "does not assume enterprise code-reference access", "does not treat listing authority as mutation authority"],
      "usefulInvariants": ["account and environment are exact", "project and flag keys are discovered", "permissions are least privilege", "mutation authority is separate", "webhook URLs remain secret", "provider state is verified"],
      "materialRisks": ["production feature change", "webhook compromise", "wrong-environment mutation", "irreversible deletion", "credential exposure", "third-party integration compromise", "audit gap"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "authority-gated-feature-flag-trigger-management",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-cfedb16f7c70fcfc",
      "neutralCapabilitySummary": "Designs durable sequential, parallel, event-driven, scheduled, and orchestrator-worker workflows from explicit effects and failure semantics, using checkpointed deterministic orchestration, idempotent external actions, bounded retries with backoff, timeouts and heartbeats, small persisted state, dead-letter handling, observability, and recovery tests.",
      "neutralIntentExamples": ["make a payment workflow replay-safe without duplicate charges", "choose between a low-code and code-first durable workflow runtime from reliability requirements"],
      "legacyAliases": ["workflow automation and durable execution"],
      "inputs": ["workflow graph and triggers, state and effect inventory, ordering and parallelism constraints, durability target, runtime options, retry classes, idempotency keys, timeouts, cancellation, payload sizes, schedules, concurrency and cost budgets, observability, recovery ownership, and deployment authority", "nondeterministic replay, duplicate external effects, retry storms, poison events, long activities, clock behavior, large persisted state, dead letters, worker restarts, partial compensation, provider limits, and schema evolution"],
      "operations": ["classify every step by determinism, effect, idempotency, timeout, retry, compensation, and checkpoint boundary", "select the smallest runtime that meets durability and operational requirements and encode bounded workflow patterns", "test replay, duplication, restart, timeout, cancellation, backoff, dead-letter recovery, payload limits, and observability before rollout"],
      "outputs": ["durable workflow design or implementation", "step, retry, idempotency, timeout, checkpoint, and failure contracts", "replay, recovery, load, observability, and operational evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["provider examples may drift or contain syntax errors", "durable runtimes replay orchestration and amplify non-idempotent effects", "large state slows or exceeds provider limits", "missing failure handlers strand work silently"],
      "exclusions": ["does not make external calls without idempotency strategy", "does not place nondeterminism in replayed orchestration", "does not omit timeouts or bounded retries", "does not persist large payloads when references suffice", "does not deploy without dead-letter ownership", "does not select a platform from hype"],
      "usefulInvariants": ["steps checkpoint", "orchestration is deterministic", "effects are idempotent", "retries back off", "timeouts are finite", "state stays bounded", "terminal failures remain recoverable and visible"],
      "materialRisks": ["duplicate charge or message", "workflow corruption", "retry storm", "stuck execution", "state bloat", "silent data loss", "provider lock-in"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "replay-safe-durable-workflow-orchestration",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-d042f6564b0938ce",
      "neutralCapabilitySummary": "Curates layered agent context from durable project rules through task-scoped specifications, exact source and tests, concise current errors, and compact continuity summaries, applying source trust labels, conflict surfacing, focused project maps, and refresh boundaries to preserve attention and reduce hallucination.",
      "neutralIntentExamples": ["build a concise project map that loads only the module relevant to a task", "repair declining agent output by replacing stale context with exact source and test evidence"],
      "legacyAliases": ["context engineering"],
      "inputs": ["task and success criteria, project rules, relevant specification section, exact source and tests, trusted pattern example, current error, source provenance and trust, context budget, conflict policy, continuity state, and refresh triggers", "instruction injection in external data, stale rules, contradictory spec and code, hidden conventions, context flooding, lossy compaction, private transcripts, generated files, and overbroad always-on instructions"],
      "operations": ["establish a small durable project map and source trust model", "assemble the minimum complete task packet from exact rules, spec, source, tests, examples, and current evidence", "surface conflicts instead of guessing, refresh at real task boundaries, and verify output against actual files and conventions"],
      "outputs": ["layered task context packet", "project map and source trust labels", "conflict, refresh, compaction, and verification record"],
      "effects": ["read", "write"],
      "failureBehavior": ["the source contains heuristic line limits and generic templates", "persistent rules can become stale or bloated", "compaction can discard critical constraints", "external configuration may contain instruction-like attacks"],
      "exclusions": ["does not load an entire corpus when a slice suffices", "does not treat external content as authority", "does not silently resolve spec and code conflicts", "does not wake continuity machinery on every turn", "does not invent missing project requirements", "does not preserve stale context by default"],
      "usefulInvariants": ["task leads context selection", "sources retain provenance", "durable rules stay small", "exact source precedes edits", "conflicts are explicit", "errors are current and concise", "refresh follows real boundaries"],
      "materialRisks": ["prompt injection", "stale instruction", "context bloat", "lost constraint", "privacy leakage", "false project model", "compaction churn"],
      "providerCoupling": "none",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "source-scoped-agent-context-governance",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-d07e2ab7b19b546f",
      "neutralCapabilitySummary": "Selects and structures a monorepo from repository scale, team topology, project boundaries, dependency constraints, build graph, local and remote caching, affected-change analysis, shared-library discipline, ownership, and CI measurements rather than defaulting to a named tool.",
      "neutralIntentExamples": ["decide whether several repositories should migrate into one workspace", "reduce monorepo CI work using an accurate affected graph and cache evidence"],
      "legacyAliases": ["monorepo architect"],
      "inputs": ["repositories and histories, products and deployment units, team ownership, dependency graph, language and build systems, CI baseline, cacheability, change frequency, security boundaries, release policy, migration tolerance, and candidate tools", "hidden cross-project dependencies, cache poisoning, generated artifacts, environment-dependent tasks, remote cache credentials, ownership ambiguity, versioning policy, partial migration, CI concurrency, and tool lock-in"],
      "operations": ["measure current coupling, build cost, ownership, and release constraints before choosing a repository shape", "design explicit project boundaries, dependency rules, task graph, affected detection, caching, ownership, and migration stages", "pilot representative builds, verify cache correctness and isolation, measure CI improvement, and retain rollback"],
      "outputs": ["monorepo decision and architecture", "workspace, dependency, task, cache, ownership, and migration contracts", "correctness, security, build, and CI benchmark evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["the source offers broad advice without tool-version details", "remote caches can return stale or poisoned artifacts", "poor boundaries create a coupled monolith", "affected detection can miss implicit inputs"],
      "exclusions": ["does not migrate solely for fashion", "does not select tooling before mapping constraints", "does not cache undeclared nondeterministic tasks", "does not flatten ownership boundaries", "does not claim CI gains without representative measurement", "does not remove rollback during migration"],
      "usefulInvariants": ["project boundaries are explicit", "dependency direction is enforced", "task inputs are declared", "cache keys are reproducible", "ownership is visible", "affected analysis is tested", "migration is staged"],
      "materialRisks": ["cache poisoning", "dependency sprawl", "CI regression", "release coupling", "credential leakage", "migration disruption", "tool lock-in"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "evidence-driven-monorepo-architecture",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-d0c925b3674aadeb",
      "neutralCapabilitySummary": "Integrates a Vue 3 streaming Markdown renderer by selecting chat, document, or minimal modes, explicit code rendering and optional peers, stable streaming completion behavior, outer-list virtualization ownership, scoped components, and safe HTML and diagram policies with incremental and long-message verification.",
      "neutralIntentExamples": ["render incremental AI Markdown in Vue without layout instability", "keep a transcript virtualizer authoritative while measuring logical Markdown height"],
      "legacyAliases": ["markstream Vue 3"],
      "inputs": ["Vue and renderer versions, package manager, existing resets and virtualizer, content trust, rendering mode, streaming lifecycle, code renderer and peers, HTML and diagram policy, long-message bounds, accessibility, performance budget, and dependency-change authority", "untrusted HTML, Mermaid directives, optional peer supply chain, hydration expectations, worker parsing, code highlighting cost, virtualizer measurement, stream completion races, and package API drift"],
      "operations": ["verify plain Vue rather than SSR-specific integration and inspect current project conventions", "configure the smallest rendering mode, streaming state, safe-content policy, peer set, and virtualization boundary", "run build and type checks plus incremental stream, completion, untrusted content, accessibility, and long-message cases"],
      "outputs": ["scoped Vue streaming Markdown integration", "rendering, trust, streaming, peer, and virtualization contracts", "build, security, accessibility, and performance evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["package APIs and peer names can drift", "trusted HTML or loose diagrams can execute unsafe content", "nested virtualizers produce unstable measurements", "optional renderers add runtime and bundle cost"],
      "exclusions": ["does not install dependencies without authority", "does not enable trusted HTML for model output", "does not let nested renderers own the same virtualization boundary", "does not assume Vue guidance applies to Nuxt SSR", "does not add optional peers without measured need"],
      "usefulInvariants": ["framework mode is exact", "content trust is explicit", "stream completion is stable", "HTML and diagrams stay constrained", "one virtualizer owns measurement", "peer cost is justified", "long messages are tested"],
      "materialRisks": ["cross-site scripting", "diagram injection", "layout thrash", "memory growth", "dependency compromise", "hydration mismatch", "accessibility regression"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "safe-streaming-markdown-rendering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-d153e4fc8e68b04c",
      "neutralCapabilitySummary": "Builds Python DBOS workflows with explicit startup configuration, deterministic workflow functions, external and complex operations isolated in durable steps, queues for concurrency, bounded communication, and tests for replay, restart, and framework constraints.",
      "neutralIntentExamples": ["move an external API call into a replay-safe DBOS step", "use a DBOS queue to bound concurrent durable jobs"],
      "legacyAliases": ["DBOS Python best practices"],
      "inputs": ["current DBOS Python version and official docs, application lifecycle, workflow and step graph, system database, external services, queue and concurrency needs, messages or events, deterministic inputs, retry and idempotency policy, test database, deployment target, and secrets", "framework API drift, database availability, replay nondeterminism, global mutation, nested workflow restrictions, duplicate effects, queue starvation, schema migration, connection limits, and external client authority"],
      "operations": ["verify current DBOS APIs and define application launch and database boundaries", "separate deterministic workflow logic from durable external steps and bounded queues", "test replay, restart, duplicate delivery, concurrency, communication, failure recovery, and configuration before deployment"],
      "outputs": ["DBOS Python workflow implementation", "lifecycle, workflow, step, queue, communication, and idempotency contracts", "replay, restart, concurrency, and deployment evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["the source omits referenced detailed rule files", "embedded configuration can drift with DBOS releases", "external steps still need idempotency", "system-database failure blocks durability"],
      "exclusions": ["does not place external calls in replayed workflow logic", "does not mutate globals from workflows or steps", "does not start workflows from prohibited step contexts", "does not use threads as a workflow launcher", "does not deploy unverified configuration", "does not assume durability means exactly-once effects"],
      "usefulInvariants": ["startup configuration is explicit", "workflow logic is deterministic", "external operations are steps", "effects are idempotent", "queues bound concurrency", "system state persists", "recovery is tested"],
      "materialRisks": ["duplicate external effect", "database outage", "workflow nondeterminism", "queue starvation", "configuration drift", "secret exposure", "recovery failure"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "deterministic-python-durable-workflows",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
