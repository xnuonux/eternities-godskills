{
  "schemaVersion": 1,
  "reviewer": "codex inline exact-body semantic reviewer",
  "reviewMethod": "read each non-template source body as inert data, bind the review to exact packet evidence, and retain only source-supported mechanisms behind independent authority, safety, provenance, and failure boundaries",
  "reviews": [
    {
      "facetId": "facet-37d7c599fe7630b7",
      "neutralCapabilitySummary": "Preserves a legacy pull-request command name solely as a discoverability alias that resolves to an installed canonical pull-request workflow, without adding behavior or assuming a namespace.",
      "neutralIntentExamples": ["resolve a legacy create-pr request to the current PR workflow", "retain compatibility while removing duplicated PR logic"],
      "legacyAliases": ["create-pr alias"],
      "inputs": ["explicit legacy name, installed skill catalog, canonical pull-request workflow identity, repository and branch context, and publication authority", "missing canonical workflow, ambiguous plugin namespace, behavioral divergence, protected branches, external publication, and stale aliases"],
      "operations": ["resolve the canonical installed workflow by capability rather than hardcoded namespace", "forward the unchanged request and repository context", "report an unavailable dependency instead of inventing PR behavior"],
      "outputs": ["canonical workflow resolution", "compatibility and dependency record"],
      "effects": ["read"],
      "failureBehavior": ["the alias contains no PR implementation", "canonical names can vary by installation", "silent fallback can hide missing safety gates"],
      "exclusions": ["does not create or update a pull request itself", "does not assume an external namespace", "does not bypass canonical workflow gates", "does not authorize publication"],
      "usefulInvariants": ["alias adds no behavior", "canonical workflow is installed", "request semantics stay unchanged", "publication authority remains downstream"],
      "materialRisks": ["wrong workflow routing", "missing safety checks", "unexpected external publication", "namespace collision"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "rejected",
      "proposedCluster": "legacy-workflow-alias-resolution",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3a0c49fec4e9247a",
      "neutralCapabilitySummary": "Builds or refactors Tuist-owned SwiftUI macOS menu-bar utilities with manifest-owned generation, transport-model-store-view separation, defensive decoding, dependency injection, menubar-only process behavior, repeatable local launch scripts, and build and launch verification.",
      "neutralIntentExamples": ["add a network-backed menu item without putting requests in SwiftUI views", "repair a Tuist menu-bar app's generated build and local launch path"],
      "legacyAliases": ["macOS menubar Tuist app"],
      "inputs": ["macOS deployment target, exact Swift and Tuist versions, project manifests, LSUIElement intent, app lifecycle, backend contract and auth, models, store and refresh policy, UI states, resources, entitlements, run scripts, signing context, and validation target", "API probing against production, credentials, generated Xcode files, process restarts, login items, accessibility permissions, network drift, stale connection state, script portability, signing, and background resource use"],
      "operations": ["verify Tuist ownership, target behavior, manifests, scripts, endpoint authority, and exact build environment", "implement model, client, observable store, and render-only view layers with minimal app wiring", "generate without opening tools, build through the Tuist path, test decoding and state, launch and stop safely, and inspect menubar-only behavior"],
      "outputs": ["Tuist-managed menu-bar application changes", "model, transport, store, view, launch, and lifecycle contracts", "build, test, launch, signing, and behavior evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["Tuist and SwiftUI APIs drift", "probe requests can affect real backends", "optional decoding can conceal contract breakage", "restart scripts can terminate the wrong process"],
      "exclusions": ["does not edit generated Xcode projects as source", "does not network from view bodies", "does not probe external endpoints without authority", "does not broaden a menu-bar app into a dock app implicitly", "does not open Xcode as a launch side effect", "does not call build success runtime proof"],
      "usefulInvariants": ["manifests own generation", "menubar behavior is explicit", "transport stays outside views", "state transitions stay in a store", "decoding failures surface", "scripts target exact processes", "build and launch are verified"],
      "materialRisks": ["credential exposure", "wrong-process termination", "API contract drift", "signing failure", "background resource leak", "generated-project divergence", "unexpected dock presence"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "manifest-owned-layered-macos-menubar-engineering",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3a98890de38a8f31",
      "neutralCapabilitySummary": "Prepares journal, conference, poster, and grant documents by resolving the exact venue, cycle, track, document type, and stage, recording current official rules and check dates, using official templates where required, and mechanically plus manually auditing anonymity, pages, fonts, metadata, accessibility, statements, supplements, and source packages.",
      "neutralIntentExamples": ["prepare an anonymous conference submission from the official current author kit", "audit a grant PDF against the exact solicitation and upload-component rules"],
      "legacyAliases": ["venue templates"],
      "inputs": ["exact venue or funder, year or cycle, track, document type and stage, official instructions and template, authoring format, manuscript or proposal content, page accounting, anonymity, required statements and forms, figure and poster constraints, institutional review, compiler, and submission authority", "rule and deadline drift, similarly named tracks, unofficial templates, identity in PDF metadata, page exclusions, LaTeX injection, grant forms, accessibility, font embedding, submission portal transformations, and confidential content"],
      "operations": ["open current official instructions and record target, source, date, and stage-specific requirements", "start from the official template or clearly labeled drafting scaffold without altering controlled layout", "compile and inspect page, font, metadata, anonymity, statements, accessibility, supplemental, and portal-preview requirements before human submission"],
      "outputs": ["venue-adapted source and PDF", "dated compliance note and official-source ledger", "mechanical and manual validation checklist with unresolved issues"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["annual rules and templates expire", "helper scripts cannot prove margins or hidden metadata", "generic scaffolds can look official", "portal rendering can differ from local PDF"],
      "exclusions": ["does not infer current rules from old filenames", "does not present scaffolds as official", "does not override controlled styles", "does not recreate required agency forms", "does not submit without human authority", "does not call page and font checks full compliance"],
      "usefulInvariants": ["target and stage are exact", "official source is current and dated", "template status is explicit", "controlled styles remain unchanged", "anonymity and metadata are checked", "manual review complements tooling", "human approves submission"],
      "materialRisks": ["desk rejection", "identity leak", "stale-rule noncompliance", "format corruption", "confidential disclosure", "accessibility failure", "wrong portal upload"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "current-source-bound-publication-format-compliance",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3ac13a992ccb23f1",
      "neutralCapabilitySummary": "Tests an explicitly authorized web application for reflected or stored HTML interpretation using minimal inert markers first, distinguishes context and script execution, traces source to sink, and documents impact and context-aware remediation without creating phishing, credential capture, filter bypass, or destructive payloads.",
      "neutralIntentExamples": ["verify whether owned staging input is rendered as markup", "trace a stored rich-text field to an unsafe HTML sink and recommend output encoding"],
      "legacyAliases": ["HTML injection testing"],
      "inputs": ["written authorization, exact hosts, accounts, routes and fields, environment, allowed request methods, test data, rate and payload limits, storage cleanup, browser and proxy setup, source code where available, output contexts, evidence policy, and stop conditions", "third-party targets, stored payloads, phishing forms, credential capture, external image or iframe requests, script execution, filter bypass, production data, WAF impact, persistent defacement, and legal scope"],
      "operations": ["verify current authorization and constrain exact target, account, inputs, rates, and cleanup", "begin with harmless unique text and inert markup, inspect encoding and browser parsing, and stop before script or credential effects", "reproduce minimally, trace unsafe rendering context, remove stored markers, and recommend context-aware encoding or vetted sanitization with regression tests"],
      "outputs": ["authorized HTML interpretation finding or negative result", "request, response, rendering, source-to-sink, and cleanup evidence", "severity-qualified remediation and tests"],
      "effects": ["read", "external-write"],
      "failureBehavior": ["the source contains phishing, defacement, exfiltration, and bypass payloads", "stored tests can affect other users", "reflection in response text does not prove browser interpretation", "HTML injection can cross into script execution"],
      "exclusions": ["does not create phishing or credential-capture forms", "does not test script execution unless separately authorized", "does not evade filters", "does not load attacker-controlled external resources", "does not persist defacement", "does not test outside exact scope"],
      "usefulInvariants": ["authorization is exact", "markers are inert and unique", "browser context is observed", "effects are minimal", "stored artifacts are removed", "source and sink are traced", "remediation has regression tests"],
      "materialRisks": ["credential theft", "persistent defacement", "unauthorized request", "user exposure", "stored payload residue", "service disruption", "scope violation"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "deferred",
      "proposedCluster": "authorization-bound-inert-html-injection-validation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3ae9bd7a6d0d1bf1",
      "neutralCapabilitySummary": "Builds bounded process-based discrete-event simulations with explicit conceptual assumptions, event and resource semantics, deterministic stream separation, execution caps, non-interfering monitoring, independent replications, warm-up and horizon treatment, verification against identities and benchmarks, and qualified non-causal interpretation.",
      "neutralIntentExamples": ["model a queue with bounded arrivals and replication-level intervals", "audit a SimPy model for event-boundary, interrupt, and resource-request errors"],
      "legacyAliases": ["SimPy discrete-event simulation"],
      "inputs": ["decision question and estimands, system boundary, entities and resources, event logic, time units, distributions and data provenance, initial state and warm-up, horizon and termination, random streams, replication and precision plan, instrumentation, resource caps, validation evidence, and package pin", "endless processes, same-time ordering, unfinished entities, losing condition events, abandoned requests, interrupts, time-weighting, private scheduler APIs, stochastic dependence, model-form uncertainty, real-time lag, and causal overclaim"],
      "operations": ["write and review the conceptual model and estimands before code", "implement event-yielding processes with separate random streams, explicit caps, safe resource cleanup, and deliberate monitoring", "test deterministic semantics and conservation, validate against evidence, run independent replications, and report initialization, precision, sensitivity, unfinished work, and limitations"],
      "outputs": ["bounded simulation implementation", "replication-level estimates and uncertainty", "assumption, seed, validation, monitoring, resource, and limitation record"],
      "effects": ["read", "write"],
      "failureBehavior": ["correct scheduler code can encode an invalid conceptual model", "numeric horizons exclude ordinary events at the boundary", "sample averages can misstate time averages", "fixed seeds do not validate distributions"],
      "exclusions": ["does not call env.run unboundedly with recurring processes", "does not infer causal effects", "does not derive confidence intervals from correlated entities in one run", "does not ignore unfinished entities", "does not rely on private APIs without pinning and tests", "does not equate Monte Carlo precision with model validity"],
      "usefulInvariants": ["purpose and estimands are explicit", "execution is bounded", "random streams are recorded", "resources release or cancel", "monitoring does not alter order", "replications are independent", "validation and limitations are reported"],
      "materialRisks": ["invalid operational decision", "unbounded compute", "biased estimator", "event-order bug", "false precision", "model-form error", "causal misinterpretation"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "bounded-validated-discrete-event-simulation",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3b3f820cc64da498",
      "neutralCapabilitySummary": "Governs local-first agent memory as concise source-linked decisions, lessons, warnings, preferences, evidence outcomes, scars, and unresolved seeds, with project-scoped recall, trust verification, ring-based durability, deterministic consolidation, redaction, supersession, and explicit forgetting rather than transcript capture.",
      "neutralIntentExamples": ["recall prior project warnings before a risky migration and verify them against current source", "store a tested failure as a redacted scar and supersede stale memory"],
      "legacyAliases": ["Tree Ring Memory"],
      "inputs": ["exact project and local guidance, installed CLI version, recall query and scope, candidate durable fact, source references, evidence outcome, privacy classification, retention and ring choice, redaction, supersession and forgetting authority, adapter source, and consolidation policy", "secrets, personal data, copyrighted text, raw transcripts, stale memory, cross-project leakage, mutable installers, source-adapter injection, hidden recording, deletion authority, and memory-source conflicts"],
      "operations": ["discover exact local memory contracts and recall narrowly before consequential repeat work", "verify recalled claims against current source and runtime evidence", "write only redacted durable source-linked records at the lowest warranted ring, attach evaluated outcomes, and audit, supersede, or forget stale material"],
      "outputs": ["scoped recall results", "durable memory or evidence records", "provenance, confidence, retention, redaction, consolidation, and forgetting receipts"],
      "effects": ["read", "write"],
      "failureBehavior": ["memory can be stale or wrong", "the source includes a version-pinned installer whose status can drift", "source adapters can import too much", "ring metaphors do not enforce policy"],
      "exclusions": ["does not store transcripts or hidden reasoning", "does not store credentials", "does not treat recall as authority", "does not install or update without explicit review and authority", "does not promote weak evidence", "does not retain sensitive data by default"],
      "usefulInvariants": ["scope is explicit", "memory is concise", "sources are linked", "current evidence wins", "durability is earned", "sensitive content is redacted", "forgetting and supersession are available"],
      "materialRisks": ["privacy breach", "stale-decision propagation", "hidden surveillance", "cross-project leakage", "installer compromise", "irreversible forgetting", "false durable truth"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "source-verified-local-agent-memory-lifecycle",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3bdb4d14698fd584",
      "neutralCapabilitySummary": "Executes an approved Conductor track from versioned product, specification, plan, workflow, style, and metadata files, preserving task state, configurable TDD and verification checkpoints, failure visibility, and final acceptance evidence while separating optional commit, documentation, archive, and deletion actions.",
      "neutralIntentExamples": ["resume an in-progress Conductor task from its metadata and test state", "execute one approved track phase with red-green-refactor verification"],
      "legacyAliases": ["Conductor implement track"],
      "inputs": ["initialized Conductor project, exact track, versioned spec and plan, workflow and code-style rules, current metadata, repository state, task acceptance criteria, TDD setting, validation commands, commit strategy and authority, phase gates, user changes, and cleanup policy", "stale or contradictory state files, broad git add, unrelated changes, automatic commits, fake timestamps, test failures, partial task state, phase approval ceremony, archive or delete choices, and absent companion resources"],
      "operations": ["verify initialized files, exact track, settled plan, repository state, workflow rules, and current task", "execute one task at a time with practical TDD where required, updating state only after corresponding evidence", "stop on unresolved failures, run phase and final verification, and perform commits, documentation sync, or cleanup only under explicit policy and authority"],
      "outputs": ["implemented track changes", "synchronized plan and metadata state", "task, phase, test, commit, acceptance, and failure evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["the source stages all files and commits automatically", "state documents can diverge from repository truth", "fixed approval pauses can impede autonomous work", "non-TDD mode can weaken regression evidence"],
      "exclusions": ["does not execute an absent or unsettled plan", "does not stage unrelated files", "does not commit without repository policy or authority", "does not mark tasks complete before evidence", "does not continue past unexplained failures", "does not delete tracks without explicit choice"],
      "usefulInvariants": ["track and task are exact", "plan matches spec", "state follows evidence", "tests lead behavior changes when practical", "failures remain visible", "phase acceptance is verified", "cleanup is separate"],
      "materialRisks": ["unrelated change commit", "state corruption", "false task completion", "test-gap regression", "user-change collision", "commit history noise", "accidental track deletion"],
      "providerCoupling": "none",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "state-synchronized-plan-driven-tdd-execution",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3d914e73f023cab4",
      "neutralCapabilitySummary": "Provides a non-invocable shared reference package for Nature-family writing workflows, loading only an exact requested journal or writing-discipline file and returning control to the caller while keeping corpus-derived style guidance distinct from official policy.",
      "neutralIntentExamples": ["load only the Nature Machine Intelligence format reference for a caller", "supply evidence-calibrated discussion guidance without treating it as journal policy"],
      "legacyAliases": ["Nature shared references"],
      "inputs": ["requesting workflow identity, exact journal and article type, specific reference path, task stage, current official policy source where needed, and context budget", "flagship versus portfolio journal confusion, corpus style mistaken for policy, preload bloat, stale limits, specialist applicability, missing caller, and external publication effects"],
      "operations": ["reject standalone routing and require a valid requesting workflow", "load only the exact named shared reference under its applicability boundary", "label corpus-derived and official material distinctly and return task control and output decisions to the caller"],
      "outputs": ["bounded shared guidance fragment", "source type, applicability, journal, and caller record"],
      "effects": ["read"],
      "failureBehavior": ["shared files can drift from official policy", "similarly named journals have different constraints", "loading the package broadly creates context bloat"],
      "exclusions": ["does not act as a standalone user workflow", "does not preload all references", "does not mix journal limits", "does not present corpus style as official policy", "does not own final QA or publication"],
      "usefulInvariants": ["caller is explicit", "reference path is exact", "journal identity is exact", "source type is labeled", "context stays bounded", "caller owns the workflow"],
      "materialRisks": ["wrong-journal guidance", "stale policy", "context bloat", "publication noncompliance", "lost workflow ownership"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-adjacent",
      "disposition": "pattern-reference",
      "proposedCluster": "caller-bound-progressive-shared-reference-loading",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3de0fd820b9c1b10",
      "neutralCapabilitySummary": "Statically audits local or immutable-ref GitHub Actions workflows for attacker-controlled data reaching AI-agent prompts, tools, outputs, sandboxes, or privileged execution through direct expressions, environment intermediaries, runtime fetches, logs, checkout paths, shell expansion, output evaluation, and permissive configuration.",
      "neutralIntentExamples": ["trace issue-comment content into an AI action prompt and shell tools", "audit a pull_request_target workflow for privileged agent checkout and prompt injection"],
      "legacyAliases": ["agentic actions auditor"],
      "inputs": ["authorized repository or immutable owner, repo and ref, root workflow files, known and discoverable AI actions, local composite and reusable workflow references, trigger events, environment expressions, permissions, secrets context, action inputs, sandbox and tool settings, source trust, depth and file limits, and report schema", "mutable remote refs, private repository credentials, malicious YAML, parser ambiguity, action-version drift, custom unrecognized agents, one-level resolution gaps, runtime-only data, false severity, token scope, and shell execution of fetched content"],
      "operations": ["discover only root workflows and fetch remote files as inert data at an exact ref", "identify AI actions and resolve bounded local or reusable references while tracing attacker inputs through all intermediaries", "evaluate injection paths separately from amplifying configurations and report source-linked evidence, impact, severity, remediation, coverage, and unresolved edges"],
      "outputs": ["static agentic-actions security report", "per-instance prompt, tool, permission, trigger, sandbox, and data-flow inventory", "coverage, unresolved-reference, source, and confidence record"],
      "effects": ["read"],
      "failureBehavior": ["known-action lists and field profiles drift", "one-level resolution misses deeper wrappers", "YAML semantics are difficult without a real parser", "static analysis cannot prove runtime exploitability"],
      "exclusions": ["does not execute fetched workflows or content", "does not modify workflow files", "does not perform runtime prompt injection", "does not call dangerous configuration alone an exploit path", "does not analyze non-GitHub CI implicitly", "does not hide unresolved references"],
      "usefulInvariants": ["remote ref is exact", "fetched YAML is inert", "attacker source starts each trace", "every hop is named", "configuration amplifiers stay distinct", "findings cite workflows and steps", "coverage limits are explicit"],
      "materialRisks": ["missed prompt injection", "false security finding", "private workflow disclosure", "token misuse", "malicious-data execution", "stale action profile", "incomplete cross-file trace"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "taint-traced-agentic-ci-static-security-audit",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3de7c8dcdef3aa30",
      "neutralCapabilitySummary": "Builds Python Azure Cosmos DB NoSQL access around identity-based authentication, deliberate partition-key and throughput design, parameterized scoped queries, point reads, optimistic concurrency and idempotency, bounded cross-partition work, async lifecycle, throttling, telemetry, and separately authorized data or infrastructure mutations.",
      "neutralIntentExamples": ["design an even tenant-aware partition key and point-read path", "implement bounded asynchronous document access with rate-limit handling"],
      "legacyAliases": ["Azure Cosmos Python SDK"],
      "inputs": ["current Azure Cosmos SDK and service contract, account and database, container purpose, data and tenant model, partition key and cardinality, item identity, consistency and region requirements, throughput and cost budget, identity and RBAC, query shapes, indexing, concurrency, retention, backup, telemetry, and mutation authority", "keys and managed identity, hot partitions, RU cost, cross-partition scans, data residency, retries, duplicate writes, ETags, destructive deletes, throughput changes, container creation, pagination, SDK drift, and personal data"],
      "operations": ["verify current SDK, identity, RBAC, data model, query workload, partition distribution, cost, and residency", "implement least-privilege clients, partition-scoped point and parameterized query paths, concurrency and retry controls, and bounded async lifecycle", "load-test representative partitions, inspect RU and throttling, verify consistency and failure behavior, and gate infrastructure or destructive mutations separately"],
      "outputs": ["Cosmos Python data-access implementation", "partition, indexing, identity, consistency, throughput, and mutation contracts", "correctness, RU, throttle, latency, concurrency, and recovery evidence"],
      "effects": ["read", "write", "external-write"],
      "failureBehavior": ["embedded APIs and preview features can drift", "upsert is not universally idempotent under concurrent semantics", "cross-partition operations can be expensive", "create-if-not-exists mutates infrastructure"],
      "exclusions": ["does not embed account keys", "does not create containers or change throughput without authority", "does not scan all partitions by default", "does not delete without exact identity and partition", "does not ignore ETags for conflicting updates", "does not call retry success cost safety"],
      "usefulInvariants": ["identity is least privilege", "partition key matches workload", "point operations include partition", "queries are parameterized and bounded", "mutations are authorized", "RU cost is observed", "recovery is tested"],
      "materialRisks": ["data breach", "hot partition", "unexpected cloud cost", "wrong-document deletion", "lost update", "regional noncompliance", "throttle cascade"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "partition-aware-cost-governed-cloud-document-access",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3e343151cb28430b",
      "neutralCapabilitySummary": "Queries a pinned public CELLxGENE Census release for single-cell or spatial metadata and expression by first estimating result size, filtering primary data, selecting exact organisms, tissues, cells, genes, datasets, and columns, then choosing in-memory or out-of-core processing and preserving source-dataset and analysis provenance.",
      "neutralIntentExamples": ["count primary lung cell types in a pinned Census release", "stream a bounded gene-expression slice too large for memory without duplicating cells"],
      "legacyAliases": ["CZ CELLxGENE Census"],
      "inputs": ["scientific question, pinned Census and client versions, organism, obs and var filters, primary-data policy, dataset and gene presence, requested columns and matrix layer, expected cell and feature counts, memory and network budget, out-of-core target, batch and donor policy, downstream method, and source-dataset citations", "release and schema drift, duplicate cells, ontology granularity, gene symbols, sparse matrix size, network transfer, donor leakage, batch effects, public-data consent and licensing, spatial extras, and scientific overgeneralization"],
      "operations": ["open an exact release and explore metadata and gene presence before expression retrieval", "estimate cell and feature size, filter primary data, narrow columns and datasets, and select in-memory or bounded out-of-core access", "validate counts and coverage, preserve source dataset and donor structure, and report batch, sampling, release, and inference limitations"],
      "outputs": ["bounded Census metadata or expression slice", "release, filter, dataset, organism, feature, count, and source provenance", "resource, duplicate, batch, coverage, and scientific limitation record"],
      "effects": ["read", "write"],
      "failureBehavior": ["cached release counts and package compatibility drift", "missing primary-data filters double-count cells", "broad slices exhaust memory", "public atlas composition does not represent every population"],
      "exclusions": ["does not analyze private local data through the public Census workflow", "does not open latest implicitly for reproducible work", "does not download whole datasets when a slice suffices", "does not ignore donor or batch structure", "does not infer causality", "does not treat gene symbols as globally unique"],
      "usefulInvariants": ["release is pinned", "primary-data policy is explicit", "query size is estimated", "columns and features are minimal", "source datasets persist", "resources are bounded", "scientific limits are reported"],
      "materialRisks": ["memory exhaustion", "duplicate-cell bias", "release irreproducibility", "donor leakage", "batch confounding", "network cost", "population overclaim"],
      "providerCoupling": "provider-bound",
      "projectCoupling": "project-bound",
      "disposition": "independent-implementation",
      "proposedCluster": "versioned-bounded-public-single-cell-census-querying",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    },
    {
      "facetId": "facet-3e3fbbf24d3f36cd",
      "neutralCapabilitySummary": "Creates two to four temporary source-level UI alternatives inside an authorized local application, makes exactly one inert-safe option visible at a time for human comparison, preserves baseline behavior and design language, then keeps the explicitly named winner and verifies removal of all temporary comparison artifacts.",
      "neutralIntentExamples": ["compare three hero variants in the running local app", "keep the selected option and remove hidden alternatives without leaving temporary markup"],
      "legacyAliases": ["Unship UI variants"],
      "inputs": ["authorized local project, exact component or flow, baseline build and design language, meaningful variant dimensions, reviewed pinned comparison tool, dev-only mount, option labels, hidden-option safety model, local preview, human selection, cleanup scope, and final validation", "duplicate IDs, hidden forms and scripts, analytics, focus traps, autoplay, stateful providers, destructive controls, package supply chain, ambiguous ordinal choices, unrelated refactors, baseline failures, and accidental production artifacts"],
      "operations": ["verify baseline, project authorization, tool provenance, and that inactive variants are inert", "implement the smallest labeled comparison with one visible option and validate local readiness", "obtain an exact human winner label, remove losers and all temporary wiring, and run artifact, build, accessibility, and behavior checks"],
      "outputs": ["local UI comparison and selected implementation", "variant labels and human decision record", "cleanup, build, accessibility, and behavior evidence"],
      "effects": ["read", "write"],
      "failureBehavior": ["hidden DOM can still execute scripts or register duplicate IDs", "ordinal selections become ambiguous after edits", "temporary tooling can leak into production", "variants can expand into an unbounded redesign"],
      "exclusions": ["does not perform production A/B testing", "does not install unpinned tools", "does not create unsafe hidden variants", "does not choose the winner for the user", "does not delete on ambiguous selection", "does not claim cleanup without a clean artifact check"],
      "usefulInvariants": ["project is local and authorized", "scope is small", "one option is visible", "inactive variants are inert", "labels are exact", "human selects", "temporary artifacts are completely removed"],
      "materialRisks": ["hidden side effect", "production artifact leakage", "wrong-variant deletion", "dependency compromise", "accessibility regression", "baseline breakage", "unrelated code loss"],
      "providerCoupling": "conditional",
      "projectCoupling": "project-bound",
      "disposition": "pattern-reference",
      "proposedCluster": "human-selected-inert-local-ui-variant-comparison",
      "confidence": "high",
      "copiedSourceProse": false,
      "promotionClaim": false
    }
  ]
}
