# 801-repository catalog: skill intake and partial Jev classification

Source acquisition completed on 2026-09-21; this extraction/classification receipt
was produced on 2026-09-22 UTC (2026-09-21 local time). **Source ingestion is
complete for the 9,789 declared new-clone entrypoints; Jev classification is not.**
This is an additive development intake, not a release of thousands of new skills.

## Imported source material

The supplied HTML catalog and 801-repository Markdown list were reconciled in the
warehouse operation `catalog-801-20260921-92ef3ca4`. Its pinned filename manifest
identified 9,789 SKILL.md paths in 95 of the 488 newly acquired repositories. The
309 reused repositories are outside this new-clone extraction boundary; they were
not rescanned or described as newly acquired.

- 9,788 regular Git blobs extracted, with exact commit, Git object and SHA-256.
- One symbolic-link entry recorded in `excluded-entries.json`, not followed.
- 8,841 distinct bodies; 947 duplicate aliases retained rather than deleted.
- 30 body hashes match recorded prior intake/reconciliation hashes; 8,811 do not.
  This is declared-hash overlap, not semantic novelty or fresh verification of
  every historical source.
- 224 source aliases lack an extractable description; all bodies decoded as UTF-8.
- 8,671 distinct selected-metadata inputs; 8,670 eligible for Jev. One conservative
  credential-pattern exclusion is actually a hyphenated phrase in paper-planning
  metadata. The false positive remains visible in this frozen input snapshot; it
  is not evidence that a credential was found.

Root license hints remain source metadata, not a compatibility determination.
No upstream code, installers, hooks, submodules or skill commands were executed.
No source was installed into the host agent instructions. The portable pack and
its release identity remain unchanged at 63 instruction-reviewed methods.

## Jev result and stop condition

Pinned shared helper: `typesafe/jev-1.13-20260917`. Each request contains at most
eight independent single-skill domain questions. Only selected names and at most
700 description characters were sent, never full bodies or private session data.
Names can be recovered from frontmatter, a Markdown H1 title (at most 200
characters), or the repository path. That limited title excerpt is public source
data, not trusted instructions or proof that every file uses formal frontmatter.
Identical selected inputs share a judgment; distinct bodies remain distinct.
The request order is round-robin by representative repository to prevent the
largest benchmark collection consuming the entire initial pass.

Nine calls returned valid results for 72 metadata inputs. The tenth returned
`unavailable: distribution-sum`: the helper rejected a probability distribution
that did not satisfy its contract. The run stopped immediately. No retries,
alternate-context dispatch, direct-provider bypass, budget increase, or validator
weakening occurred. No background continuation is scheduled.

| Exact-body disposition | Count |
| --- | ---: |
| Provisional Jev domain | 51 |
| Jev abstention | 22 |
| Jev selected unknown | 1 |
| Unavailable/rejected provider batch | 8 |
| Not dispatched | 8,758 |
| Conservative selected-metadata exclusion | 1 |
| Total | 8,841 |

The 72 successful input judgments comprise 50 domain proposals, 21 abstentions
and one unknown; shared inputs account for the slightly larger body counts.
Accounted cost including the failed call's reservation: 21,432 micro-USD ($0.021432).
That is accounting evidence, not a claim about the final invoice. Jev confidence
does not establish accuracy, quality, safety or semantic equivalence.

## Discover and refine

All source aliases participate in the existing offline cold-intake search:

```powershell
node scripts/query-cold-intakes.mjs --query "paper planning" --limit 3
```

The new intake also has an exact-manifest-bound domain/status-aware lookup:

```powershell
node scripts/query-catalog-skill-intake.mjs --query "documentation" --domain writing --limit 3
node scripts/query-catalog-skill-intake.mjs --query "simulation" --limit 3
```

Omit the domain filter to search unclassified material too. An empty filtered
result does not establish absence of a skill. These commands expose bounded
metadata, not instructions to follow automatically.

`data/quarry-intake-2026-09-21-catalog801/refinement-queue.jsonl` accounts for every
distinct body, all source aliases and its exact classification state. The queue
requires body review and original synthesis before promotion. A useful next
refinement must inspect the pinned method and relevant notices, compare it with
the current product, and earn a new method or owner extension through evidence.
This intake alone promotes zero skills.

## Reproduction and continuation

Source and classification manifests bind every tracked input/output and provider
receipt. The full request expansion is reproducible from metadata and is ignored
by Git to avoid committing redundant request payloads. No provider is called by
the builder or query tools:

```powershell
node scripts/build-catalog-skill-queue.mjs
node --test tests/catalog-skill-intake.test.mjs tests/catalog-skill-classification.test.mjs tests/catalog-skill-intake-artifacts.test.mjs
```

The extraction script is intentionally a source-bound workstation maintenance
tool, not a dependency of the universal product. Its prior-corpus input can be
supplied by `GODSKILLS_PRIOR_CORPUS`; the exact path/hash is recorded. It refuses
changed acquisition manifests or changed repository HEAD/origin and writes fresh
intake files exclusively. Do not rerun it over this frozen intake.

## Review disposition

Luna reviewer Bacon (`01a0c674-bdb9-7822-b907-9dad2691cc34`) independently inspected
the intake/classification code and ran its focused tests. It reported no critical
findings and four important hardening findings. The parent applied and tested:

- Exact persisted request-plan digest verification, plus required inner digest
  and snapshot binding even for the saved unavailable response.
- Credential-pattern checking before name/description clipping, including
  fine-grained GitHub token patterns. Rebuilding selected metadata from all saved
  sources after this change produced exactly the original input bytes; no
  additional suspect input had been sent. No actual name hit the clipping bound.
- Strict completion: every body needs a non-unknown provisional domain; an
  abstention, exclusion, unavailable or uncalled record cannot count as complete.
- Duplicate source-ID rejection and exact regular-plus-excluded source accounting.
- Canonical prior-corpus paths and idempotent output checks that refuse to replace
  changed queue/summary evidence.

Two reviewer scope suggestions were resolved by making the claim boundary
explicit rather than rewriting past evidence. A bounded public Markdown title is
permitted selected metadata here; it is not a promise of frontmatter-only input.
The shared helper's network wrapper is outside this repository's request snapshot.
The receipt records the tool arguments, helper identity and required returned model,
not an independently observed exact network envelope. The helper uses a wire alias
and rejects a different returned pinned model. Its policy/model were not changed.
The reviewer did not independently inspect every source, verify every generated
receipt, or re-review the parent's final corrections. The final regression checks
are parent-run evidence, not a second independent certification.

Final pre-integration suite: 1,011 tests, 1,009 passed, zero failures and two
conditional skips (Windows file-symlink creation unavailable; cross-volume test
not enabled for this intake-only change). The 19 focused intake, classification
and frozen-artifact tests passed. Existing product bytes and the coordinated
Godagents-facing protected paths are unchanged. The ordinary paper-planning
query and Jev-filtered documentation query returned bounded source metadata.

Any later provider run must respect the shared helper's current status and
limits, preserve these receipts, never redispatch uncertain/refused attempts under
fresh IDs, and create a separately reviewable continuation. The current task is
not represented as fully classified or synthesized.
