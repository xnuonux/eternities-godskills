# Eternities Godskills release one

Release one is certified as a narrow, working sovereign capability layer. It does not claim that the complete warehouse has been rewritten. It certifies the source quarry, the evidence and promotion machinery, one refined skill, one categorical Godskill, and a reversible two-skill Codex profile.

## Measured result

- 12 approved starred repositories were reconciled: 9 newly cloned and 3 exact existing remotes, with no failed row.
- The cold catalog contains 4,741 unique source records, including 65 new skill entries from five newly ingested skill-bearing repositories.
- The catalog build is byte-deterministic and receipts the exact certified index hash `c0f865f263c8eed61b727af47544d494c56b711e952ae3f6ce94eb61304eb687`.
- Duplicate evidence contains 360 exact-name and exact-content groups, 3 different-name alias-evidence groups, and 142 same-name behavioral candidate groups. No group is automatically merged or deleted.
- The first-party ontology has 16 named capability families plus `general`. Membership is multi-label lexical evidence, so family counts overlap.
- The promotion engine fails closed across critical coverage, direct requests, paraphrases, exclusions, conflicts, token budgets, unresolved effects, and measured baseline improvement.

## Promoted capabilities

### Sovereign Skill Refinery

`sovereign-skill-refinery` converts a small source quarry into a neutral contract, exact provenance dispositions, independently written instructions, baseline evaluation, and an explicit promotion decision. It passed 8 of 8 critical static cases and measures 820 estimated entrypoint tokens against an 8,800-token composite baseline.

### Eternities Oracle

`eternities-oracle` selects local, current official, or hybrid evidence paths for consequential research. It separates verified, unverified, rejected, and deferred findings and yields to installed exact skills and ordinary engineering. It passed 10 of 10 critical static cases and measures 885 estimated entrypoint tokens against a 7,200-token local-only composite baseline.

These promotions prove the declared deterministic fixtures and policy gates. They do not claim a broad live-model benchmark. Fresh Codex proof separately verified that both names enter discovery.

## Active Codex profile

The global `eternities-core` profile contains exactly two junctions:

- `C:\Users\Dom\.agents\skills\eternities-oracle`
- `C:\Users\Dom\.agents\skills\sovereign-skill-refinery`

Both target the merged `C:\dev\eternities-godskills` main checkout and are owned by `receipts/profile-eternities-core.json`. Preview after activation reports zero additions and two exact existing links. The rollback drill reports exactly those two links and performs no deletion.

A fresh `codex debug prompt-input 'say hello'` render returned exit code 0 and contained both skill names. It did not contain `eternities-pantheon`, `source-records.jsonl`, `source-ledger.jsonl`, or `skill-index.json`. The 4,741-source quarry and vector matrix therefore remain cold.

## Provenance and safety

- Ten exact source identities are retained in the provenance ledger across the refinery and Oracle.
- Unknown or restricted sources remain pattern-only. The generated skills do not copy candidate prose and do not claim legal clearance.
- Repository ingestion never initializes submodules or executes third-party repository code.
- Missing descriptions are explicit. Their content digests include source identity so blank translated copies cannot become false exact matches.
- Global activation accepts promoted receipts only, refuses collisions, verifies junction targets, preserves first-activation ownership across repeated activation, and removes only links its own receipt created.

## Remaining uncertainty

- 4,739 source records have not become promoted first-party skills. The quarry is evidence, not completion.
- 142 behavioral candidate groups require neutral-contract comparison before any synthesis.
- 159 source records have no original indexed description.
- 439 records remain in the `general` ontology family.
- Lexical classification is deterministic but broad. In particular, generic agent terminology makes `agent-orchestration` overlap many records.
- Fresh prompt testing proves discovery and absence of cold-payload leakage, not every live routing decision.
- The active junctions target the merged main checkout. Any future repository relocation must migrate them through the exact-target profile workflow.

## Continuation map

Future work should be planned and certified separately:

1. bulk neutral-contract extraction with human-reviewed cluster dispositions;
2. highest-leverage refined engineering skills;
3. visual and narrative Godskills;
4. memory and context Godskills;
5. governance and external-action Godskills;
6. Ultragodskill composition and an explicit-only Pantheon.

The exact machine-readable verdict is `receipts/release-one-certification.json`.
