# Catalog continuation integration

Approved continuation of Dom's Godskills intake work. Completion: saved valid Jev continuation judgments participate in the normal cold-source query, original frozen intake and receipts remain unchanged, and malformed or conflicting additions fail closed. This is metadata discovery, not product promotion.

1. Reuse exact request reconstruction and receipt normalization. Add a compact, explicitly enumerated continuation manifest with request-plan binding and per-receipt byte hashes; no directory-wide trust or duplicate copies of the full queue.
2. Test first: valid additions become searchable, originals remain unchanged, and unknown/duplicate requests, changed hashes/bindings, local admission refusals, and conflicting original receipts cannot become labels.
3. Integrate the normal query with the validated overlay. Include provider-bound unavailable results as unavailable, preserve abstentions, and exclude the unbound local paced refusal with an explicit documented reason. Do not trust precomputed validated-labels files instead of raw receipts.
4. Verify actual saved receipts and deterministic output, run focused tests and the full suite if shared query behavior changes, independently review, fix material findings, then merge/push under standing approval. Preserve worktrees and all protected activation artifacts.

Scope: no new API calls, key changes, source execution, model routing, installed skill changes, or bulk reclassification. Next product phase remains family refinement and compact-guidance evaluation; this checkpoint makes existing intake useful first.
