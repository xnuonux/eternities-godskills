# Godskills completion authority and gates

On 2026-09-22 Dom explicitly authorized MiMo Pro to perform the remaining Godskills work without a numerical spending ceiling, with the parent Codex engineer orchestrating and finalizing the complete result. This supersedes the pending $1/$5 batch question. It is specific to Godskills completion, not unrelated projects, account purchases, safeguards, automatic fallback providers, or duplicate in-flight jobs.

The parent reconciled canonical main with origin/main at `fa1617889bcf50741b157c34d582011427495ccd` before dispatch (zero commits ahead/behind). Work remains isolated in `feat/universal-product-v1`. No new paid-call ceiling is imposed locally; provider-side account/rate limits still apply. Use MiMo 2.6 Pro for implementation/refinement and separate review jobs, deterministic tooling for exact accounting, and parent judgment for acceptance. Track real worker completion and actual usage; keep shared prompt prefixes stable. Missing cache usage is unknown, not a claimed saving.

## Definition of done

1. Reconcile acquired corpus strata without counting aliases as additional source bodies. Preserve explicit unresolved identities and source access limitations.
2. Give each in-scope source a traceable disposition; classification alone is not body review or completed synthesis. Useful distinct mechanisms enter portable methods or conditional references, while duplicates, shallow launchers, vendor adapters and unsupported material receive reasoned dispositions.
3. Make all delivered methods discoverable through the compact portable catalog, with useful triggers/exclusions, valid related links, provenance, and accurate maturity. No compulsory personal machine paths, warehouse, paid API or private mind dependency in the product.
4. Test implementation changes and review authored instructions. Record actual agent exercises separately from route fixtures. Fix material defects without weakening gates or rebranding unresolved work as complete.
5. Reconcile upstream, run relevant regression/portability/install checks, preserve the three frozen activation paths and historical receipts, integrate verified changes, and publish only verified repository work under existing authorization.
6. Refresh the authorized local agent installation using the verified installer, preserve unrelated files and rollback evidence, and verify installed manifests. Explain that already-open tasks may cache discovery metadata.

Completion means a finished finite release with honest source dispositions and explicit limits, not omniscience, universal superiority, or acquisition of every future skill on the web. The gap audit and subsequent receipts must distinguish finished product work from excluded or genuinely inaccessible source evidence.
