# Universal intake distillation, wave 7

Baseline: `5b82972c857044244a6a9bdee00b3134167ed41d`. This wave strengthens three
existing capabilities instead of increasing the top-level skill count. The pack
still contains 63 instruction-reviewed methods across 21 primary domains. It does
not declare the entire source corpus refined.

## Three portable extensions

- **Arcadia:** browser/game runtime lifetime, animation ownership, bounded frame
  timing, resize and input boundaries, stale asset completion, and ownership-aware
  teardown. The reference is engine-neutral and loaded only for relevant work.
- **Chorus:** evidence-bearing stakeholder updates with metric comparability,
  separate recipient/reviewer material, natural audience-appropriate voice, and
  proportionate action coordination. A simple message does not need a ledger.
- **Agora:** account and evidence-room decisions that separate document presence,
  freshness, restrictions, validation and approval. Pipeline, account health,
  metric and escalation signals stay attributable rather than becoming a false
  all-purpose score. Only relevant lanes are used.

No new host, provider, warehouse, private data, fixed filesystem, or paid service
is required by these methods. They neither grant external-action authority nor
require asking again for authority already supplied. They preserve the agent's
judgment and ordinary useful prose rather than forcing a persona or template.

Two non-forked Luna max source-review workers supplied bounded drafts. The parent
reworked their conditionality, orthogonal document states, analyst judgment and
authorization boundaries. Eleven entrypoints plus one subordinate browser
checklist were bound to exact source identities, hashes, commits and Git blobs.
See the [source receipt](../artifacts/universal-product-v1/wave7/source-integrity.json)
and [per-source dispositions](../artifacts/universal-product-v1/wave7/parent-refinement.md).
Unresolved licenses remain identified; no source prose, templates or code were
copied into the product. No acquired instructions were executed.

## Source-form triage is not completed synthesis

A single bounded Jev call advised on three public-source body selections. It cost
43 microusd as reported/accounted by the service; this is not the wave's total
compute cost. One selection was a parent paraphrase, not a verbatim source quote,
as documented in the preserved receipt notes.

A deterministic observer recognizes one exact description-and-launcher body
template. Of the 410 frozen family leads, 153 match that template and 257 do not.
All source bytes and current commits verified. This does not classify source
safety, frontmatter, external implementations, semantic quality or total capability.
An unmatched body is not automatically substantive. No source is promoted or
deleted, and all observation rows retain `modelBodyReview: false`.

The old observation command still produces the identical legacy report. Its
original report, family plan and intake remain immutable. The new optional report
is [separate evidence](../artifacts/universal-product-v1/wave7/family-body-forms.json).
This prevents launcher counts from silently becoming claimed finished skills.

## Actual consumer exercises

A fresh Luna max consumer implemented a browser-scene controller from frozen task
and method inputs. The parent read its code before execution and ran the separately
frozen checker: **10 passed, zero failed**. It handled duplicate starts, bounded
time deltas, stale frames, late owned leases, borrowed resources, restart races,
obsolete errors and clock rebasing. See the
[result](../artifacts/universal-product-v1/wave7/browser-exercise/result.json).
This is a synthetic adapter exercise, not DOM/GPU/browser qualification or visual
quality evidence.

The separate business/communication exercise uses fictional records and frozen
inputs. Parent judgment recorded **15 passes and 3 partials** against 18 predeclared
criteria. Its email changed an agreement to request an update into a claim that
the request had already been made. The closing recommendation also joined two
different decision gates, and required-document states were listed without the
explicit completeness fraction. Correct metrics, privacy separation and useful
next steps do not erase those defects. The first output is preserved unchanged in
[the original exercise](../artifacts/universal-product-v1/wave7/business-communication-exercise/result.json).

The product now explicitly checks action tense, separates each decision's actual
prerequisites, and states required-document numerators/denominators. A fresh
different-case recheck recorded **5 passes and 1 partial**. It correctly separated
Trial eligibility from bundle readiness, gave required availability as 1/2, and
kept the intended request in the future. However, its email softened an absent
report into “still being confirmed,” which can imply an ongoing action not
established by the records. Its internal packet correctly said absent. The
[recheck](../artifacts/universal-product-v1/wave7/business-recheck/result.json) is
preserved, not called a fully passing behavior qualification. Further wording was
not piled onto the method to chase one sample. The changed task and explicit
pitfall reminders also prevent attributing its results causally to the revision.

These are assisted single samples with native host instructions and explicit task
requirements, not raw baselines, repeated trials or evidence of causal superiority.

## Verification and release

Discovery regressions failed before the new references/metadata and passed after
integration. Search remains explainable lexical matching with advisory relations,
not semantic certainty, vector retrieval, or automatic activation. The initial
full batch run reported 992 tests: 991 passed, zero failed and one pre-existing
Windows file-symlink fixture skipped. Real junction and C:/D: checks ran.

The final independent bounded review passed all 12 focused tests and product
validation, found no critical/important/minor issue in its declared scope, and
bound 22 file hashes. The parent rechecked those exact hashes before integration.
The fresh final full run again reported 992 tests: 991 passed, zero failed, one
pre-existing Windows file-symlink fixture skipped. See
[the independent review](universal-wave7-independent-review.md). Consumer judgment
and overall outcome quality are outside that review verdict; their partials above
remain. The broader corpus's unresolved identities and unrefined bodies remain
unfinished. Canonical publication, installation and ZIP receipts follow below.

A standalone copy validated and found all three owners with an empty PATH and an
explicitly supplied Node executable. No package installation, warehouse or network
was used. This is a Windows clean-copy check, not Linux/macOS qualification.
