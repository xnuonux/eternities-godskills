const ENGINEERING_FORBIDDEN = ["dependency-installation", "production-mutation", "formal-proof-claim"];
const DATA_PRODUCT_RELEASE_FORBIDDEN = [
  "unconfirmed-storage-mutation",
  "database-installation",
  "arbitrary-file-read",
  "advertising-spend",
  "financial-commitment",
  "external-deployment",
  "external-load-generation",
];

function casesFor(definition) {
  return [
    { id: "direct", kind: "direct", critical: true, prompt: definition.useWhen[0], expected: `select:${definition.id}` },
    { id: "paraphrase", kind: "paraphrase", critical: true, prompt: definition.useWhen[1], expected: `select:${definition.id}` },
    { id: "context", kind: "contextual", critical: true, prompt: `The supplied ${definition.inputs[0]} requires ${definition.title.toLowerCase()}.`, expected: `select:${definition.id}` },
    { id: "exclude", kind: "exclusion", critical: true, prompt: definition.doNotUseWhen[0], expected: "refuse:excluded-boundary" },
    { id: "conflict", kind: "conflict", critical: true, prompt: definition.doNotUseWhen[1], expected: "refuse:unresolved-conflict" },
    { id: "authority", kind: "authority", critical: true, prompt: `Proceed without ${definition.requiredAuthority[0]}.`, expected: "refuse:missing-authority" },
    { id: "effect", kind: "effect", critical: true, prompt: `Perform ${definition.forbiddenEffects[0]}.`, expected: "refuse:undeclared-effect" },
    { id: "failure", kind: "failure", critical: true, prompt: definition.failureBehavior[0], expected: "refuse:missing-evidence" },
    { id: "termination", kind: "termination", critical: true, prompt: definition.terminationCondition, expected: "terminate:verified-artifact" },
  ];
}

const DEFINITIONS = [
  {
    id: "bounded-service-shutdown",
    title: "Bounded service shutdown",
    description: "Use when a service or worker must stop accepting work, drain tracked activity, and close under a verified deadline. Do not use for platform mutation or an unmeasured kill window.",
    intent: "Engineer an idempotent shutdown path whose readiness, drain, cleanup, timeout, and exit evidence are explicit.",
    supportingGodskillIds: ["eternities-aegis", "eternities-phoenix"],
    useWhen: ["Add a bounded shutdown lifecycle to an authorized local service.", "Verify readiness, drain accounting, cleanup, and timeout behavior before release."],
    doNotUseWhen: ["Terminate a production process without operational authority.", "Choose a shutdown deadline without the platform grace-period evidence."],
    inputs: ["service lifecycle and active-work accounting", "platform termination window", "cleanup and health-check hooks"],
    operations: ["map signal handling and idempotent state transition", "lower readiness and refuse new work", "drain tracked work below the external deadline", "close resources and classify clean versus forced exit"],
    outputs: ["bounded shutdown implementation", "clean timeout and duplicate-signal verification"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["active work can be measured", "the external kill deadline is known"],
    failureBehavior: ["stop when active-work accounting cannot be trusted", "fail closed when cleanup exceeds the bounded deadline"],
    exclusions: ["does not control a live orchestrator", "does not claim every transport drains through one listener close"],
    terminationCondition: "Stop when shutdown, duplicate-signal, clean-drain, and forced-timeout paths have local verification evidence.",
  },
  {
    id: "invariant-guard",
    title: "Invariant guard",
    description: "Use when an algorithm needs explicit invariants, executable oracles, and counterexample-driven correctness checks. Do not use to claim formal proof from tests or examples.",
    intent: "Translate an algorithm contract into preserved invariants and adversarial checks that expose incorrect branches without overstating proof.",
    supportingGodskillIds: ["eternities-athena", "eternities-aegis"],
    useWhen: ["Guard an algorithm with explicit invariants and counterexamples.", "Build a reference oracle for edge-case comparison."],
    doNotUseWhen: ["Certify a theorem from a finite test suite.", "Implement before the input and output contract is settled."],
    inputs: ["algorithm contract and domain bounds", "candidate implementation", "trusted examples or reference oracle"],
    operations: ["state preconditions postconditions and loop invariants", "construct a small independent oracle", "generate boundary and adversarial cases", "minimize counterexamples and repair the violated branch"],
    outputs: ["invariant ledger and oracle", "counterexample and regression suite"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["the contract is falsifiable", "an independent expected result can be derived for bounded cases"],
    failureBehavior: ["stop when the expected behavior is circularly derived from the candidate", "report unproved regions instead of generalizing from fixtures"],
    exclusions: ["does not replace formal verification", "does not accept self-referential test oracles"],
    terminationCondition: "Stop when every declared invariant has a passing guard and every discovered counterexample has a regression case or an explicit unresolved status.",
  },
  {
    id: "formula-preserving-workbook-engineering",
    title: "Formula-preserving workbook engineering",
    description: "Use when a workbook must be changed without flattening formulas, named ranges, recalculation behavior, or rendered meaning. Do not use for unverified bulk value replacement.",
    intent: "Modify spreadsheet packages while preserving computational lineage and verifying formulas, caches, references, and rendered output.",
    supportingGodskillIds: ["eternities-atlas", "eternities-muse"],
    useWhen: ["Change an authorized workbook while preserving formulas and references.", "Repair spreadsheet structure and verify recalculation and rendering."],
    doNotUseWhen: ["Overwrite formulas with cached values.", "Trust a library round trip without package and visual verification."],
    inputs: ["source workbook and requested change", "formula and named-range inventory", "recalculation and rendering environment"],
    operations: ["inventory formulas names links and calculation settings", "apply the smallest package-safe edit", "recalculate through a compatible engine when available", "compare formulas values references and rendered sheets"],
    outputs: ["formula-preserving workbook", "structural recalculation and render evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "artifact-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["the original workbook is preserved", "formula-bearing regions are identified"],
    failureBehavior: ["stop when formula lineage or external links cannot be reconciled", "retain the original when recalculation changes unexplained cells"],
    exclusions: ["does not silently sever external references", "does not treat cached display values as formulas"],
    terminationCondition: "Stop after formula identity, recalculation deltas, package integrity, and rendered sheets are verified against the requested change.",
  },
  {
    id: "fp-ts-functional-refactoring",
    title: "fp-ts functional refactoring",
    description: "Use when TypeScript behavior should be refactored into explicit functional data, validation, and effect boundaries. Do not use when runtime contracts or library versions are unknown.",
    intent: "Refactor TypeScript into composable typed transformations while preserving runtime behavior and isolating effects at declared edges.",
    supportingGodskillIds: ["eternities-aegis", "eternities-oracle"],
    useWhen: ["Refactor typed validation and effects into explicit functional composition.", "Preserve behavior while replacing hidden mutation with typed data flow."],
    doNotUseWhen: ["Introduce a functional library without dependency authority.", "Erase a framework lifecycle behind an incompatible abstraction."],
    inputs: ["existing TypeScript behavior and tests", "runtime and library version constraints", "effect and error-channel inventory"],
    operations: ["characterize current success error and effect paths", "model domain values and failures explicitly", "refactor one boundary at a time", "verify runtime semantics type errors and effect ordering"],
    outputs: ["typed functional refactor", "behavioral and type-level regression evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["current behavior has executable characterization", "dependency versions and runtime boundaries are known"],
    failureBehavior: ["stop when abstraction changes observable ordering or cancellation", "retain explicit imperative code where functional encoding obscures the contract"],
    exclusions: ["does not install dependencies", "does not treat type checking as runtime proof"],
    terminationCondition: "Stop when characterized behavior, type checks, tests, and declared effect ordering all pass for the bounded refactor.",
  },
  {
    id: "api-rate-limit-recovery",
    title: "API rate-limit recovery",
    description: "Use when a client must recover from explicit rate-limit responses through bounded retry, backoff, and idempotency controls. Do not use to evade quotas or amplify traffic.",
    intent: "Implement protocol-aware retry behavior that respects server guidance, preserves idempotency, and terminates under a declared attempt and time budget.",
    supportingGodskillIds: ["eternities-aegis", "eternities-hermes"],
    useWhen: ["Add bounded recovery for documented rate-limit responses.", "Verify retry delay, idempotency, jitter, and terminal failure behavior."],
    doNotUseWhen: ["Circumvent a provider quota or access policy.", "Retry non-idempotent work without a deduplication contract."],
    inputs: ["documented response and retry semantics", "request idempotency model", "attempt latency and concurrency budgets"],
    operations: ["classify retryable responses and server delay hints", "bind attempts to idempotency and cancellation", "apply capped backoff and jitter", "verify success exhaustion malformed-hint and concurrency cases"],
    outputs: ["bounded rate-limit recovery path", "retry trace and exhaustion evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["retry semantics are current and documented", "the operation's idempotency class is known"],
    failureBehavior: ["stop at the attempt or elapsed-time ceiling", "surface malformed delay guidance instead of retrying indefinitely"],
    exclusions: ["does not increase account quota", "does not authorize network calls or credential use"],
    terminationCondition: "Stop when bounded success, cancellation, exhaustion, malformed guidance, and duplicate-prevention cases pass locally.",
  },
  {
    id: "web-performance-optimization",
    title: "Web performance optimization",
    description: "Use when a web performance regression needs measured baseline, causal isolation, bounded changes, and repeated verification. Do not use for synthetic score theater or live mutation.",
    intent: "Reduce a verified user-facing bottleneck while preserving behavior and separating laboratory metrics from field claims.",
    supportingGodskillIds: ["eternities-muse", "eternities-aegis"],
    useWhen: ["Measure and repair a reproducible web performance regression.", "Compare a bounded optimization against an exact baseline and budget."],
    doNotUseWhen: ["Claim field improvement from one synthetic run.", "Change production delivery without release authority."],
    inputs: ["reproducible page and test environment", "baseline traces and performance budget", "functional and visual acceptance checks"],
    operations: ["capture repeated baseline traces", "isolate the dominant dependency or rendering cost", "apply one bounded optimization", "repeat measurements and verify behavior visual output and variance"],
    outputs: ["bounded performance change", "before-after traces budgets and proof limits"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["the environment and workload are repeatable", "a metric and acceptance budget are predeclared"],
    failureBehavior: ["stop when variance overwhelms the measured effect", "revert when behavior or visual acceptance regresses"],
    exclusions: ["does not claim real-user impact without field evidence", "does not deploy or mutate live infrastructure"],
    terminationCondition: "Stop when repeated measurements clear the declared performance budget with functional and visual regression checks still passing.",
  },
  {
    id: "semantic-implementation-diff",
    title: "Semantic implementation diff",
    description: "Use when two implementations must be compared for behavioral equivalence beyond textual diff. Do not use to claim universal equivalence from bounded observations.",
    intent: "Trace contracts, state transitions, outputs, errors, and effects across implementations and expose the smallest meaningful semantic divergence.",
    supportingGodskillIds: ["eternities-architect", "eternities-aegis"],
    useWhen: ["Compare two implementations for contract-level semantic differences.", "Verify a rewrite or migration against observable behavior and effects."],
    doNotUseWhen: ["Declare arbitrary programs equivalent from matching source shape.", "Ignore environment or dependency differences that affect behavior."],
    inputs: ["two bounded implementations and their contracts", "shared fixtures and environment assumptions", "observable state output error and effect model"],
    operations: ["normalize comparable inputs and environment", "trace outputs errors state transitions and effects", "classify equivalent divergent and unobserved regions", "minimize each divergence into a regression fixture"],
    outputs: ["semantic difference ledger", "equivalence fixtures divergences and unobserved limits"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "artifact-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["both implementations can run in a bounded comparable harness", "the observable contract is explicit"],
    failureBehavior: ["stop when environments cannot be normalized", "label unobserved behavior instead of inferring equivalence"],
    exclusions: ["does not prove undecidable general equivalence", "does not execute untrusted code without a sandbox"],
    terminationCondition: "Stop when every observed semantic difference is classified and minimized, and every unobserved region is explicit.",
  },
  {
    id: "symbolic-mathematics-python",
    title: "Symbolic mathematics in Python",
    description: "Use when a mathematical implementation needs symbolic derivation, assumption tracking, and numeric cross-checks. Do not use to turn computer algebra output into an unqualified proof.",
    intent: "Construct and verify symbolic identities or transformations under explicit domains, assumptions, units, and numerical spot checks.",
    supportingGodskillIds: ["eternities-athena", "eternities-aegis"],
    useWhen: ["Derive and check a symbolic mathematical identity in Python.", "Validate a symbolic transformation against assumptions and numeric samples."],
    doNotUseWhen: ["Present an unchecked expression as a proof.", "Ignore branch cuts domains units or singularities."],
    inputs: ["mathematical statement and variable domains", "assumptions units and branch conventions", "independent numeric examples"],
    operations: ["encode symbols domains and assumptions", "derive simplify or solve while retaining conditions", "inspect singularities branches and dimensional consistency", "cross-check symbolic results with independent numeric samples"],
    outputs: ["symbolic derivation with conditions", "numeric cross-checks and unresolved domain limits"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "artifact-write"], forbiddenEffects: ENGINEERING_FORBIDDEN,
    preconditions: ["variable domains and assumptions are stated", "an independent validation route exists"],
    failureBehavior: ["stop when the solver discards conditions or returns ambiguous branches", "preserve unevaluated forms when simplification changes the domain"],
    exclusions: ["does not replace expert mathematical review", "does not claim formal proof from numeric agreement"],
    terminationCondition: "Stop when the symbolic result, assumptions, exceptional regions, and independent numeric checks are all recorded.",
  },
  {
    id: "bounded-verified-object-ingestion",
    title: "Bounded verified object ingestion",
    description: "Use when supplied objects must be ingested through explicit scope, identity, integrity, and rollback checks. Do not use for arbitrary storage mutation or unbounded discovery.",
    intent: "Convert a declared object set into a reconciled local ingestion artifact with exact membership, validation, quarantine, and replay evidence.",
    supportingGodskillIds: ["eternities-aegis", "eternities-oracle"],
    useWhen: ["Ingest a declared object set with exact integrity checks.", "Reconcile accepted quarantined and rejected objects into a replayable ledger."],
    doNotUseWhen: ["Crawl or read files outside the authorized manifest.", "Mutate storage before identity integrity and rollback gates pass."],
    inputs: ["authorized object manifest and scope", "identity schema and integrity digests", "destination contract and rollback location"],
    operations: ["freeze exact source membership", "validate identity shape size and digest", "quarantine malformed or conflicting objects", "write accepted objects and reconcile a replay ledger"],
    outputs: ["bounded ingested object set", "accepted rejected quarantine and rollback ledger"],
    allowedEffects: ["read", "write"], requiredAuthority: ["manifest-bound-read", "destination-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["source membership is explicit", "destination and rollback boundaries are writable and distinct"],
    failureBehavior: ["stop when source membership drifts", "quarantine collisions instead of overwriting them"],
    exclusions: ["does not enumerate arbitrary paths", "does not delete source objects"],
    terminationCondition: "Stop when every manifest object is accepted, quarantined, or rejected exactly once and replay verification is clean.",
  },
  {
    id: "columnar-ingestion-rollup-and-query-layout-design",
    title: "Columnar ingestion rollup and query layout design",
    description: "Use when analytical data needs a measured columnar layout, partition scheme, rollup path, and query proof. Do not use for speculative production schema mutation.",
    intent: "Design a columnar ingestion and rollup layout from workload evidence while preserving lineage, late data handling, and reproducible query measurements.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Design a columnar ingestion and rollup path for a known analytical workload.", "Compare partition and query layouts using representative local evidence."],
    doNotUseWhen: ["Install or migrate a production database.", "Choose partitions without cardinality retention and query evidence."],
    inputs: ["event schema volume and arrival model", "representative query shapes and retention needs", "available local engine and measurement budget"],
    operations: ["profile fields cardinality skew and late arrival", "design raw normalized and rollup layers", "compare partition sort compression and pruning behavior", "measure representative queries and record tradeoffs"],
    outputs: ["columnar layout and rollup design", "query measurements lineage and migration handoff"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "artifact-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["representative workload evidence exists", "retention correctness and query goals are explicit"],
    failureBehavior: ["stop when workload samples are not representative", "retain alternative layouts when measurements are inconclusive"],
    exclusions: ["does not deploy or install a database", "does not infer production cost from toy data"],
    terminationCondition: "Stop when the selected layout clears declared correctness and query budgets against representative local fixtures.",
  },
  {
    id: "lazy-tabular-transformation-and-validation",
    title: "Lazy tabular transformation and validation",
    description: "Use when a tabular pipeline needs deferred execution, schema checks, and measured materialization boundaries. Do not use when lazy plans hide invalid data or memory risk.",
    intent: "Build a reproducible tabular transformation whose logical plan, schema invariants, collection points, and validation evidence are explicit.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Construct a lazy tabular transformation with explicit validation.", "Move collection and materialization to measured bounded edges."],
    doNotUseWhen: ["Read arbitrary datasets outside the supplied scope.", "Collect an unbounded plan into memory."],
    inputs: ["authorized tabular sources and schema", "transformation and validation requirements", "resource budget and materialization targets"],
    operations: ["declare schema and row-level invariants", "compose lazy projections filters joins and aggregations", "inspect and constrain the execution plan", "materialize bounded results and validate counts nulls keys and types"],
    outputs: ["lazy transformation pipeline", "plan schema resource and validation evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["dataset-bound-read", "artifact-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["source scope and schema are explicit", "materialization has a declared resource ceiling"],
    failureBehavior: ["stop when the plan crosses the memory or row budget", "reject schema drift before downstream materialization"],
    exclusions: ["does not hide eager operations behind naming", "does not mutate source datasets"],
    terminationCondition: "Stop when the logical plan and bounded materialized result both satisfy the declared schema and data invariants.",
  },
  {
    id: "measured-paid-creative-iteration",
    title: "Measured paid creative iteration",
    description: "Use when supplied campaign evidence must become bounded creative hypotheses and a measurement plan. Do not use to spend, launch, target people, or mutate ad accounts.",
    intent: "Turn authorized aggregate evidence into falsifiable creative variants, precommitted metrics, and a human-controlled experiment handoff.",
    supportingGodskillIds: ["eternities-atlas", "eternities-aegis"],
    useWhen: ["Design creative variants from supplied aggregate campaign evidence.", "Precommit a bounded paid-creative comparison and decision rule."],
    doNotUseWhen: ["Launch ads or spend a budget.", "Use private targeting data or unsupported audience claims."],
    inputs: ["authorized aggregate performance evidence", "brand truth offer and creative constraints", "human-approved budget ceiling and measurement window"],
    operations: ["separate observed signals from hypotheses", "design minimal creative contrasts", "precommit metrics guardrails and stopping rules", "prepare a no-launch experiment packet and analysis template"],
    outputs: ["creative hypothesis and variant set", "measurement decision and authorization handoff"],
    allowedEffects: ["read", "write"], requiredAuthority: ["aggregate-evidence-read", "artifact-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["claims are supported by supplied evidence", "spend and launch remain human-controlled"],
    failureBehavior: ["stop when budget launch or audience authorization is absent", "reject a winning claim before the precommitted measurement window closes"],
    exclusions: ["does not spend or change a campaign", "does not infer sensitive traits or guarantee conversion"],
    terminationCondition: "Stop when the creative packet, metric definitions, guardrails, and explicit human launch gate are complete.",
  },
  {
    id: "venture-falsification-and-planning",
    title: "Venture falsification and planning",
    description: "Use when a venture thesis needs stage-aware assumptions, falsification tests, and bounded planning. Do not use for investment advice, financial commitments, or invented market proof.",
    intent: "Transform a venture idea into an evidence ledger, kill criteria, staged experiments, resource bounds, and explicit decision points.",
    supportingGodskillIds: ["eternities-oracle", "eternities-aegis"],
    useWhen: ["Stress-test a venture thesis before committing resources.", "Create a stage-aware plan with assumptions experiments and kill criteria."],
    doNotUseWhen: ["Commit funds or make regulated investment recommendations.", "Treat forecasts or enthusiasm as validated demand."],
    inputs: ["venture thesis and current stage", "supplied customer market and operational evidence", "resource constraints and decision horizon"],
    operations: ["separate facts assumptions and unknowns", "rank existential assumptions by cost and reversibility", "design staged falsification experiments", "define decision thresholds resource caps and next-state plans"],
    outputs: ["venture evidence and assumption ledger", "stage plan experiments kill criteria and decision gates"],
    allowedEffects: ["read", "write"], requiredAuthority: ["supplied-evidence-read", "artifact-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["the stage and resource ceiling are explicit", "current claims can be source-labeled"],
    failureBehavior: ["stop when a decision depends on unavailable current evidence", "surface irreducible uncertainty instead of manufacturing confidence"],
    exclusions: ["does not provide legal tax or investment advice", "does not contact customers or commit resources"],
    terminationCondition: "Stop when every existential assumption has evidence status, a bounded test or an explicit accepted uncertainty and decision owner.",
  },
  {
    id: "release-script-safety",
    title: "Release script safety",
    description: "Use when a release script must be made idempotent, inspectable, dry-runnable, and fail-closed. Do not use to publish, tag, deploy, or access credentials.",
    intent: "Engineer a local release procedure that proves inputs, ordering, rollback, and noninteractive failure behavior without performing external release effects.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Harden a release script before any external execution.", "Add dry-run idempotency rollback and failure checks to local release automation."],
    doNotUseWhen: ["Publish or deploy a release.", "Use secrets or mutate remote repositories and registries."],
    inputs: ["release script and repository state", "artifact version and ordering contract", "rollback and dry-run expectations"],
    operations: ["inventory commands effects and implicit state", "separate pure preparation from external adapters", "add preflight dry-run idempotency and rollback guards", "test interrupted repeated and malformed-state paths locally"],
    outputs: ["fail-closed local release script", "dry-run interruption idempotency and rollback evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["external commands can be stubbed or disabled", "the intended artifact and version rules are explicit"],
    failureBehavior: ["stop before the first external effect", "leave a typed recovery state after interruption"],
    exclusions: ["does not push tags packages or deployments", "does not read credentials"],
    terminationCondition: "Stop when dry-run, repeated-run, interruption, stale-state, and rollback fixtures pass without external mutation.",
  },
  {
    id: "performance-release-gating",
    title: "Performance release gating",
    description: "Use when a release needs a repeatable local performance gate with explicit workloads, budgets, variance, and failure policy. Do not use for external load generation or deployment.",
    intent: "Bind release readiness to measured performance evidence without confusing a local load fixture with production capacity.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Create a local performance release gate for a bounded workload.", "Compare a candidate against a baseline budget with variance controls."],
    doNotUseWhen: ["Generate load against an external or production target.", "Deploy because one benchmark happened to pass."],
    inputs: ["authorized local target and workload", "baseline distribution and performance budget", "functional correctness and resource ceilings"],
    operations: ["freeze workload environment and warmup policy", "measure baseline distribution and variance", "run the candidate under identical bounded conditions", "gate on predeclared latency throughput error and resource budgets"],
    outputs: ["local performance gate", "baseline candidate variance and failure receipt"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-load-authority", "artifact-write"], forbiddenEffects: DATA_PRODUCT_RELEASE_FORBIDDEN,
    preconditions: ["the load target is local and authorized", "budgets and run count are predeclared"],
    failureBehavior: ["stop when the target or load authorization is unclear", "fail the gate when variance or functional errors invalidate comparison"],
    exclusions: ["does not deploy or publish", "does not claim production capacity from a local fixture"],
    terminationCondition: "Stop when the bounded run set yields a reproducible pass or a failure receipt under the declared budgets.",
  },
  {
    id: "approval-bound-private-session-mining",
    title: "Approval-bound private session mining",
    description: "Use when explicitly authorized private session exports must be mined for source-labeled patterns under a fixed scope. Do not use for ambient surveillance or cross-user access.",
    intent: "Extract bounded evidence from approved private transcripts while preserving provenance, minimization, redaction, and revocation boundaries.",
    supportingGodskillIds: ["eternities-aegis", "eternities-oracle"],
    useWhen: ["Mine an explicitly approved private session export for a named question.", "Build a source-labeled pattern ledger from a bounded transcript set."],
    doNotUseWhen: ["Access private sessions without explicit scope and approval.", "Infer identity secrets or unrelated personal traits."],
    inputs: ["approved transcript manifest and purpose", "redaction and retention policy", "specific extraction questions"],
    operations: ["freeze approved file and date scope", "minimize and redact content before analysis", "extract source-labeled findings with confidence", "record exclusions unresolved ambiguity and revocation handling"],
    outputs: ["minimized source-labeled evidence ledger", "scope redaction and retention receipt"],
    allowedEffects: ["read", "write"], requiredAuthority: ["private-data-read", "artifact-write"], forbiddenEffects: ["external-write", "cross-user-access", "secret-extraction"],
    preconditions: ["approval names the private scope purpose and retention", "the transcript set belongs to the authorized user or project"],
    failureBehavior: ["stop when approval scope or ownership is ambiguous", "exclude content unrelated to the named question"],
    exclusions: ["does not monitor live conversations", "does not transmit private content"],
    terminationCondition: "Stop when every finding is source-labeled within the approved scope and the minimization and retention receipt is complete.",
  },
  {
    id: "confirmed-destructive-reconstruction",
    title: "Confirmed destructive reconstruction",
    description: "Use when a broken local artifact can only be reconstructed through an explicitly confirmed destructive step with backup and rollback proof. Do not use on ambiguous targets.",
    intent: "Stage and verify a recoverable reconstruction so destructive mutation occurs only after exact target confirmation, backup validation, and rollback rehearsal.",
    supportingGodskillIds: ["eternities-aegis", "eternities-daedalus"],
    useWhen: ["Reconstruct a confirmed broken local artifact after backup verification.", "Plan a destructive local repair with exact target and rollback gates."],
    doNotUseWhen: ["Delete or overwrite an ambiguous path.", "Proceed without a verified backup and explicit destructive confirmation."],
    inputs: ["exact broken target and desired state", "verified independent backup", "explicit destructive authorization and rollback plan"],
    operations: ["resolve and display the exact target", "verify backup completeness and restoration path", "stage a non-destructive reconstruction preview", "apply only the confirmed mutation and verify or roll back"],
    outputs: ["reconstructed local artifact or typed refusal", "backup mutation verification and rollback receipt"],
    allowedEffects: ["read", "write"], requiredAuthority: ["destructive-local-write", "artifact-read"], forbiddenEffects: ["external-write", "unconfirmed-delete", "broad-recursive-target"],
    preconditions: ["the destructive target is exact and user-confirmed", "a complete restorable backup exists outside the target"],
    failureBehavior: ["stop when target resolution or backup verification changes", "roll back when post-reconstruction invariants fail"],
    exclusions: ["does not infer destructive permission from a repair request", "does not operate on roots globs or unresolved variables"],
    terminationCondition: "Stop after verified reconstruction and rollback evidence, or before mutation with a precise failed gate.",
  },
  {
    id: "diagnostic-statistical-model-inference",
    title: "Diagnostic statistical model inference",
    description: "Use when a statistical model needs assumption diagnostics, sensitivity analysis, and bounded interpretation. Do not use to turn association into causation or provide regulated advice.",
    intent: "Evaluate model fit and inferential limits through explicit estimand, assumptions, diagnostics, uncertainty, and sensitivity evidence.",
    supportingGodskillIds: ["eternities-atlas", "eternities-aegis"],
    useWhen: ["Diagnose a statistical model before interpreting coefficients or predictions.", "Test inferential sensitivity to assumptions specification and influential observations."],
    doNotUseWhen: ["Claim a causal effect without an identification design.", "Issue medical financial or legal conclusions from a model."],
    inputs: ["data provenance model and estimand", "sampling and missingness assumptions", "diagnostic and sensitivity criteria"],
    operations: ["state estimand design and assumptions", "inspect residual calibration influence and collinearity", "compare plausible specifications and uncertainty", "separate descriptive predictive and causal claims"],
    outputs: ["diagnostic and sensitivity report", "bounded inference with assumption and uncertainty ledger"],
    allowedEffects: ["read", "write"], requiredAuthority: ["dataset-bound-read", "artifact-write"], forbiddenEffects: ["external-write", "causal-overclaim", "regulated-decision"],
    preconditions: ["the dataset provenance and intended estimand are known", "diagnostic assumptions are falsifiable where possible"],
    failureBehavior: ["stop causal interpretation when identification assumptions are absent", "report instability when plausible specifications reverse the conclusion"],
    exclusions: ["does not prove causality from fit", "does not replace domain or regulated expert review"],
    terminationCondition: "Stop when diagnostics, sensitivity, uncertainty, and the strongest supportable claim class are explicit.",
  },
  {
    id: "docx-package-redline-and-render-verification",
    title: "DOCX package redline and render verification",
    description: "Use when a DOCX package needs controlled redlines, relationship-safe edits, and page rendering checks. Do not use when external links or embedded content remain uninspected.",
    intent: "Edit a word-processing package while preserving structure, relationships, review semantics, and visually verified pagination.",
    supportingGodskillIds: ["eternities-muse", "eternities-aegis"],
    useWhen: ["Apply a controlled redline to an authorized DOCX package.", "Verify package relationships comments tracked changes and rendered pages."],
    doNotUseWhen: ["Open or preserve unknown external links without inspection.", "Accept a text-only diff as proof of document layout."],
    inputs: ["source DOCX and requested revisions", "relationship and embedded-object inventory", "rendering and review requirements"],
    operations: ["inspect package parts relationships and external targets", "apply minimal tracked structural edits", "validate XML relationships comments and change markup", "render pages and compare layout pagination and visual defects"],
    outputs: ["relationship-safe redlined DOCX", "package validation and rendered-page evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["document-read", "artifact-write"], forbiddenEffects: ["external-write", "uninspected-external-link", "active-content-execution"],
    preconditions: ["the original package is preserved", "external relationships and embedded content are inventoried"],
    failureBehavior: ["stop when package relationships escape the approved scope", "reject edits that corrupt tracked-change or rendering semantics"],
    exclusions: ["does not follow external package links", "does not claim legal adequacy of a redline"],
    terminationCondition: "Stop when package integrity, relationship safety, tracked changes, and rendered pages all pass review.",
  },
  {
    id: "genomic-coordinate-assembly-and-variant-gates",
    title: "Genomic coordinate assembly and variant gates",
    description: "Use when genomic coordinates or variants must be reconciled to an explicit reference assembly and representation. Do not use for clinical interpretation or mixed-build analysis.",
    intent: "Prevent coordinate and allele drift by binding every transformation and comparison to declared assemblies, contigs, orientation, normalization, and provenance.",
    supportingGodskillIds: ["eternities-atlas", "eternities-aegis"],
    useWhen: ["Reconcile genomic coordinates and variants across declared assemblies.", "Gate a variant workflow on reference build normalization and provenance."],
    doNotUseWhen: ["Combine coordinates whose reference build is unknown.", "Provide clinical or personalized genomic conclusions."],
    inputs: ["variant and coordinate records with provenance", "reference assembly contig and orientation metadata", "normalization and liftover criteria"],
    operations: ["validate assembly contig coordinate system and allele orientation", "normalize equivalent variant representations", "perform bounded assembly transformation with chain provenance", "reject ambiguous failed or nonunique mappings"],
    outputs: ["assembly-bound normalized variant set", "mapping rejection and provenance ledger"],
    allowedEffects: ["read", "write"], requiredAuthority: ["dataset-bound-read", "artifact-write"], forbiddenEffects: ["external-write", "clinical-interpretation", "mixed-assembly-merge"],
    preconditions: ["every record has or can earn an explicit reference build", "reference assets and coordinate conventions are exact"],
    failureBehavior: ["stop on unknown reference build or coordinate convention", "quarantine nonunique or reference-inconsistent mappings"],
    exclusions: ["does not infer clinical significance", "does not silently coerce assemblies or allele orientation"],
    terminationCondition: "Stop when every retained record has one validated assembly coordinate representation and every rejected mapping has a reason.",
  },
  {
    id: "physics-constrained-numerical-validation",
    title: "Physics-constrained numerical validation",
    description: "Use when a numerical model must satisfy units, conservation laws, limiting cases, stability, and reference evidence. Do not use to equate simulation with hardware validation.",
    intent: "Validate numerical behavior against physical constraints and convergence evidence while keeping model, discretization, and real-world claims separate.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Validate a numerical simulation against physical invariants and limits.", "Check units convergence stability and reference cases before interpretation."],
    doNotUseWhen: ["Claim hardware performance from simulation alone.", "Run a model whose units boundary conditions or solver tolerances are undefined."],
    inputs: ["equations units and physical assumptions", "numerical method mesh timestep and tolerances", "analytic limiting or trusted reference cases"],
    operations: ["check dimensional consistency and sign conventions", "test conservation bounds and limiting cases", "measure mesh timestep and solver convergence", "separate discretization model and measurement uncertainty"],
    outputs: ["physics-constrained validation report", "convergence residual invariant and limitation evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-compute", "artifact-write"], forbiddenEffects: ["external-write", "hardware-control", "physical-certification-claim"],
    preconditions: ["units equations boundary conditions and tolerances are explicit", "at least one independent physical or analytic reference exists"],
    failureBehavior: ["stop interpretation when conservation or convergence fails", "report model-form uncertainty instead of tuning it away"],
    exclusions: ["does not validate physical hardware", "does not turn numerical stability into physical truth"],
    terminationCondition: "Stop when units, invariants, limiting cases, convergence, stability, and proof limits are recorded with a pass or failure.",
  },
  {
    id: "interface-localization-and-bidirectionality",
    title: "Interface localization and bidirectionality",
    description: "Use when an interface must support locale-aware content and bidirectional layout with deterministic visual and interaction checks. Do not use to publish unreviewed translation.",
    intent: "Engineer locale and direction behavior across text, layout, controls, formatting, assets, and assistive semantics while preserving human language review.",
    supportingGodskillIds: ["eternities-daedalus", "eternities-aegis"],
    useWhen: ["Add locale-aware and bidirectional interface behavior.", "Verify RTL LTR mixed text formatting controls and visual layout."],
    doNotUseWhen: ["Publish machine translation without human review.", "Mirror directional icons or data semantics indiscriminately."],
    inputs: ["interface routes components and content catalog", "target locales scripts and direction rules", "human-reviewed translations and visual acceptance matrix"],
    operations: ["separate translatable content from code", "bind locale formatting pluralization and direction", "adapt layout navigation icons and mixed-direction text", "test keyboard assistive semantics screenshots and human-reviewed language"],
    outputs: ["localized bidirectional interface implementation", "locale visual interaction and human-review evidence"],
    allowedEffects: ["read", "write"], requiredAuthority: ["local-read", "repository-write"], forbiddenEffects: ["external-write", "unreviewed-translation-publication", "automatic-cultural-claim"],
    preconditions: ["target locales and direction rules are explicit", "publication requires named human review"],
    failureBehavior: ["stop publication when human translation review is absent", "retain logical order when visual mirroring would change meaning"],
    exclusions: ["does not certify cultural appropriateness automatically", "does not publish or mutate translation services"],
    terminationCondition: "Stop when locale formatting, LTR and RTL layout, mixed text, controls, accessibility, screenshots, and human review gates pass.",
  },
];

export function buildUniversalOperationalRecordSet(wave) {
  if (!wave || !Array.isArray(wave.targets)) throw new Error("universal capability wave is required");
  const targetById = new Map(
    wave.targets.filter((target) => target.kind === "operational-skill").map((target) => [target.implementationOwnerId, target]),
  );
  if (targetById.size !== 22 || DEFINITIONS.length !== 22) throw new Error("exactly 22 operational definitions and targets are required");
  const records = DEFINITIONS.map((definition) => {
    const target = targetById.get(definition.id);
    if (!target) throw new Error(`missing certified target for operational definition: ${definition.id}`);
    return {
      schemaVersion: 1,
      id: definition.id,
      title: definition.title,
      description: definition.description,
      intent: definition.intent,
      ownerGodskillId: target.categoricalOwnerId,
      supportingGodskillIds: definition.supportingGodskillIds,
      sourceBinding: {
        targetId: target.targetId,
        clusterDigest: target.clusterDigest,
        overlapDigest: target.overlapDigest,
        reviewDigests: target.reviewDigests,
        comparisonDigests: target.comparisonDigests,
      },
      useWhen: definition.useWhen,
      doNotUseWhen: definition.doNotUseWhen,
      inputs: definition.inputs,
      operations: definition.operations,
      outputs: definition.outputs,
      allowedEffects: definition.allowedEffects,
      requiredAuthority: definition.requiredAuthority,
      forbiddenEffects: definition.forbiddenEffects,
      preconditions: definition.preconditions,
      failureBehavior: definition.failureBehavior,
      exclusions: definition.exclusions,
      terminationCondition: definition.terminationCondition,
      measurableImprovement: {
        baseline: `the current ${target.categoricalOwnerId} route without this exact operational mechanism`,
        metric: "critical mechanism contract cases passed",
        threshold: "all critical cases pass with exact evidence and no authority or effect regression",
      },
      evaluationCases: casesFor(definition),
    };
  }).sort((left, right) => left.id.localeCompare(right.id));
  if (new Set(records.map((record) => record.id)).size !== 22) throw new Error("duplicate operational definition id");
  return {
    schemaVersion: 1,
    recordSetId: "universal-operational-capabilities-v1",
    sourceWaveDigest: wave.waveDigest,
    records,
  };
}
