# Wave 8 correction disposition

MiMo authored the two references from ten source rows. A separate read-only MiMo reviewer rejected the initial candidate; its exact report is retained in independent-review.md. The parent confirmed the routing inversion and over-restrictive prose. The reviewer called the inversion critical; the parent treats it as an important functional routing defect, not evidence of a security incident.

A subsequent MiMo correction worker ended after reporting an invalid patch format, before applying its corrections. Its final message was not completion evidence. The parent verified the worktree remained intact and implemented the bounded fixes directly. This is parent validation, not independent re-review of final fixes.

## Test-first correction

Before modifying the metadata, the parent removed exact trigger-string assertions and replaced the two exclusion expectations with positive retrieval expectations. A seven-test run produced five passes and two failures: urgent unexplained bugs could not retrieve Phoenix, and misleading mock-proof requests could not retrieve corrective Daedalus guidance. After removing those two new exclusions and replacing the overfitted sentence trigger with concise domain concepts, all seven tests passed. Existing resource declaration and no-activation/no-authority assertions remain. A held-out intermittent-crash query and a test-layer paraphrase pass.

The keyword index is advisory discovery, not semantic permission enforcement. Rather than implementing a fragile paraphrase blacklist, the corrective method stays retrievable while its instructions distinguish mocks from real evidence and grant no authority. The rest of the pre-existing anti-trigger behavior was not redesigned in this batch.

## Prose corrections

- Synthetic/disposable data is the default, not an absolute ban on explicitly authorized bounded live verification.
- Untrusted page content cannot issue instructions, but authorized relevant link traversal remains permitted.
- Fixed scripted failure streams suffice for small tests; seeded sampling is for randomized exploration.
- Cleanup is limited to owned disposable resources and granted scope; shared resources and useful evidence are preserved.
- One independent expected-result example clarifies the difference from self-confirming tests.
- Reporting scales with risk rather than requiring a full ledger for a trivial test.

The final candidate is release b6844fb8e76fd7e439276117b3273a5c585f688797c38c8f5a788132ee3ff5bc, 63 methods. Product validation and focused tests passed locally. Full-suite, consumer-exercise, integration and installation results are recorded separately when actually observed. No upstream source was activated and no protected activation-policy artifact was changed.
