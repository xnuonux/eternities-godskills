# Wave 10: governed vocabulary validation

## Contract and source review

Outcome: a coding agent building a consumer for structured records can separate format acceptance, membership in a versioned vocabulary, relationship applicability, completeness and real-world truth. Inputs are records, the declared vocabulary/rules and intended consumer. Outputs are source/rule-bound diagnostics and a verified authorized repair when requested. Effects are read-only validation unless edits were authorized; publication and other effects retain separate authority checks.

Use for governed annotations and machine-consumed terminology. Skip the extra reference for ordinary format checks, a simple static value edit or conversation. No vendor, account, local path, ontology package or external service is mandatory. Missing rules, ambiguous mappings and unsupported formal reasoning remain explicit unavailable scope. Finish at the requested validated slice, authorized repair, or a documented missing-rule/ambiguity boundary.

All four exact family entrypoints were read by MiMo; parent independently read the same four bodies and verified SHA-256, current Git commit, working-file blob and commit:path blob against intake before authoring. Three bodies were shallow launchers rather than methods. The ontology validator contributed a distinct mechanism; exact decisions and license uncertainty are retained in source-dispositions.json. Linked upstream scripts and references were neither imported nor executed; no complete dependency-closure or upstream runtime claim is made.

## Authoring and tests

MiMo wrote initial discovery tests and observed them fail, then its provider stream ended with `idle timeout waiting for SSE` and CLI exit 1. It wrote no product edits or final report. The parent confirmed terminal state before continuing inline; no duplicate inference attempt was launched. Parent removed text-regex assertions that could not establish behavior, retained the discovery cases, and added ordinary-use/exclusion checks. Before editing, three route cases failed and two baseline cases passed.

Parent independently authored one conditional reference under structured-output-contracts, broadened the entrypoint from model-only output to machine-consumed records, and added bounded terminology metadata and provenance. After rebuilding, all five discovery tests passed. They establish search/resource registration and authority boundaries, not semantic determinism or superior agent performance. Existing authority/activation remain none. The new reference is lazy-loaded; no other skill's normal instructions expanded.

No new top-level skill was created: the pack stays at 63 methods. This is a useful extension to an existing capability, not four skills mechanically relabeled as Godskills. The candidate remains instruction-reviewed pending the separate review record; no new model-consumer exercise or superiority experiment was run. Product release and final integration/installation verification are recorded in the final disposition when observed.
