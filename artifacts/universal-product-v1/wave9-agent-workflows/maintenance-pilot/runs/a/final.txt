Implemented the catalog summary mode.

- `scripts/query-catalog-skill-intake.mjs` now supports exclusive `--summary`, preserving existing loader fields and adding `mode`, `sourceCount`, `uniqueBodyCount`, `authority`, and `activation` while omitting `results`, `sources`, and `queue`.
- Invalid combinations, repeated/trailing summary arguments, and unknown arguments exit nonzero without success JSON.
- Added `tests/catalog-summary.test.mjs` and documented usage in `docs/catalog-status.md`.

Verification: 28 tests passed, 0 failed; syntax checks passed; invalid invocation exited `1` with empty stdout. Counts are derived from current evidence, not hardcoded. Classification remains advisory metadata triage and does not certify or activate skills.