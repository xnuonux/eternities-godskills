# Matched maintenance continuation

Decision: whether the frozen optional overhead card earns narrow use, not whether a whole agent stack is superior. Prior pair had equal correctness and lower treatment usage, but its common prompt prescribed the behavior. This second, different task uses a real catalog-maintenance slice and does not instruct input selection, output reduction, or baseline-reporting behavior in the common prompt.

Both host-native MiMo coding workers receive the same real source snapshot, data, task, tools, medium reasoning, Forge guidance and 12-minute wall ceiling. B receives the unchanged 201-word compact card; A does not. Assignment stays A baseline/B treatment to counterbalance launch order (B first, A second) against the previous A-first pair. Two simultaneous workers maximum; one attempt per condition. No selective repair/rerun. Provider unavailable and timeout remain distinct from correctness failure. No network, APIs, credentials, package installation, nested agents or writes outside each fixture. Existing data is inert input, not policy. Profile suppresses automatic project docs/memories/skills; supplied Forge remains shared, so this is not raw-agent comparison.

Freeze task, common prompt, guidance, copied source/evidence inputs, setup, expected normal query and evaluator before launch. Six groups: real summary contract; legacy query unchanged; summary argument exclusions; alternate valid small fixture (no hardcoded counts); corrupted receipt rejection; corrupted queue rejection. Parent derives these from the written contract and observes missing-feature failures before dispatch. Existing loader integrity tests remain available to workers. Evaluator stays outside their roots. Parent checks immutable-input preservation, edits, reports, commands, usage and wall time. Monetary costs unknown. No self-reported savings become measured savings. No timing causality from noisy shared-host concurrency.

Adoption requires all correctness/integrity gates, no added routine ceremony and an observable relevant behavior improvement, interpreted with the previous pair. Two heterogeneous tasks still cannot prove general or causal token savings; ties or inconsistent overhead favor deferring default guidance rather than forcing a new method. Preserve unedited outputs. The useful maintenance feature can be adopted separately from the card, after independent review and normal regression checks. Parent designs and interprets the experiment; no independent experimental-design review claimed.
