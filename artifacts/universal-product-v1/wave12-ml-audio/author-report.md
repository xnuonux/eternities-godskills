# Wave12 ML/audio authoring report

## Result

One independently authored scientific validation method is ready for independent review at `proposed-product/skills/scientific-surrogate-validation/`. It focuses on the gap between scientific prediction evidence and the adjacent owners' work on artifact reuse, agent comparisons, and inference/compute selection. The audio lane adds no second skill: the useful speech chunking and pronunciation patterns are already reviewed in the wave5 draft, while the other audio bodies either advertise capabilities without a method or depend on particular middleware.

This is an authoring sidecar only. It does not change the installed or published core66 release. Dom's corrected baseline is installed release `f19243979f1fe5324bf7b16621cf6a8f3ed57916e3fa4544262c0b99e1a195b7`, main `7438872`; the present worktree HEAD is `7438872d9ac89762608f4b05ef213298eb4b1a50`. No catalog, product skill, ledger, install, commit, or push was changed.

## Neutral contract

The method's outcome is a validation design and bounded result for a scientific predictor used in screening, optimization, or data acquisition. Inputs are identified data and labels, feature and model identities, the intended decision domain, correlated groups, decision-relevant metrics and constraints, and complete candidate-selection records when evaluation is adaptive. Outputs are a frozen protocol, split and data identities, matched baseline and candidate results, group and coverage breakdowns, uncertainty checks when appropriate, excluded and failed cases, and unresolved assumptions.

The method freezes the claim and split before scoring, keeps correlated cases together, fits learned preprocessing within training folds, protects an outer holdout from model selection, compares against relevant baselines on the same cases, and examines errors and uncertainty across groups and training-domain coverage. Adaptive-campaign claims require the full pool and records for selected, unselected, failed, and negative outcomes. The method has local analysis effects only; it does not authorize training, acquisition, publication, deployment, or an automated scientific decision.

It must stop without a generalization claim when the final holdout influenced choices, correlated cases cross the split, target definitions or units conflict, outcomes were dropped after observation, or the available evidence cannot support the intended domain. Further details and exact per-source dispositions are in `source-dispositions.json`.

## Source accounting and integrity

The fixed plan has 14 `ml-training-evaluation` and 7 `audio-production-analysis` leads (21 total). Its SHA-256 is `4e68ad4b393440c5f602e6331ae9f824a9f17672d939a347fd00d3ef8d53c6a6`. The packet binding is release `8de2600c40fab57bfbe8b26b50ca7ccfa3351cda02eaf6e86fe4263dd704bee4`, queue digest `7db62307c0198e669eff2e82bce9fd3f1fc72b6bac6c20c6ddc7e13588b22d9d`.

All 21 identities resolved through the family packet and the `data/quarry-intake-2026-09-21-exa/sources.jsonl` acquisition index. For each, this pass checked the actual warehouse path, byte count, SHA-256 against the pinned plan, Git blob, and checkout HEAD against the pinned commit. All 21 matched. This hash and Git check proves identity only.

Read provenance is deliberately separate. Eighteen complete source bodies were recovered from the earlier worker's captured tool output; this authoring pass did not perform those original reads and does not claim them as its own. The hash-bound recovery assessment reports their output ranges cover each full body; the 34,793-character molecular GNN and 33,534-character surrogate-validation bodies were split across ranges. Three audio bodies (`r0100`, `r0306`, `r0319`) reuse exact prior body reviews and dispositions in the wave5 audio sidecar, matched by source ID and body hash. The other captured source code remained inert. No linked reference was needed for the adopted generic mechanisms, and no acquired script was executed.

The 21 per-source records specify retained mechanisms, rejected or deferred parts, the existing owner or draft, and evidence limits. Three prior exact source dispositions were found and reused (`r0100`, `r0306`, `r0319`). No prior exact record matched the other 18 body hashes. Intake license labels are provenance clues only; where absent or conflicting, uncertainty remains. No legal clearance is claimed.

## Comparison and design

The existing `experiment-artifact-lineage` entrypoint handles continuity and safe reuse of artifacts, including split-membership and leakage checks. `counterbalanced-agent-evaluation` handles controlled comparisons of agents, prompts, and harnesses. `eternities-hephaestus` qualifies inference and compute options. The audio owners handle supplied-media provenance and alignment (`eternities-orpheus`) and separate DSP deadline, numeric, and graph review (`audio-dsp-integrity-review`). These five current product entrypoints were read in full; their byte counts and SHA-256 values are in `author-receipt.json`.

The distinct method retained from the two substantive scientific bodies is narrower than either source: define the intended prediction domain before scores; use grouped, leakage-resistant holdouts; keep preprocessing and selection within training folds; compare relevant baselines on identical cases; report error by group and coverage; test held-out uncertainty with explicit assumptions; and preserve the whole adaptively selected candidate pool, including failures and negative outcomes. Molecular architecture code, materials-specific features, model recipes, thresholds, package versions, GPU requirements, unsupported physical rules, and vendor or framework directions were excluded.

The draft is fresh language, has no package/provider dependency, machine path, mandatory private system, or cross-skill Markdown link, and is marked `draft`. Its metadata relates by IDs to the three adjacent product methods. The audio sources add no distinct portable method beyond the existing wave5 speech chunk alignment draft and owner boundaries; wave12 records their reasons without duplicating that work.

## Cases and observed checks

`check-cases.json` records direct, paraphrased, exclusion, conflict, and boundary scenarios. The staged validator checks exact plan counts and hashes, one-to-one source and disposition accounting, current-byte/Git/commit match fields, draft metadata and portability constraints, required boundary language, all five case classes, and the ready-for-review/adoption boundary.

Command: `node artifacts/universal-product-v1/wave12-ml-audio/validate-wave12.mjs`

Observed result: `PASS: 21 exact plan leads, source bindings, draft portability, and five static contract cases.` The validator also reports: `LIMIT: structural and authored-case checks only; no model, audio, or agent-performance evaluation.` These are static contract checks and authored routing cases, not runtime expertise, source-code execution, an independent review, or evidence that the method improves model performance.

## Unresolved limits and next review

Repository-level license completeness remains uncertain for some sources; labels in intake or source frontmatter are not legal advice. The captured wave12 source bodies were reviewed in their recovered output, not recaptured by this authoring agent. No scientific dataset, model, uncertainty estimate, adaptive campaign, audio signal, renderer, or human listener was exercised. No whole-corpus completion or empirical-superiority claim is made. The previously failed worker's immutable receipt and unknown usage settlement remain untouched; this pass used no worker, provider call, GPU, training, or external service.

Next action: independent review the source dispositions and the candidate's scientific-method boundaries, especially whether the selected/unselected adaptive-pool requirement and uncertainty assumptions are sufficiently clear. Keep the candidate draft-only until that review and a separate authorized agent-performance or scientific-data exercise; parent owns any integration.
