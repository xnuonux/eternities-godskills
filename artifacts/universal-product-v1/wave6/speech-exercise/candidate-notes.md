The declared budget is six extended grapheme clusters, so the protected “Dr. Vega” span is retained as one eight-cluster blocked chunk rather than split or converted to an engine-specific spelling. The remaining text is divided at source boundaries into fitting chunks; spaces and punctuation remain accounted for, including whitespace carried at chunk edges.

This is a text-only plan. No renderer, audio, listening, or seam validation occurred, and the supplied reading remains unverified. The historical r0 “complete” record is stale for current r1; the r1 record is cancelled and only metadata reports a partial file. Without independently verified audio artifacts or receipts, those records cannot form a completed current-revision result. The next step is to confirm renderer capacity, obtain fresh r1 output, and verify emitted segments before assembly.
