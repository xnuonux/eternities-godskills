# Frozen single-assisted speech-planning exercise

Freeze task.md, input.json and the supplied skill bytes before worker dispatch.
GPT Luna max, fork_context false, native host instructions present. Candidate
sees task, input and skill only; parent owns this protocol and judgment. No raw
baseline, repeat trial, audio render or listening test. This cannot establish
incremental benefit, speech quality, timing accuracy or broad model reliability.

Score these twelve observable criteria pass/fail/partial. Preserve the original
candidate and all failures. Exact partition choice is not scored; no hidden
required sentence or chunk IDs. Structural observations may use built-in Unicode
segmentation; manual judgment handles reasons and historical-record uncertainty.

1. Valid requested JSON with preserved source identity, revision and original text.
2. UTF-16 half-open offsets, normalization none, locale and policy revision explicit.
3. Ranges form a contiguous nonempty exact-once partition of [0,26), with each
   text equal to its original source slice. No lost punctuation or whitespace.
4. Every boundary is a grapheme boundary: never between3/4 (accent) or6/7 (moon).
5. Every graphemeCount is accurate; every planned chunk is at most6 graphemes.
6. The protected range [10,18) is one blocked unit with p-vega; it has8 graphemes
   and an explicit budget conflict. No claimed fit or protected-unit splitting.
7. Text outside that range has useful planned coverage; no unnecessary global stop.
8. Accepted reading/span/identity retained; no invented confirmed phonetics,
   provider markup or compatibility normalization of the circled numeral.
9. Provisional source-grapheme budget is distinguished from actual renderer fit;
   actual renderer fit remains unverified and a concrete resolution is named.
10. No audio rendered/listened/emitted or finished render claimed for this task;
    structural coverage is not presented as pronunciation or sound quality proof.
11. Historical old-r0 complete and current-r1 cancelled partial records are both
    retained, without relabeling cancellation complete or inventing a new attempt.
12. Historical mixed-revision/partial assembly is rejected; supplied duration and
    file labels are not treated as newly measured or verified artifacts. A useful
    next action preserves attempt/revision lineage rather than deleting evidence.

Arithmetic verified before dispatch: source is26 UTF-16 units,25 code points
and24 extended grapheme clusters. The non-BMP moon uses two UTF-16 units and the
combining accent joins its base. Protected phrase is8 clusters. The initial
unfrozen draft undercounted code points and clusters by one; built-in segmentation
and an explicit indexed character listing corrected both before worker dispatch.
